# AI 协同开发铁律 (System Rules)

1. 【契约神圣不可侵犯】：你无权修改 `/supabase/migrations/` 下的 DDL 文件、`/src/lib/contracts.ts`。如果实现当前功能需要修改底层 Schema 或跨模块接口，必须立即停止并向人类报告，绝不能擅自修改！

2. 【单一职责与微会话】：你当前只负责 [当前模块名称] 的开发。请勿触碰、重构或"优化"任何其他模块的代码。

3. 【测试驱动开发 (TDD)】：在编写任何业务逻辑前，必须先根据验收标准编写单元测试。业务逻辑代码必须以"通过测试"为唯一目的，通过后立即停止，禁止过度设计。

4. 【证据链与信用隔离】：任何涉及信用的代码，禁止直接 `UPDATE credit_records`，必须通过引用 `evidence_log_id` 触发；Bandit 算法模块禁止导入任何包含 `credit_records` 的类型或查询。
