# 宪法收敛登记表（CONVERGENCE LOG）

> 依据：`docs/DESIGN_CONSTITUTION.md` §2 ——「已有代码与本文不符的（历史遗留）不算违规，
> 但每个结构性改动（重构/抽层）必须顺带收敛一处，并在改动中标注『宪法收敛：条文 #n』。」
>
> **本表是唯一事实来源**：一切结构性改动（文件 rename / 抽层 / 契约修订）合入前，
> 必须在此追加一行；`npm run check:convergence` 门禁会自动核对 git rename 记录是否
> 与本表对应，缺行即报错，禁止合入。
>
> 登记人：opencode agent（受 AGENTS.md 约束）。

| 日期 | Commit | 结构性改动 | 收敛的条文 | 收敛说明 |
|------|--------|-----------|-----------|----------|
| 2026-07-23 | `382663d` | SW 源码移动：`src/app/sw.ts` → `src/app/sw.js`（修复 Vercel 构建，serwist manifest 注入冲突） | §2 历史遗留（宪法定稿 08-13 前） | 追认登记：PWA 构建文件位置调整，非九域结构化重构；补记确保登记册完整 |
| 2026-07-23 | `0a64fbd` | SW 源码移动：`src/app/sw.js` → `sw.js`（移出 App Router 编译路径） | §2 历史遗留（宪法定稿 08-13 前） | 追认登记：sw.js 挪到项目根，next.config swSrc 同步；补记保持登记册唯一事实来源完整 |
| 2026-07-23 | `7f868c3` | SW 源码回移：`sw.js` → `src/app/sw.ts`（移除 webpack-obfuscator 冲突后恢复 sw.ts） | §2 历史遗留（宪法定稿 08-13 前） | 追认登记：最终形态恢复为 src/app/sw.ts；三次移动（ts→js→root→ts）终态收敛，补记闭环 |
| 2026-08-13 | `7754884` | base 九域 git mv 100 文件 + ammo 四表建立 | #1 底座优先 / #3 先配表后写码 | 从 lib/ + src/ 平铺收敛为「base/ 发射管 + ammo/ 弹药表」形态（ADR-0006/0007 落地） |
| 2026-08-13 | `c74b20f` | ADR-0007 v2：C1 OrderCore 抽象 + ammo/sop.ts 补建 + C4 hardGates 对齐 | #1 / #2 接口保守 / #3 | 状态机语义收敛进 OrderCore 契约锚点；SOP 弹药表不写死进 base 组件 |
| 2026-08-13 | `5cb0d44` | ADR-0008 争议小法官：judge 规则 + Gateway 接线 + 挂载 | #7 LLM 可介入就介入 | 争议定责从纯状态机收敛到规则引擎 + LLM 复核定责链 |
| 2026-08-13 | `004bb23` | ADR-0009 Sentinel：发布闸门 + 事件流接入 store | #9 多防线一体 / #10 降级设计 | 反欺诈从 roam 单点拦截收敛为多因子聚合 + 缺因子降级 |
| 2026-08-13 | `4da1c5e` | 0010-0015 批次：`lib/chat/` → `base/ai/chat/` rename；transport union 新字段；useWaveStore 多域状态扩充 | #1 / #2 / #4 弹药可插拔 | chat 引擎从业务目录 lib/ 收敛归位 base/ai；IM/隐私号/表单/geo 适配全部底座化 |
| 2026-08-13 | `a6edc57` | RPG 哲学原文从桌面断链 → docs/ 入仓 | §5 出处闭合 | 宪法出处文档从「仓库外引用」收敛为「仓库内 git 跟踪」 |
| 2026-08-13 | `97d1381` + `619a332` | ADR-0016：`base/safe/ageGate.ts` + `base/platform/quietHours.ts` + `ammo/risk-rule` age-required 引信 + 全量接线（identity birthYear/guardianConsent、PublishSheet 发布分派、ProfilePage 设置 UI、NotificationCenter shouldNotify 分流） | #8 隐私血液 / #4 弹药可插拔 / #10 降级 | 未成年人合规从「未定义」收敛为分级模式（法规对齐）；推送从「无免打扰」收敛为用户自主静音（不绑付费） |
| 2026-08-14 | `6ede2fb` | **base/ammo 共享层提升至仓库根**：`oto-spatial-web/src/base/`（125 文件）与 `oto-spatial-web/src/ammo/`（11 文件）git mv → 根 `src/base/`、`src/ammo/`（136 rename 0 新增，保历史）；路径重映射（子/根双 tsconfig `@/base/*`、`@/ammo/*` 指向根 src、base 内 `@/store/*` 跨包桥接、destFilter 单点断链改跨包相对路径、子 test:units 51+4 前缀改 `../src/`）；子项目全量单测 425/425 全绿 + 根/子双 tsc 0 错 | #1 底座优先 / #2 接口保守 | 共享层从子项目业务目录平铺收敛为仓库根底座（移动纯 rename、零逻辑改动，接口契约不变）；根 tsc/vitest/eslint 对 base/ammo 暂以 exclude 隔离（类型检查由子 tsc 经 paths 承担），待 Phase 3/4 接线时开放 |
| 2026-08-14 | `a66510c` | **子 11 个 API 路由迁入根并收敛（D-08 落地）**：`chat` → `src/app/api/waves/chat/route.ts`（waves 对话改名，根协议 `/api/chat` 保持）；`asr/tts/voice-intent/gateway/cluster/decompose/diagnose/judge/push/subscribe/push/send` 11 路由 git mv 至根；**asr 双语义合流**：根旧 `api/ai/asr`（LLM 协议提取）与子 GLM-ASR 转写合并为单一 `/api/asr`（`file`→转写 `{text}`、`rawText/audio`→协议提取 `{success,text,protocol}`），旧 `ai/asr` 删除、`VoiceInput` 改指 `/api/asr`；waves 对话实际调用方 `base/ai/chat/llmEngine.ts` fetch 改 `/api/waves/chat`；根侧 `ai/push-recommendations` 语义互补保持；`dimension3-ai.test.ts` 同步至新 `/api/asr`；验证：根/子 tsc 0 错 + 子单测 425/425 + 根 vitest 回落存量基线 11 failed（无新增） | #1 底座优先 / #2 接口保守 | API 面收敛：waves 对话与协议对话拆分为 `/api/waves/chat` 与 `/api/chat` 双端点（语义隔离）；语音链双实现 → 单一 `/api/asr`（接口契约合并，调用方同步）；路由 import 基线全部指向根 `@/base/*` 与根依赖，无跨包残留 |
| 2026-08-14 | `2ce4c48` | ammo 四表 → base 引擎接线批次：broadcastMatches/passesHardFilter/requiresVerification 增 rule 参数（DEFAULT_BROADCAST_RULE 等价原常量）；riskOf/extraLogin 增 params 参数（DEFAULT_ROAM_PARAMS 等价原阈值）；WaveFeed/useWaveStore/useRoamStore/RoamGuardPanel/AdminPanel 从 ammo 表取值注入；BiddingSandboxCard reserve 地板价；PublishSheet SOP 预填 + 定价建议 + 有效期档表驱动；ammo/sop 补「羽毛球约局」别名 | #3 先配表后写码 / #4 弹药可插拔 / #5 引信跟弹药走 | 写死权重/阈值/默认值收敛为 ammo 表驱动（参数注入式，base 保持零 import 可 node:test 直跑）；契约修订仅增补可选参数，未改既有语义 |
| 2026-08-14 | `941a54a` | **Phase 4 UI/Store/路由/PWA 深度融合**：58 组件 git mv 入根（29 → `src/components/waves/`；29 → `src/components/oto-ui/` 含 3d/admin/chat/destinations/profile 子域；`Badge.tsx` 改名 `OtoBadge.tsx` 防 Windows 大小写撞车并全量重映射引用）；7 store 提升 `src/store/` + 8 lib（mockData/mockResponders/dial/scan/qr 及测试）提升 `src/lib/`；子 5 屏 SPA 入 `src/app/(oto)/` 路由组（嵌套 layout 承载 ToastHost/PwaServiceWorker/Geist + `globals.css` 隔离为 `.oto-app` 作用域 + `--oto-*` 变量前缀，不覆盖根 shadcn 主题）；根 Header 增 OTO 入口；子手写 sw.js 协议（PRECACHE/SKIP_WAITING/push/notificationclick）并入根 `src/app/sw.ts` 并删除子 sw.js；public 资源归并（lounge.glb/models、manifest.json、oto-icon 改名防同名冲突）；8 处根业务 `React.ElementType` → `LucideIcon` 修复 R3F v9 JSX namespace 合并引发的 props never 污染。验证：根/子 tsc 0 错 + 子单测 425/425 + 根 vitest 存量 11 failed 不变 + 门禁 exit 0 | #1 底座优先 / #2 接口保守 | 组件/store/lib 目录平面结构提升为根侧收敛目录（waves/oto-ui 域划分 + 路由组隔离 + PWA 单实现归一），逻辑零改动仅物理落位 + 类型标注收敛；Badge→OtoBadge 为 Windows 大小写敏感性防撞改名 |
| 2026-08-15 | `afce408` | **Phase 5 测试合流与缺陷清零**：`(oto)/` 路由组 → `oto/` 真实目录（`(oto)/page.tsx`/`layout.tsx`/`globals.css` 三文件 rename；路由组不参与 URL 路径，`(oto)/page` 与根 `page.tsx` 同为 `/` 冲突导致 5 屏 SPA 构建期被根页面吞并、`/oto` 生产 500 —— 修复后 `/oto` 正常输出 5 屏 SPA HTML）；子 57 node:test 保持幂等不迁移（实测 vitest 收集 `node:test` 语法文件 57/57 "No test suite found"，D-05 裁决维持 node:test 通道）；根 `npm test` 组合为 `vitest run`（426）+ 子 `test:units`（425）一键 851 全绿；vitest.config.ts 补 `@/base` `@/ammo` `@/store` alias；11 个存量 vitest 失败清零（AI 组 4 测试入参对齐 `NegotiateInput`、SLA 组 mock `getServiceClient`+`getNextFundStatus`+`single` 链、Milestone 组 mock 链补 `select/single/maybeSingle`、confirm 实现补清 `auto_confirm_at`、credit-engine mock 补 `getNewbornProtectionFactor/getWeekendMultiplier`）；e2e 12 个 `e2e-*.mjs` 留存无损；`npm test` 851/851 + 根/子 tsc 0 错 + `next build` exit 0（121 静态页，serwist /sw.js 注入，`/oto` 生产可访问） | #1 底座优先 / #2 接口保守 | 路由从"(oto) 路由组语义路径"收敛为真实 URL 目录（消除与根 page 的 `/` 冲突，OTO 5 屏 SPA 恢复可达）；测试体系双通道（vitest + node:test）按 D-05 幂等并轨，`npm test` 单命令全量验证；存量测试契约（mock 链/入参/实现语义）对齐修复，无逻辑复杂度新增 |
| 2026-08-15 | `1b1eb64` | **Phase 6 终态归档**：16 脚本从 `oto-spatial-web/scripts/` git mv 至根 `scripts/`（12 e2e-mjs + verify-prod/restart-prod/export-sofa-glb/generate-vapid）；随后 `oto-spatial-web/` 整体废弃移除（68 tracked 文件 git rm）；`test:oto:units` node:test 425 清单从子 package.json 迁根 package.json 直跑；workspaces 收敛 `["mobile","packages/*"]`；tsconfig 删 `@oto/*` 别名与 exclude、vitest/eslint exclude 同步；`dev-all.mjs` 单进程化（:3000）；README/PROJECT_STATUS/MERGE_PROGRESS 单仓口径重写；终态回归 tsc 0 错 + `npm test` 851/851 + build exit 0 + 门禁 exit 0 | #1 底座优先 / #2 接口保守 | E2E 与运维脚本从子项目业务目录收敛为根 `scripts/`（单一工具链入口，BASE=:3000 语义不变）；子项目双应用部署形态收敛为单仓单体应用（workspaces 收窄、别名/exclude 清理、node:test 测试通道根内化），原 47 页 + 5 屏 + 105+ API 全部归位根应用 |
| 2026-08-15 | `a11d85e` | **底座反腐外科手术（D1~D3 治理）**：D1 拔除底座反向依赖——llmEngine/mockEngine 移除 useAppStore 运行时 import，新增 ChatEngineContext 契约（getChatMessages/isWorkerOnline），ChatPage 显式注入；D2 越级契约上收——WaveBundle 自 store/useWaveStore 提升至 src/types/wave-bundle.ts（新文件，transport/supabase 改引契约）；D3 业务词剥离至弹药层——sentinel 进家词表→ammo/risk-rule 引信 params.homeAccessKeywords（新增 homeAccessKeywordsFor）、broadcast DEFAULT_BROADCAST_RULE.requiresVerified 置空（ammo dispatch-rule 装填）、booking ICON_RULES→ammo/scene-template CATEGORY_ICON_RULES（iconFor/applyBooking/bookingWorkerOrder 增注入参数）；生产调用方 CapabilityPanel/WaveFeed/useWaveStore/useAppStore 全量改注入；测试适配弹药注入语义并补「未装填→通用兜底」断言；验证：tsc 0 错 + npm test 856/856 全绿 + 门禁 exit 0 | #3 单向依赖 / #4 引信跟弹药走 | 无文件 rename（WaveBundle 为新建 src/types 契约 + store 本地定义删除），契约修订类收敛；ChatEngine/iconFor/applyBooking/bookingWorkerOrder/requiresVerification/sentinelCheck 签名入参化 |
| 2026-08-15 | `87d28bf` | **核心设计模型契约固化（人类创始人注入）**：白皮书新增 §二（五态原子状态机 PUBLISHED-MATCHED-IN_SERVICE-INSPECTED-SETTLED + 跃迁矩阵 + 伴生事件 Sub-Events 插拔 + 与现有 wave 状态机过渡映射 + 三类引信矩阵 IMPACT/DELAY/PROXIMITY + 数字人格信用飞轮）；新建 src/types/ammo-schema.ts 与 src/types/fuze-policy.ts 纯类型契约（零业务依赖，tsc 0 错 / 856 全绿）；原白皮书章节顺延（三~六） | #2 接口保守 / #5 引信跟弹药走 / #6 信用跨弹药累积 | 契约新增（无 rename）；五态为目标模型，现有 wave 状态机按宪法 #2 渐进收敛映射，本次不改业务代码 |
| 2026-08-15 | `fad5a8a` | **28 模块主蓝图固化（人类创始人注入）**：白皮书 §三 2.4 升级六层防御圈 x 核心模块职责矩阵（标准模块编号 L1-M1～L6-M4 全项目永久统一）+ 26 行代码落位与成熟度对照表（🟢13/🟡12/⚪️1，L4-M3 待建设）；宪法元宪法声明增补模块编号标准；口径核查：清单净 26 模块与标题 28 差 2 席，编号体系完整成立，差量标注待人类裁决 | #1 六圈定位声明（模块编号即定位锚点） | 纯文档批次（无 rename 无代码变更），856 基线不动，门禁 exit 0 |
| 2026-08-15 | `cab6f33` | **三阶段推进路线图固化（人类创始人注入）**：白皮书新增 §四（Phase 1 MVP 验证期 0-1 housekeeping.ammo.ts 碰炸引信 / Phase 2 体系成熟期 1-10 meetup 延期 + companion 近炸 / Phase 3 规模壁垒期 10-100 弱网容灾熔断存证），每阶段绑定模块编号 + 缺口衔接 + 现状标注；顺带修复小节编号一致性（§三 2.x→3.x、落差审计 3.x→5.x） | #1 六圈定位（模块编号即定位锚点） | 纯文档批次（无 rename 无代码变更），856 基线不动，门禁 exit 0 |
| 2026-08-15 | `W1W2W6` | **W1~W6 生产接线总装（第一批次）**：Wave 契约增补 `ammoId` 可选字段（`base/order/wave.ts` Wave/CreateWaveInput/createWave 三处透传，零破坏）；`PublishSheet` 接入 `DynamicDraftCard` 弹药草稿预览 + `resolveAmmoIdForPublish` 中文品类归一化直挂官方弹药（家政保洁→housekeeping-v1 / 羽毛球约局→meetup-social-v1）；`oto/page.tsx` HomePage 顶部挂 `StatusCapsule` + `toAtomicFiveState` 实时投影；`layout.tsx` 挂 `OnlineStatusBridge`（navigator.onLine 桥）；`ProfilePage` 安全中心新增「👵长辈模式」+「🛡️应急伪装」入口与覆盖层（静默报警直连 raiseCrisis）；暴露并修复 3 处 base 层预存 tsc bug（runner.ts ISubEventResult 未 import + HookOutcome.fallbackUsed 缺 BLOCK——因 tsconfig exclude src/base 长期未被类型检查，接线后纳入编译图）；修复 OfflineQueueIndicator 快速切换时恢复 Toast 覆盖离线条缺陷；新增连线测试 wiring.test（6）+ OnlineStatusBridge.test（4），960/960 全绿 + tsc 0 + 门禁 exit 0 | #3 单向依赖（契约增补 ammoId 可选字段）/ #4 引信跟弹药走（ammoId 落库供插槽装载） | 契约修订（Wave 增 ammoId 可选字段，向后兼容）+ base/ammo/runner 类型修复（类型对齐，运行时零变化） |
| 2026-08-15 | `W3W4W5W7` | **W3~W7 全链路接线通电总装（第二批次 · 终极闭环）**：W7 新建 `ArbitrationSheet`（半屏上滑争议抽屉：物证比对链[投诉诉求/完工照片+AI 视觉标注/聊天记录] + AI 小法官建议卡[退款/补偿券/责任/信用/理由链，仅 Advisory] + 隔离墙双出口[接受调解→BREACH_SETTLED / 申请人工→资金冻结]）；W3 新建 `FulfillmentCenter` 并挂载 Trip 屏（进行中 Wave 投影三态 MATCHED/IN_SERVICE/INSPECTED → 顶部主视口优先渲染 FulfillmentCockpit，ammoId→scenario 毫秒自适应插槽）；W4 插槽事件接线（保洁增项改价确认→订单总额动态更新 / 组局 500m 围栏扫码→定金解冻 / 陪玩伪装假电话→全屏来电遮罩接听挂断脱身）；W5 核销 CTA 生产首次调用 `advanceLifecycle` 真实引擎（MATCHED→IN_SERVICE→INSPECTED→SETTLED 全链），流转结果 `setFulfilment` 回写 store → toAtomicFiveState 投影 → 顶栏 StatusCapsule 实时流转 🟢；registry 新增 `getAmmoById`（ammoId 反查整弹）；store 新增 `fulfilment` 回写位（只增不改）；新增 E2E 装配回归 18 例（ArbitrationSheet 6 + 纯函数矩阵 4 + 端到端流转 4 + 插槽接线 4），**978/978 全绿**（vitest 488 + node:test 490）+ tsc 0 + 门禁 exit 0 | #3 单向依赖（store 增 fulfilment 回写位、仅投影消费）/ #2 接口契约（advanceLifecycle 生产首次调用，引擎接通） | 契约修订：store 增 fulfilment/setFulfilment（向后兼容零触碰）；base/ammo/runner 生产首次被 UI 层真实调用（类型已修，无运行时变化） |
