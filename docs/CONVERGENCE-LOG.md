# 宪法收敛登记表（CONVERGENCE LOG）

> 依据：`docs/DESIGN_CONSTITUTION.md` §2 ——「已有代码与本文不符的（历史遗留）不算违规，
> 但每个结构性改动（重构/抽层）必须顺带收敛一处，并在改动中标注『宪法收敛：条文 #n』。」
>
> **本表是唯一事实来源**：一切结构性改动（文件 rename / 抽层 / 契约修订）合入前，
> 必须在此追加一行；`npm run check:convergence` 门禁会自动核对 git rename 记录是否
> 与本表对应，缺行即报错，禁止合入。
>
> 登记人：opencode agent（受 AGENTS.md 约束）。

| 日期 | Commit | 结构性改动 | 收敛的条文 | 收敛说明 |
|------|--------|-----------|-----------|----------|
| 2026-08-18 | `1ed8b62` | **/api/tts 生产路由 Webpack 空桩崩溃与流挂起根治（无 rename）**：① `next.config.ts` 追加 `serverExternalPackages: ['ws','bufferutil','utf-8-validate']`——根治打包层空桩（webpack 将 ws 可选原生加速器 bufferutil 打成 `()=>{}` 空模块，require 不抛错→ws 内置 try/catch 不触发→掩码 wrapper 安装→首帧发送 `TypeError: b.mask is not a function`→msedge-tts 音频流永不产出/永不结束→路由无限挂起，实测全天刷屏 unhandledRejection）；外置后 ws 走运行时 require，未安装即抛错→被 ws 兜底→纯 JS 掩码回退（dev 一直能用的原因）；② `scripts/restart-prod.mjs` 子进程注入 `WS_NO_BUFFER_UTIL=1` / `WS_NO_UTF_8_VALIDATE=1`（PS 5.1 无 Start-Process -Environment，经 `$env:` 前缀继承注入），双保险；③ `src/app/api/tts/route.ts` 重构：单一 8s 总预算（GLM fetch AbortSignal + edge 消费 `consumeTtsStreamWithDeadline` Promise.race），超时 `tts.close()` 释放套接字 + 确定性 504，消费侧错误双分支收敛杜绝二次 unhandledRejection；④ 新增 `src/app/api/tts/route.test.ts`（vitest 5 例：停滞流超时/快流拼接/空流/报错流/竞速不误伤）；验证：`npm run build` exit 0（产物复核：route.js 无 `b.mask` 包装器、无 bufferutil 字符串）+ 真实 HTTP 实测 POST /api/tts → **200 audio/mpeg 1.64s**（修复前同请求永久挂起）+ 验证实例错误日志 0 unhandledRejection + 正式实例复测 200/2s + tsc 0 + `npm test` **1473/1473 全绿**（vitest 598→603 本次 +5）+ 收敛门禁 exit 0 | #10 降级设计（确定性时限，8s 看门狗）/ 红线 5（失败是设计：504 交客户端 speechSynthesis 降级链） | 契约修订类收敛：TTS 链路从「可无限挂起」收敛为「8 秒内确定性完成或 504」；无文件 rename（git 门禁零检测项）；看门狗逻辑纯函数化可单测 |
| 2026-08-18 | `8b450eb` | **D-3 架构防腐战役：进家业务词汇彻底解耦与引信参数化（无 rename）**：`src/ammo/risk-rule.ts` 新增 `HOME_ACCESS_KEYWORDS_MAP` 权威类目映射（housekeeping/家政保洁/厨师·上门做饭/遛狗遛弯/水电维修/陪诊陪护/按摩推拿 7 键中英双键）+ `homeAccessKeywordsFor(category)` 行为增强（MAP 命中优先 → 引信参数回落 → 空数组，既有调用与断言零改动）；`src/ammo/housekeeping.ammo.ts` 新增显式引信词表导出 `HOUSEKEEPING_HOME_ACCESS_KEYWORDS`（直取 MAP.housekeeping）；`src/base/risk/sentinel.test.ts` +2、`src/ammo/housekeeping.ammo.test.ts` +1（中英双键同表/未装填回落全局引信/弹药键注入 ×1.2/显式装配=权威 MAP）；底座 `sentinel.ts` 复核零业务词零改动；白皮书 §5.2 D-3 🔴→🟢 并如实标注 decompose/llmEngine 意图识别词残余；验证：tsc 0 错 + 1465→**1468/1468 全绿** + build exit 0 + 门禁 exit 0 | #3 单向依赖（底座零业务词，词表唯一声明点=弹药层）/ #4 引信跟弹药走（进家词表经 MAP + 弹药显式装配注入）/ #5 引信跟弹药走（housekeeping 显式挂载 D3 引信词表） | 契约修订类收敛：homeAccessKeywordsFor 检索语义增强（MAP 优先，未命中回落引信参数，行为向后兼容——既有"家政保洁"路径原样返回可命中词表）；无文件 rename（git 门禁零检测项） |
| 2026-07-23 | `382663d` | SW 源码移动：`src/app/sw.ts` → `src/app/sw.js`（修复 Vercel 构建，serwist manifest 注入冲突） | §2 历史遗留（宪法定稿 08-13 前） | 追认登记：PWA 构建文件位置调整，非九域结构化重构；补记确保登记册完整 |
| 2026-07-23 | `0a64fbd` | SW 源码移动：`src/app/sw.js` → `sw.js`（移出 App Router 编译路径） | §2 历史遗留（宪法定稿 08-13 前） | 追认登记：sw.js 挪到项目根，next.config swSrc 同步；补记保持登记册唯一事实来源完整 |
| 2026-07-23 | `7f868c3` | SW 源码回移：`sw.js` → `src/app/sw.ts`（移除 webpack-obfuscator 冲突后恢复 sw.ts） | §2 历史遗留（宪法定稿 08-13 前） | 追认登记：最终形态恢复为 src/app/sw.ts；三次移动（ts→js→root→ts）终态收敛，补记闭环 |
| 2026-08-13 | `7754884` | base 九域 git mv 100 文件 + ammo 四表建立 | #1 底座优先 / #3 先配表后写码 | 从 lib/ + src/ 平铺收敛为「base/ 发射管 + ammo/ 弹药表」形态（ADR-0006/0007 落地） |
| 2026-08-13 | `c74b20f` | ADR-0007 v2：C1 OrderCore 抽象 + ammo/sop.ts 补建 + C4 hardGates 对齐 | #1 / #2 接口保守 / #3 | 状态机语义收敛进 OrderCore 契约锚点；SOP 弹药表不写死进 base 组件 |
| 2026-08-13 | `5cb0d44` | ADR-0008 争议小法官：judge 规则 + Gateway 接线 + 挂载 | #7 LLM 可介入就介入 | 争议定责从纯状态机收敛到规则引擎 + LLM 复核定责链 |
| 2026-08-13 | `004bb23` | ADR-0009 Sentinel：发布闸门 + 事件流接入 store | #9 多防线一体 / #10 降级设计 | 反欺诈从 roam 单点拦截收敛为多因子聚合 + 缺因子降级 |
| 2026-08-13 | `4da1c5e` | 0010-0015 批次：`lib/chat/` → `base/ai/chat/` rename；transport union 新字段；useWaveStore 多域状态扩充 | #1 / #2 / #4 弹药可插拔 | chat 引擎从业务目录 lib/ 收敛归位 base/ai；IM/隐私号/表单/geo 适配全部底座化 |
| 2026-08-13 | `a6edc57` | RPG 哲学原文从桌面断链 → docs/ 入仓 | §5 出处闭合 | 宪法出处文档从「仓库外引用」收敛为「仓库内 git 跟踪」 |
| 2026-08-13 | `97d1381` + `619a332` | ADR-0016：`base/safe/ageGate.ts` + `base/platform/quietHours.ts` + `ammo/risk-rule` age-required 引信 + 全量接线（identity birthYear/guardianConsent、PublishSheet 发布分派、ProfilePage 设置 UI、NotificationCenter shouldNotify 分流） | #8 隐私血液 / #4 弹药可插拔 / #10 降级 | 未成年人合规从「未定义」收敛为分级模式（法规对齐）；推送从「无免打扰」收敛为用户自主静音（不绑付费） |
| 2026-08-14 | `6ede2fb` | **base/ammo 共享层提升至仓库根**：`oto-spatial-web/src/base/`（125 文件）与 `oto-spatial-web/src/ammo/`（11 文件）git mv → 根 `src/base/`、`src/ammo/`（136 rename 0 新增，保历史）；路径重映射（子/根双 tsconfig `@/base/*`、`@/ammo/*` 指向根 src、base 内 `@/store/*` 跨包桥接、destFilter 单点断链改跨包相对路径、子 test:units 51+4 前缀改 `../src/`）；子项目全量单测 425/425 全绿 + 根/子双 tsc 0 错 | #1 底座优先 / #2 接口保守 | 共享层从子项目业务目录平铺收敛为仓库根底座（移动纯 rename、零逻辑改动，接口契约不变）；根 tsc/vitest/eslint 对 base/ammo 暂以 exclude 隔离（类型检查由子 tsc 经 paths 承担），待 Phase 3/4 接线时开放 |
| 2026-08-14 | `a66510c` | **子 11 个 API 路由迁入根并收敛（D-08 落地）**：`chat` → `src/app/api/waves/chat/route.ts`（waves 对话改名，根协议 `/api/chat` 保持）；`asr/tts/voice-intent/gateway/cluster/decompose/diagnose/judge/push/subscribe/push/send` 11 路由 git mv 至根；**asr 双语义合流**：根旧 `api/ai/asr`（LLM 协议提取）与子 GLM-ASR 转写合并为单一 `/api/asr`（`file`→转写 `{text}`、`rawText/audio`→协议提取 `{success,text,protocol}`），旧 `ai/asr` 删除、`VoiceInput` 改指 `/api/asr`；waves 对话实际调用方 `base/ai/chat/llmEngine.ts` fetch 改 `/api/waves/chat`；根侧 `ai/push-recommendations` 语义互补保持；`dimension3-ai.test.ts` 同步至新 `/api/asr`；验证：根/子 tsc 0 错 + 子单测 425/425 + 根 vitest 回落存量基线 11 failed（无新增） | #1 底座优先 / #2 接口保守 | API 面收敛：waves 对话与协议对话拆分为 `/api/waves/chat` 与 `/api/chat` 双端点（语义隔离）；语音链双实现 → 单一 `/api/asr`（接口契约合并，调用方同步）；路由 import 基线全部指向根 `@/base/*` 与根依赖，无跨包残留 |
| 2026-08-14 | `2ce4c48` | ammo 四表 → base 引擎接线批次：broadcastMatches/passesHardFilter/requiresVerification 增 rule 参数（DEFAULT_BROADCAST_RULE 等价原常量）；riskOf/extraLogin 增 params 参数（DEFAULT_ROAM_PARAMS 等价原阈值）；WaveFeed/useWaveStore/useRoamStore/RoamGuardPanel/AdminPanel 从 ammo 表取值注入；BiddingSandboxCard reserve 地板价；PublishSheet SOP 预填 + 定价建议 + 有效期档表驱动；ammo/sop 补「羽毛球约局」别名 | #3 先配表后写码 / #4 弹药可插拔 / #5 引信跟弹药走 | 写死权重/阈值/默认值收敛为 ammo 表驱动（参数注入式，base 保持零 import 可 node:test 直跑）；契约修订仅增补可选参数，未改既有语义 |
| 2026-08-14 | `941a54a` | **Phase 4 UI/Store/路由/PWA 深度融合**：58 组件 git mv 入根（29 → `src/components/waves/`；29 → `src/components/oto-ui/` 含 3d/admin/chat/destinations/profile 子域；`Badge.tsx` 改名 `OtoBadge.tsx` 防 Windows 大小写撞车并全量重映射引用）；7 store 提升 `src/store/` + 8 lib（mockData/mockResponders/dial/scan/qr 及测试）提升 `src/lib/`；子 5 屏 SPA 入 `src/app/(oto)/` 路由组（嵌套 layout 承载 ToastHost/PwaServiceWorker/Geist + `globals.css` 隔离为 `.oto-app` 作用域 + `--oto-*` 变量前缀，不覆盖根 shadcn 主题）；根 Header 增 OTO 入口；子手写 sw.js 协议（PRECACHE/SKIP_WAITING/push/notificationclick）并入根 `src/app/sw.ts` 并删除子 sw.js；public 资源归并（lounge.glb/models、manifest.json、oto-icon 改名防同名冲突）；8 处根业务 `React.ElementType` → `LucideIcon` 修复 R3F v9 JSX namespace 合并引发的 props never 污染。验证：根/子 tsc 0 错 + 子单测 425/425 + 根 vitest 存量 11 failed 不变 + 门禁 exit 0 | #1 底座优先 / #2 接口保守 | 组件/store/lib 目录平面结构提升为根侧收敛目录（waves/oto-ui 域划分 + 路由组隔离 + PWA 单实现归一），逻辑零改动仅物理落位 + 类型标注收敛；Badge→OtoBadge 为 Windows 大小写敏感性防撞改名 |
| 2026-08-15 | `afce408` | **Phase 5 测试合流与缺陷清零**：`(oto)/` 路由组 → `oto/` 真实目录（`(oto)/page.tsx`/`layout.tsx`/`globals.css` 三文件 rename；路由组不参与 URL 路径，`(oto)/page` 与根 `page.tsx` 同为 `/` 冲突导致 5 屏 SPA 构建期被根页面吞并、`/oto` 生产 500 —— 修复后 `/oto` 正常输出 5 屏 SPA HTML）；子 57 node:test 保持幂等不迁移（实测 vitest 收集 `node:test` 语法文件 57/57 "No test suite found"，D-05 裁决维持 node:test 通道）；根 `npm test` 组合为 `vitest run`（426）+ 子 `test:units`（425）一键 851 全绿；vitest.config.ts 补 `@/base` `@/ammo` `@/store` alias；11 个存量 vitest 失败清零（AI 组 4 测试入参对齐 `NegotiateInput`、SLA 组 mock `getServiceClient`+`getNextFundStatus`+`single` 链、Milestone 组 mock 链补 `select/single/maybeSingle`、confirm 实现补清 `auto_confirm_at`、credit-engine mock 补 `getNewbornProtectionFactor/getWeekendMultiplier`）；e2e 12 个 `e2e-*.mjs` 留存无损；`npm test` 851/851 + 根/子 tsc 0 错 + `next build` exit 0（121 静态页，serwist /sw.js 注入，`/oto` 生产可访问） | #1 底座优先 / #2 接口保守 | 路由从"(oto) 路由组语义路径"收敛为真实 URL 目录（消除与根 page 的 `/` 冲突，OTO 5 屏 SPA 恢复可达）；测试体系双通道（vitest + node:test）按 D-05 幂等并轨，`npm test` 单命令全量验证；存量测试契约（mock 链/入参/实现语义）对齐修复，无逻辑复杂度新增 |
| 2026-08-15 | `1b1eb64` | **Phase 6 终态归档**：16 脚本从 `oto-spatial-web/scripts/` git mv 至根 `scripts/`（12 e2e-mjs + verify-prod/restart-prod/export-sofa-glb/generate-vapid）；随后 `oto-spatial-web/` 整体废弃移除（68 tracked 文件 git rm）；`test:oto:units` node:test 425 清单从子 package.json 迁根 package.json 直跑；workspaces 收敛 `["mobile","packages/*"]`；tsconfig 删 `@oto/*` 别名与 exclude、vitest/eslint exclude 同步；`dev-all.mjs` 单进程化（:3000）；README/PROJECT_STATUS/MERGE_PROGRESS 单仓口径重写；终态回归 tsc 0 错 + `npm test` 851/851 + build exit 0 + 门禁 exit 0 | #1 底座优先 / #2 接口保守 | E2E 与运维脚本从子项目业务目录收敛为根 `scripts/`（单一工具链入口，BASE=:3000 语义不变）；子项目双应用部署形态收敛为单仓单体应用（workspaces 收窄、别名/exclude 清理、node:test 测试通道根内化），原 47 页 + 5 屏 + 105+ API 全部归位根应用 |
| 2026-08-15 | `a11d85e` | **底座反腐外科手术（D1~D3 治理）**：D1 拔除底座反向依赖——llmEngine/mockEngine 移除 useAppStore 运行时 import，新增 ChatEngineContext 契约（getChatMessages/isWorkerOnline），ChatPage 显式注入；D2 越级契约上收——WaveBundle 自 store/useWaveStore 提升至 src/types/wave-bundle.ts（新文件，transport/supabase 改引契约）；D3 业务词剥离至弹药层——sentinel 进家词表→ammo/risk-rule 引信 params.homeAccessKeywords（新增 homeAccessKeywordsFor）、broadcast DEFAULT_BROADCAST_RULE.requiresVerified 置空（ammo dispatch-rule 装填）、booking ICON_RULES→ammo/scene-template CATEGORY_ICON_RULES（iconFor/applyBooking/bookingWorkerOrder 增注入参数）；生产调用方 CapabilityPanel/WaveFeed/useWaveStore/useAppStore 全量改注入；测试适配弹药注入语义并补「未装填→通用兜底」断言；验证：tsc 0 错 + npm test 856/856 全绿 + 门禁 exit 0 | #3 单向依赖 / #4 引信跟弹药走 | 无文件 rename（WaveBundle 为新建 src/types 契约 + store 本地定义删除），契约修订类收敛；ChatEngine/iconFor/applyBooking/bookingWorkerOrder/requiresVerification/sentinelCheck 签名入参化 |
| 2026-08-15 | `87d28bf` | **核心设计模型契约固化（人类创始人注入）**：白皮书新增 §二（五态原子状态机 PUBLISHED-MATCHED-IN_SERVICE-INSPECTED-SETTLED + 跃迁矩阵 + 伴生事件 Sub-Events 插拔 + 与现有 wave 状态机过渡映射 + 三类引信矩阵 IMPACT/DELAY/PROXIMITY + 数字人格信用飞轮）；新建 src/types/ammo-schema.ts 与 src/types/fuze-policy.ts 纯类型契约（零业务依赖，tsc 0 错 / 856 全绿）；原白皮书章节顺延（三~六） | #2 接口保守 / #5 引信跟弹药走 / #6 信用跨弹药累积 | 契约新增（无 rename）；五态为目标模型，现有 wave 状态机按宪法 #2 渐进收敛映射，本次不改业务代码 |
| 2026-08-15 | `fad5a8a` | **28 模块主蓝图固化（人类创始人注入）**：白皮书 §三 2.4 升级六层防御圈 x 核心模块职责矩阵（标准模块编号 L1-M1～L6-M4 全项目永久统一）+ 26 行代码落位与成熟度对照表（🟢13/🟡12/⚪️1，L4-M3 待建设）；宪法元宪法声明增补模块编号标准；口径核查：清单净 26 模块与标题 28 差 2 席，编号体系完整成立，差量标注待人类裁决 | #1 六圈定位声明（模块编号即定位锚点） | 纯文档批次（无 rename 无代码变更），856 基线不动，门禁 exit 0 |
| 2026-08-15 | `cab6f33` | **三阶段推进路线图固化（人类创始人注入）**：白皮书新增 §四（Phase 1 MVP 验证期 0-1 housekeeping.ammo.ts 碰炸引信 / Phase 2 体系成熟期 1-10 meetup 延期 + companion 近炸 / Phase 3 规模壁垒期 10-100 弱网容灾熔断存证），每阶段绑定模块编号 + 缺口衔接 + 现状标注；顺带修复小节编号一致性（§三 2.x→3.x、落差审计 3.x→5.x） | #1 六圈定位（模块编号即定位锚点） | 纯文档批次（无 rename 无代码变更），856 基线不动，门禁 exit 0 |
| 2026-08-15 | `W1W2W6` | **W1~W6 生产接线总装（第一批次）**：Wave 契约增补 `ammoId` 可选字段（`base/order/wave.ts` Wave/CreateWaveInput/createWave 三处透传，零破坏）；`PublishSheet` 接入 `DynamicDraftCard` 弹药草稿预览 + `resolveAmmoIdForPublish` 中文品类归一化直挂官方弹药（家政保洁→housekeeping-v1 / 羽毛球约局→meetup-social-v1）；`oto/page.tsx` HomePage 顶部挂 `StatusCapsule` + `toAtomicFiveState` 实时投影；`layout.tsx` 挂 `OnlineStatusBridge`（navigator.onLine 桥）；`ProfilePage` 安全中心新增「👵长辈模式」+「🛡️应急伪装」入口与覆盖层（静默报警直连 raiseCrisis）；暴露并修复 3 处 base 层预存 tsc bug（runner.ts ISubEventResult 未 import + HookOutcome.fallbackUsed 缺 BLOCK——因 tsconfig exclude src/base 长期未被类型检查，接线后纳入编译图）；修复 OfflineQueueIndicator 快速切换时恢复 Toast 覆盖离线条缺陷；新增连线测试 wiring.test（6）+ OnlineStatusBridge.test（4），960/960 全绿 + tsc 0 + 门禁 exit 0 | #3 单向依赖（契约增补 ammoId 可选字段）/ #4 引信跟弹药走（ammoId 落库供插槽装载） | 契约修订（Wave 增 ammoId 可选字段，向后兼容）+ base/ammo/runner 类型修复（类型对齐，运行时零变化） |
| 2026-08-15 | `W3W4W5W7` | **W3~W7 全链路接线通电总装（第二批次 · 终极闭环）**：W7 新建 `ArbitrationSheet`（半屏上滑争议抽屉：物证比对链[投诉诉求/完工照片+AI 视觉标注/聊天记录] + AI 小法官建议卡[退款/补偿券/责任/信用/理由链，仅 Advisory] + 隔离墙双出口[接受调解→BREACH_SETTLED / 申请人工→资金冻结]）；W3 新建 `FulfillmentCenter` 并挂载 Trip 屏（进行中 Wave 投影三态 MATCHED/IN_SERVICE/INSPECTED → 顶部主视口优先渲染 FulfillmentCockpit，ammoId→scenario 毫秒自适应插槽）；W4 插槽事件接线（保洁增项改价确认→订单总额动态更新 / 组局 500m 围栏扫码→定金解冻 / 陪玩伪装假电话→全屏来电遮罩接听挂断脱身）；W5 核销 CTA 生产首次调用 `advanceLifecycle` 真实引擎（MATCHED→IN_SERVICE→INSPECTED→SETTLED 全链），流转结果 `setFulfilment` 回写 store → toAtomicFiveState 投影 → 顶栏 StatusCapsule 实时流转 🟢；registry 新增 `getAmmoById`（ammoId 反查整弹）；store 新增 `fulfilment` 回写位（只增不改）；新增 E2E 装配回归 18 例（ArbitrationSheet 6 + 纯函数矩阵 4 + 端到端流转 4 + 插槽接线 4），**978/978 全绿**（vitest 488 + node:test 490）+ tsc 0 + 门禁 exit 0 | #3 单向依赖（store 增 fulfilment 回写位、仅投影消费）/ #2 接口契约（advanceLifecycle 生产首次调用，引擎接通） | 契约修订：store 增 fulfilment/setFulfilment（向后兼容零触碰）；base/ammo/runner 生产首次被 UI 层真实调用（类型已修，无运行时变化） |
| 2026-08-15 | `companion-v1` | **第三枚官方标杆弹药 companion-v1（纯 📡 近炸引信）大满贯**：新建 `src/ammo/companion.ammo.ts`（同城陪玩/交友 · 高人身风险：PROXIMITY_FUZE_TEMPLATE 直挂 + HOURLY ¥100/h 起步 1h 超时 ×1.2 + PrivacyShieldHook[IN_SERVICE 前置 BLOCK：虚拟号绑定/行程守护/敏感词监听三闸] + DepartureFinishHook[SETTLED 后置 SKIP：300m 离开自动结账停表 + 信用分奖励载荷]）；注册表挂载 companion/dating/escort 三键 + 中文品类陪玩/交友/约会归一化；**契约修订：dating 类目自 meetup 迁移至 companion**（同人风险归近炸引信，meetup.ammo.test.ts 两处断言同步修正）；新增 18 例 node:test 白名单注册，**996/996 全绿**（vitest 488 + node:test 508）+ tsc 0 + 门禁 exit 0 | #2 接口契约（dating 键迁移：行为契约修订）/ #4 弹药归一化（中文类目新增三键，getAmmoDefinition 语义不变，未配置类目仍保底） | 契约修订：OFFICIAL_AMMO.dating 由 meetupAmmo → companionAmmo；CATEGORY_TO_OFFICIAL 新增 陪玩/交友/约会；package.json test:oto:units 白名单追加 companion.ammo.test.ts（仅追加，无删除） |
| 2026-08-15 | `escrow.ts` 资金引擎收敛 | **L2-M4 账户清结算 100% 闭环（阶段二资金引擎深水区大收敛）**：① 新建 `src/base/money/escrow.ts`——统一资金托管纯函数引擎（红线 1 零 LLM：六模式托管矩阵 calculateEscrowHold / 三阶段阶梯退款+违约 20% 罚金 calculateTieredRefund / AA 多方分账 calculateMultiPartySplit / 资金安全底线 verifyFundSafetyGuard / release 收敛 calculateProviderSettlement），提炼 m13-payment 六模式+退款语义与 api/payment/release 内联 10% 抽成；② `src/base/ammo/runner.ts` 新增资金挂接：MATCHED 托管校验（余额安全底线 BLOCK）与 SETTLED 清结算对账清单装配（buildSettlementLedger，四大弹药统一产出；零 escrowPayload 完全透传，既有跃迁行为不变）；③ `app/api/payment/release/route.ts` 收敛调统一引擎（响应契约不变）；④ 新增 28 例 node:test 白名单注册，**1024/1024 全绿**（vitest 488 + node:test 536）+ tsc 0 + 门禁 exit 0 | #10 降级/失败是设计（资金校验 BLOCK 准入）+ 红线 1 隔离墙（资金计算纯函数化） | 契约新增：runner AdvanceInput.payload.escrowPayload（可选，缺省零影响）；api/payment/release 响应结构不变；既有测试零改动 |
| 2026-08-15 | `geofence-watcher/nfc-adapter/rpc-client` L4-M3 唤醒 | **L4-M3 物理履约闭环 + 全仓 28 模块 0 空白项（阶段三：唤醒沉睡战力）**：① 新建 `src/base/geo/geofence-watcher.ts`——50m 高精 GPS 围栏纯函数引擎（Haversine 复用 base/geo + 精度漂移过滤 accuracyWarning + 停留时长防刷 + 陪玩 300m 安全距离脱离，红线 3 零 UI 依赖）；② 新建 `src/base/platform/nfc-adapter.ts`——Web NFC 碰碰无头适配器（HMAC 防重放载荷 + 弱校验/强验签双通道 + 动态码扫码降级，红线 5 不抛未捕获异常）；③ 新建 `src/base/platform/rpc-client.ts`——6 大已落库 Supabase 原子 RPC 通道（grab_demand/release_checkpoint_rpc/sla_auto_release_rpc/match_demands_hybrid/init_provider_wallet/submit_withdrawal_request + p_ 前缀 camel 映射 + 确定性 Mock 降级）；④ `ArbitrationSheet.tsx` 接入 export-judicial-package 司法存证包导出（SHA-256 审计证书卡，加载/错误/成功三态）；⑤ 白皮书 L4-M3 ⚪️→🟢、L1-M2 围栏 🟡→🟢、L1-M3 适老 UI 🟡→🟢，**状态列 0 个 ⚪️**；⑥ 新增 37 例 node:test 白名单注册，**1061/1061 全绿**（vitest 488 + node:test 573）+ tsc 0 + 门禁 exit 0 | #4 弹药归一化（引信/围栏参数随弹药声明）；红线 5 降级（NFC 不可用 → 动态码） | 契约新增：base 层三个新模块（无既有引用方改动）；ArbitrationSheet 纯新增 UI 区块；既有测试零改动 |
| 2026-08-16 | `1ec5018` | **路由置换与 OTO 主力产品全面转正**：`src/app/oto/` → `src/app/(oto)/` 路由组（3 文件 git mv，路由组不占 URL 段，OTO 5 屏直接呈现于根 `/`）；`src/app/page.tsx` → `src/app/dp/page.tsx`（老协议门户归位 /dp）；`next.config.ts` redirects：`/oto` → `/` 307；`Header.tsx` 导航双端适配（OTO空间→/、协议后台→/dp）；验证：tsc 0 + 1180/1180 全绿 + 全新 build exit 0 + dev 冷启动 CDP 实测（/ 200 OTO 渲染、/dp 200 老门户、/oto 307→/） | #1 职责单一（根路径=OTO 主站、/dp=协议门户，路径职责分离）/ #2 接口契约（/oto 307 兼容旧链，契约不变） | 路由契约修订：路径归属调整（内容零改动，git mv 100% 识别）；头导航仅入口文案/指向调整，无逻辑改动 |
| 2026-08-16 | `d4c7b23` | **8 维全息弹药契约与 AmmoFactory 工业级装配流水线**：契约修订（无 rename，仅增补可选字段与新增类型）——`src/types/ammo-schema.ts` 增 `IHolographicAmmoConfig`（8 维：D1 供给准入 / D2 计价护栏 / D3 引信 / D4 传感降级 / D5 正向钩子 / D6 逆向违约阶梯 / D7 清算仲裁 / D8 视界表单）+ `ICancellationTier` / `ISplitRules` / `SensorKind` / `SensorFallbackLadder` / `PricingParams` + `IWorkerRequirement.isPoliceVerified` + `ISubEventContext.ammoSnapshot` + `IAmmoDefinition.holographic` 镜像（全部可选，向后兼容零破坏）；新建 `src/ammo/factory.ts`（4 级工序：validateAmmoConfig 静态语义审查[资金守恒 =1.0 容差 1e-9 / C2_IN_HOME 一票否决 / 加价熔断 ≤0.5 / 计价边界 / 阶梯边界 / 钩子白名单] + HOOK_OPERATOR_REGISTRY 6 算子静态白名单 + assembleAmmo deepFreeze 全图冻结 + registerDynamicAmmo 热注册）；`src/ammo/registry.ts` 增 DYNAMIC_AMMO_POOL（检索链动态池优先，官方/四表/保底回落语义不变）；`src/base/ammo/runner.ts` AdvanceInput 增 ammoSnapshot 可选（快照优先调度，缺省回落 input.ammo 零回归）；`package.json` test:oto:units 白名单追加 factory.test.ts（12 例）；验证：tsc 0 + 1210/1210 全绿（vitest 537 + node:test 673）+ build exit 0 + 门禁 exit 0 | #2 接口保守（只增补不删改既有语义）/ #4 弹药可插拔（8 维声明式配置 + 动态热注册）/ #10 降级设计（传感降级阶梯 + 快照冻结热更新免疫） | 契约修订类收敛：IAmmoDefinition/ISubEventContext/AdvanceInput 仅增可选字段、检索链仅增动态池优先位；既有断言与调用零改动（既有语义经官方/四表/保底回落路径原样保留） |
| 2026-08-16 | db13baf | **三大标杆弹药 8D 全息化出厂 + ESM 循环依赖治理（方案 A 收官）**：① `housekeeping.ammo.ts` / `meetup.ammo.ts` / `companion.ammo.ts` 三枚官方弹药重构为 8 维全息配置（D1~D8：supplyCluster/workerRequirement · pricingModel/pricingParams/minFloorPrice/maxCeilingPrice/maxSurchargeRatio/creditWaiverRule · fuzePolicy · requiredSensors/sensorFallbackLadder · forwardHooks · cancellationTiers · autoAcceptanceTimeoutHours/splitRules · theme/cockpitSlot）经 `assembleAmmo` 流水线静态审查出厂（模块加载期强制门禁，任一不合格抛错拒出厂），dispatchRule/sop 存量字段显式 spread 保留（入户实名关键词硬门槛与发布页 SOP 预填语义零丢失），deepFreeze 全图冻结发布；② 契约修订——`ammo-schema.ts` `SensorKind` 增补 5 个降级成员（CELL_TOWER_COARSE_GEO / MANUAL_BASE_PHOTO_AUDIT / HTML5_NATIVE_FALLBACK / QR_SCAN_VERIFICATION / PROXIMITY_DEPARTURE_MANUAL_CHECK，人类创始人裁决，既有 4 主传感器成员不变）；③ 依赖收敛（ESM 循环依赖 TDZ 根治）——`factory.ts` 本地定义并导出 `DYNAMIC_AMMO_POOL`（移除对 registry.ts 反向 import），`registry.ts` 改为 import + re-export，依赖图收敛为无环 DAG（factory→types；ammo→factory；registry→factory+ammo）；④ 钩子契约切换——五态引擎 fiveStateHooks 由工厂算子白名单解析（operator.onsite-quote/cleaning-check/arrival-check/aa-split-settle/privacy-shield/departure-finish），存量富钩子保留导出供直测（AA 多退少补/违约金赔付/300m 停表计费语义零丢失）；⑤ minSafetyScore 双量表裁决——恪守 base 0-100 量表（tri-credit ESF/PQS 门槛），housekeeping 60 + isPoliceVerified:true（factory C2 policeOk 通道）、companion 65；⑥ 三测试文件同步 8D 契约断言（装备完整性/全息字段/算子载荷）；验证：tsc 0 + 1217/1217 全绿（vitest 537 + node:test 680）+ build exit 0 + 门禁 exit 0 | #2 接口保守（SensorKind 只增补不删改）/ #3 单向依赖（DYNAMIC_AMMO_POOL 归位装配层，打断循环）/ #4 弹药可插拔（三大标杆弹药 100% 流水线归一）/ #10 降级设计（传感降级阶梯 + 违约阶梯 + 加价熔断全部静态出厂） | 契约修订 + 依赖收敛类收敛：传感器联合类型仅增 5 降级位、钩子白名单接管引擎级流转（富钩子直测保留）、工厂/注册表依赖方向翻转（消费方导入面经 re-export 零改动）；base 层（tri-credit/escrow/runner）零改动 |
| 2026-08-17 | `4a5b7de` | **发布端动态长尾弹药直拨（P0 缺口闭环，无 rename）**：`src/ammo/registry.ts` 新增 `resolveDynamicAmmoByInput` 纯函数（精确 class key → D8 中文类目别名 `aliases` 遍历只读匹配，Map.get / includes 零写入）并接入 `resolveAmmoIdForPublish` 检索链头部（动态池优先 → 官方中文映射 → 聚合/保底），PublishSheet 以口语化中文发单即可直达动态弹药 Wave.ammoId；契约增补——`src/types/ammo-schema.ts` `IHolographicAmmoConfig` 增可选字段 `aliases?: string[]`（D8 声明式检索别名），`src/ammo/factory.ts` `validateAmmoConfig` 增非法别名条目拒出厂（INVALID_AMMO_ALIAS）；大考考卷 `dynamic-production-exam.test.ts` 增发布端接线环节（未注册中文回落 default-ammo / 注册后「农田无人机植保」「无人机打药」双别名直拨 drone-crop-spray-v1 / 家政/组局/陪玩官方映射回归护栏 / 池删除后别名随删失效）；验证：tsc 0 错 + 1225→**1226/1226 全绿**（node:test 688→689）+ build exit 0 + 门禁 exit 0 | #2 接口保守（只增补可选字段不删改既有语义）/ #4 弹药可插拔（动态弹药直达发布端，检索位 100% 运行时） | 契约修订类收敛：IHolographicAmmoConfig 仅增可选 aliases 位、检索链仅增动态池优先位；既有断言与调用零改动（官方中文映射与聚合/保底回落路径原样保留） |
| 2026-08-20 | `36894f9` | **全站 UI/UX 信息架构终极重组（无 rename，结构性页面层级收敛）**：① 首页灭双头怪——ChatPage 新增 `slim` 渲染态（隐藏重复「AI 撮合助手」标题头/意图气泡/greeting 重播卡，仅过滤 greeting 单条，消息流与引擎零改动 → 多轮澄清/订单卡转化能力 100% 保留）；② Trip 屏层级收敛——物理删除旅游假数据面（`otoActivities`/目的地假数据/3D 路线图/AR 指南/地图视图/分享行程 4 选项卡/`ActivityRow`，-190 行 + import 摘除 `OtoBadge`/`MapPin`/`formatActivityTime`/`OTOActivity`），收敛为「FulfillmentCenter 履约座舱（唯一核心，自隐藏保留）+ 无进行中订单极简雷达空态卡 + 我的需求 + 真实预订列表（待出行/完成/取消状态徽标）+ 空预订引导」；③ Profile 主页平铺层级收敛——新建 `ProfileDrawer` 通用毛玻璃半屏抽屉（AuthSheet 同源质感 + 边缘滑动手势锁定 + 内部滚动），18 层大平铺收纳为「登录行 + 身份卡 + 资产钱包卡（统计 + 虚拟钱包/信用/额度）+ 服务者工作台入口 + 3 抽屉入口 + 我的订单/我的接单/好友内容区」，设置类控件全收抽屉（安全=SafetyKit+SOS EPA+长辈+伪装+紧急联系人 / 隐私=未保分级+脱敏+遗忘权 / 系统=撮合偏好+PWA 真推+免打扰+演示座舱+本地备份+能力声明），**主屏 375×812 零滚动**；④ 门禁：ChatPage.test +2 slim 用例（slim 收敛渲染断言 + 弹药草稿回钩保留断言）→ **1544/1544 全绿**（vitest 654 + node:test 890）+ tsc 0 + build exit 0 + 收敛门禁 exit 0 + Chrome CDP 四屏真机回归（首页无重复问候/行程零假数据/Profile 三抽屉逐一滑出/AR 场景探索）+ 控制台 0 应用错误 | #1 集权效速（页面信息架构统一收权：主屏只留内容流，设置类控件全部收纳进三级抽屉）/ #2 直营正牌（首页「AI 撮合助手」身份让位给单行 AI 问候发单条，重复标题/气泡/重播问候全收敛）；结构收敛：页面层级由 18 层平铺收敛为「4 主卡 + 3 抽屉」两级模态 | 契约修订类收敛：ChatPageProps 增可选 `slim`（渲染态收敛，默认 false 原行为零改动）；无文件 rename（git 门禁零检测项） |
| 2026-08-17 | `e5e44d3` | **D8 视界与动态前端插槽深度装配（无 rename）**：新建 `src/components/waves/slots/DynamicAmmoSlot.tsx`（长尾动态弹药通用履约插槽：bizParams 参数快照 / WATERMARK_CAMERA Before·After 打卡区 / fuzePolicy 引信徽标 / 申请调解申诉，自包含 CSS + 弹药 theme 微氛围，零品类硬编码）；前端键契约修订——`CockpitScenario` 联合类型增第四成员 `dynamic`（+`SCENARIO_THEME_META.dynamic` + `describeCompletionCta` dynamic 分支 + 新纯函数 `describeFormSchemaFields`/`resolveDraftThemeClass`/`describeBizParamRows`/`describeFuzeBadges`/`requiresWatermarkCamera`/`resolveSlotThemeClass`）、`FulfillmentCockpitProps` 增可选 `dynamic?: DynamicAmmoSlotProps`（未提供时优雅降级）、`resolveCockpitScenario` 兜底语义由 meetup 改为 dynamic 且新增弹药声明 `cockpitSlot` 优先分发（COCKPIT_SLOT_SCENARIO 映射）；验证：tsc 0 错 + 1226→**1235/1235 全绿**（vitest 537→546，本次 +9）+ build exit 0 + 门禁 exit 0 | #2 接口保守（联合类型/插槽 props 只增不改既有三制式语义与断言）/ #3 单向依赖（插槽纯 UI 视口层，零 base 反向注入）/ #4 弹药可插拔（cockpitSlot 声明优先分发 + formSchema 声明式渲染） | 契约修订类收敛：CockpitScenario 增 dynamic 成员、Cockpit props 增可选 dynamic 透传位、resolveCockpitScenario 兜底语义归一（原 meetup 兜底仅作用于未命中任何制式/类目的长尾弹药，官方三制式与中文类目正则命中路径原样保留）；既有断言与调用零改动（FulfillmentE2EIntegration 23/23 回归绿） |
| 2026-08-17 | `5cc281d` | **阶段1 拟人大考 4 缺陷定向修复（无 rename）**：① `voiceIntent.ts` publishHit 发布关键词表补齐口语同义词（打扫/做卫生/扫地/擦玻璃/做保洁前缀已有）+ matchTime 口语时间精确时分（10点→10:00、10点半→10:30、X点Y分→HH:MM）；② `runner.ts` buildSettlementLedger 缺省分账口径对齐弹药 D7 splitRules（家政 85/10/5 等，保险计提入账对账单 insuranceFee 位，90/10 裂隙 ¥8.50 消除，与收付通指令分角一致）；显式 platformRate 仍尊重显式值（既有调用零变化）；③ `DynamicDraftCard.tsx` 新增 resolveDraftPricing 全息优先定价解析（holographic.pricingModel 装配出厂权威源，缺省回落顶层，起步时长脱节兜底）；④ 断言同步（人类裁决）：real-user-sim-basic A3 翻转为修复实证 + D1/D3② 三比守恒与指令一致性 + voiceIntent.test 回归 +1 + escrow.test esc-4/esc-5 期望对齐弹药契约（90/10 旧缺省 270/216 废弃）；验证：tsc 0 错 + 1414→**1415/1415 全绿**（node:test 831→832 本次 +1，vitest 583 不动）+ build exit 0 + 收敛门禁 exit 0 | #2 接口保守（SettlementLedger.split 只增可选 insuranceFee 位）/ #4 弹药可插拔（缺省分账口径跟弹药声明走） | 契约修订类收敛：SettlementLedger.split 仅增可选字段、缺省分账语义由 90/10 兜底收敛为弹药 D7 三比（显式传参语义原样保留）；voiceIntent 行为修复（口语词表/时间格式）；esc-4/esc-5 断言锁旧缺陷行为，经人类裁决后对齐弹药契约 |
| 2026-08-18 | `c2afad6` | **阶段4 方案 A：Wave 实体强类型契约扩列与履约座舱 UI 全量闭环（无 rename）**：① `src/base/order/wave.ts` 契约修订（宪法条文 #2 增补不改义）——`Wave` 与 `CreateWaveInput` 增可选字段 `customRequirements?: INormalizedCustomIntent`（inline import 类型，红线 3 仅类型导入零 React/UI 依赖），`createWave` 透传（缺省 undefined 向后兼容，既有断言零改动）；② 发单端全量贯通——`ChatPage.tsx` 语音意图 `lastCustomRequirementsRef` 缓存 + `createPendingWave` 注入（普通文本发单缺省 undefined 零影响）；`PublishSheet.tsx` 需求备注 note 经 `normalizeCustomIntent` 语义驯化后注单（违禁命中走既有治理闸门 2 词表扫描，发单行为不变）；③ 履约座舱与插槽 UI 全量消费——`FulfillmentCenter.tsx` 透传 `activeWave.customRequirements` + `evaluateRuntimeSafety`（家政入户基础 20 + 敏感定制 30 + 夜间 20，真实本地时刻）→ `PROXIMITY_ENHANCED` 时注入 `forceArmed`/`safetyBadge`；`FulfillmentCockpit.tsx` 新增 `customRequirements?`/`forceArmed?`/`safetyBadge?` 可选 props + 强化安全守护条（琥珀/翡翠微光卡「🛡️ 强化安全守护中（虚拟号通话+全程行程守护+敏感词实时监听）」+ 引擎文案优先）+ 中性化定制标签栏（`describeCustomRequirementTags` 纯函数：`[工作着装: 女仆主题]`/`[期望年龄: 20-30岁]`/`[性别偏好: 女性]`，无定制零渲染）；`HousekeepingSlot`/`DynamicAmmoSlot` 参数快照区结构化标签卡（DRESS 映射中性投影，杜绝原始粗糙词直显）；④ 测试矩阵——`FulfillmentCockpit.test.tsx` 新增 7 例（forceArmed 渲染/引擎文案优先/标准回退/家政与动态插槽透传/describeCustomRequirementTags 纯函数）；验证：tsc 0 错 + 1450→**1458/1458 全绿**（vitest 583→591 本次 +8）+ build exit 0 + 收敛门禁 exit 0 | #2 接口保守（Wave/CreateWaveInput/Cockpit 仅增可选字段与可选 props，不删改既有语义与断言）/ #3 单向依赖（wave.ts 仅类型导入 ammo-schema；插槽纯 UI 视口层零 base 反向注入） | 契约修订类收敛：Wave 实体与 CreateWaveInput 仅增可选 customRequirements 位（缺省 undefined 100% 向后兼容，既有 wave 构建/断言零改动）；Cockpit/插槽 props 纯增可选位（无定制订单布局与文案零变化）；发单端仅新增注入路径（既有发单分支原样保留） |
| 2026-08-20 | `caddf34` | **OTO 商业全景整备五战场（无 rename，契约仅增补）**：① 导航 IA 对齐——`DockPage` 联合类型增第四成员 `im`（+`DockId` 导出），FloatingDock 标准化为 4 核心键 首页/消息/行程/我的（AR 从 Dock 移出为首页上下文悬浮按钮），Dock「消息」键投影 IM 未读角标；② 首页上下文悬浮按钮——HomePage 挂「AR 扫描」（保住 e2e-app「AR 扫描」getByRole 锚点全绿）+ TripPage 挂「拍照存证」（ProofCamera 存证相机模态，照片直通履约争议物证链），`TripPageProps` 增可选回调位；③ 首页冷启动商业活化——`inspirationSetFor` 纯函数（五档时段灵感）+ WaveFeed 信任胶囊/MatchTicker 弹幕/供给雷达空态光斑；④ 草稿卡触觉化——DynamicDraftCard 增纯函数 `describeSopAdjusters`/`describeLiveEstimate`/`clampAdj` + 内联调节器（SOP 行/data-param 展开 + formSchema 字段 number/enum/string 微调）+ CTA 深压涟漪；⑤ 排版系统——全局字体栈/六步字阶/六级对比度/等宽数字 `.font-tabular`/price-roll 等 keyframes；⑥ 数据出清——mockData 删 `otoActivities` 全数组 + 巴厘岛/马尔代夫双条目（`OTOActivity`/`formatActivityTime` 类型与函数随删，全仓 0 命中），`oto-kyoto-ryokan` 保京都 3200km 断言、圣托里尼 ¥1,990→¥1,290 承接 lt1500 档断言（destFilter.test.ts 零改动）；验证：tsc 0 错 + 1544→**1547/1547 全绿**（vitest 654→657 本次 +3）+ build exit 0 + Chrome CDP 五屏真机回归（灵感 chips→草稿卡→内联调节器→联动估算 ¥400 实证 / 消息屏空态 / 行程拍照存证模态 / AR 悬浮钮通路 / Dock 四键）/ 控制台仅环境噪音 | #2 接口保守（DockPage 只增 im 成员、TripPage/DynamicDraftCard props 只增可选位与纯函数导出，不删改既有成员与断言）/ #4 弹药可插拔（SOP 调节器由弹药表驱动，零品类硬编码）| 契约修订类收敛：DockPage 联合类型仅增 im 成员（四键渲染路径新增，既有 home/trip/profile 语义零改动）；Dock「AR 扫描」键移除属 UI 层级变更非类型契约删改（AR 能力经悬浮按钮保留，e2e-app/e2e-offline 锚点全绿）；TripPage/DynamicDraftCard 纯增可选 props 与导出纯函数（既有调用与断言零改动） |
| 2026-08-21 | `9716b4d` | **根布局 Header 污染根除与 /dp 专区隔离（无 rename）**：`src/app/layout.tsx` 移除 `Header` 挂载 + 新建 `src/app/dp/layout.tsx` 专属挂载 `Header`，根 `/` 纯净 OTO 视口，`/dp` 协议专区保留后台导航；验证 tsc 0 + build exit 0 + 门禁 exit 0 | #1 职责单一（根路径=OTO 主站、/dp=协议门户） | 布局隔离类收敛：`Header` 从全局根布局收敛为 `/dp` 协议专区专属布局，根路径职责分离，`Header` 隐藏判定保留 |

| 2026-08-23 | UNCOMMITTED | **Step 0 ����ɨ��ս�ۣ��� rename��**����mockData ������ϣ�types ����Լ + ammo ���ݱ�˫��ƽ�ƣ�3 import ��д���߼��Ķ���+ DestinationHub/Card ����� git rm��������ʵ֤��+ no-console error ������scripts/e2e/test �Ϸ������ + src/lib/modules/SplitDemandView ���������ڻ��⣬Step 1 ɾ override��+ dormant/mobile-v1 tag ���᣻�Ž� tsc 0 + lint 0/0 + 1623/1623 + build 0 + verify-prod 12/12 + ���� exit 0 | #1 �������ȣ�types ��Լ��/ammo ���ݱ���ְ���λ������ 3 �ֲ��غ㣩/ #8 ��˽ѪҺ��sms ������־����� | ����Լ�޶�����ɾ���� import ��д��Wave/Ammo ��Լ�㴥���������Ž� exit 0 |
| 2026-08-23 | UNCOMMITTED | **Step 1 �����������峡��׼���³����� rename��**������·�� ��3 + modules ��4 + ���� agent-gateway + mockData ���壨destFilter �ȸĵ� types/ammo �غ��� 3�����½� base/risk/admission.ts ��բͳһ��������homeAccessKeywords ���÷�ע�룩��useWaveStore ˫ action ���ߡ�publishWave ©բ���룻�Ž� tsc 0 + lint 0/0 + 1589/1589 + build 0 + verify-prod 12/12 + four-ammos 5/5 + ���� exit 0 | #1 �������ȣ�׼�����Ȧ���� base/risk ��һ���ڣ�UI ������ƴװ��/ #4 ��ҩ�ɱ�Σ����Ҵʱ��ɵ�ҩ��ע�������ҵ��ʣ�/ #8 ѪҺ����δ���� minor բȫ��·��Ч�� | ����Լ�޶���store ����������ѡ minorBlocked ����� sentinelScore ֻ���ֶΣ���� null ��Լ���֣������Ž� exit 0 |
| 2026-08-23 | UNCOMMITTED | **Step 2 ���Ľӵ磺�������̬Ȩ�������� rename��**��������Ǩ�ƣ�ö�ٶ��� AtomicFiveState/kind/ammo_id/order_seats/�ı� RLS/logs �ݵȼ���+ publish/transition ˫Ȩ�� API���ݵ�բ��/CAS ˫����/���ն��ᴫ��/SETTLED ledger �Զ���⣩+ store write-behind �ӵ� + FulfillmentCenter ��ԾǨ����ߣ��Ž� tsc 0 + lint 0/0 + 1598/1598 + build 0 + verify-prod 12/12 + four-ammos 5/5 + ���� exit 0 | #1 ����ǽ��״̬���ʽ���� 100% runner ��������·������д���ˣ�/ #2 �ӿڱ��أ�QueueOp ֻ������Ա��orders ����ȫȱʡ���Wave ʵ���㴥����/ #4 ��������SETTLED �����ɵ�ҩ D7 splitRules �� buildSettlementLedger �غ�װ�䣩/ #10 ����������write-behind ʧ���� offlineQueue �ݵ��طţ� | ���ƻ�����Լ�޶���orders ���о��� DEFAULT ��������У�store ������ Wave �����ֶ��㴥���������Ž� exit 0 |