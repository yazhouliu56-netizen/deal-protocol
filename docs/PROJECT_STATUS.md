# 项目状态档案 — deal-protocol（单一真相源）

> 管辖范围：`deal-protocol/` 单体仓库（含原 `oto-spatial-web/` 子项目，ADR-0018 Phase 6 已并入废弃）。父项目 v3.0.0-PROD 基线见 `PROJECT_STATE.md`。
> **维护规则**（见 AGENTS.md）：凡代码改动影响功能/验证基线/阶段 → 同步更新本文件对应表格，并刷新底部 `LAST_SYNC`。
> **前身**：`NEXT_STEPS.md` 于 2026-08-07 并入本文件后废弃。
> **文档索引**：全仓资产与技术债唯一事实源 → [`docs/DEBT_CLEANUP_REGISTER.md`](DEBT_CLEANUP_REGISTER.md)（99 路由 / 126 组件 / 14 模块 / 级联台账，2026-08-15 建档基线）。

---

## 一、基线 / 技术栈

| 项 | 值 |
|---|---|
| 定位 | 空间化本地线下面基服务 PWA（AI 撮合对话 + 六维评分 + 双视角闭环 + AR + 全息玻璃 UI） |
| 栈 | Next.js 16.2.12 (App Router) · React 19.2.4 · TS strict · Tailwind v4 · Three 0.185 · R3F/drei · Zustand 5 · Framer Motion · Supabase |
| 活跃周期 | 2026-08-03 初始化（独立于父项目历史） |
| 主 LLM | Zhipu GLM-4.7-Flash（zhipu→gemini→mock 三层降级链） |
| LLM Gateway | ✅ ADR-0005：provider 表单一来源（gemini/zhipu/qwen/groq/openrouter 五行，补 key 即扩容）+ per-provider 配额（独立串行/间隔/429 冷却/健康分沉底）+ 按任务路由（chat→Gemini 首选；voice-intent/cluster/decompose/diagnose→智谱 JSON 稳定首选）+ /api/gateway 统一入口 + 五路由全部薄层化（含 cluster/decompose/diagnose 手写链收敛） |
| 架构 | 5 屏(home/ai/ar/trip/profile) · 11 API(chat/cluster/decompose/diagnose/gateway/judge/asr/tts/voice-intent/push×2) · 核心状态机 `useWaveStore.ts`(36KB) · waves 组件 29 个 |

## 二、阶段定义

- **M1-M4**（oto-ai-platform-design）：需求→撮合→订单→AR 架构设计
- **P0-P5**（pwa-grand-refactor）：工程化/3D 栈/设计系统/地图/Supabase/PWA 深化
- **P1-P7**：waves 撮合经济闭环（广播/磋商/鸽子险/评价/治理/信任）
- **P8**：商业化（账号漫游 + PWA 真通知 + 公开竞价学 Airtasker 佣金）

## 三、达成状态

| 期 | 状态 | 附证 |
|----|------|------|
| **元宪法确立（RPG 火箭筒微内核模型）** | ✅ `docs/ARCHITECTURE_TAXONOMY.md` 白皮书落盘（四层架构 + 6 大工程红线固化 + 全仓资产归属映射：base 11 域/ammo 6 表/火控雷达/六圈投影 + 落差审计 D1-D8/P0-P3 缺口）+ `DESIGN_CONSTITUTION.md` 增元宪法声明节与修订记录（宪法 10 条文 = 元宪法工程投影） | 2026-08-15 |
| M1-M4 架构 | ✅ 超额完成（已演进为 waves 经济） | oto-ai-platform-design.md |
| 设计宪法定稿 | ✅ `docs/DESIGN_CONSTITUTION.md`（10 条文 + §3 冲突上报用户拍板 + §4 ADR 模板两栏）+ 根/子 AGENTS.md 挂指针 + `docs/adr/TEMPLATE.md` + ADR-0006/0007 标注派生 | 2026-08-13 |
| ADR-0007 v2 契约全兑现 | ✅ C1 OrderCore（`base/order/orderCore.ts` 投影桥，22 调用方零改动）+ ammo 补 sop.ts + C4 hardGates 对齐 + e2e「填表即新弹药」（遛狗遛弯四表读全/引擎消费/零 base 修改）+ mobile 归属登记 → 单测 314 全绿、tsc/lint 0 错、build/冒烟通过 | 2026-08-13 |
| ADR-0008 智能争议小法官 | ✅ 第三圈①判决能：`base/ai/judge.ts` 规则引擎（硬伤升档/反驳降档/承诺落空升档 + 全责 100%/demander 0% 钳制）+9 单测 → `/api/judge` LLM 语义比对（judge task 入 Gateway 三级降级，毒丸围栏回落规则）+ 挂载 AcceptancePanel 争议视图 JudgePanel（反驳输入 → 建议赔付+话术 → 一键采纳）→ 单测 323 全绿、tsc/lint 0 错、build 通过、API 双场景实测 200（全责¥200/反驳降至36%） | 2026-08-13 |
| ADR-0009 多因子反欺诈探针 | ✅ 第五圈风控：`base/risk/sentinel.ts` 四路信号聚合（设备/信用/行为/图）+ 高危单因子不被稀释（宪法 #9 兜底）+ 缺数据剔除重归一（宪法 #10）+ 进家类目加权 +9 单测 → 发布闸门接入（high → `blocked:"sentinel"` 冷拒 / watch → 放行+事件流）→ PublishSheet 拒检提示 + AdminPanel SentinelDashboard 分数环/因子条/事件流 → 单测 332 全绿、tsc/lint 0 错、build 通过、浏览器实测（多开高危→发布被拒+仪表盘设备/图谱双因子触发） | 2026-08-13 |
| ADR-0010 隐私号+IM 归位 | ✅ 第二圈通讯：`base/comm/privacyNumber.ts`（48h 双向虚拟号池、掩码、拨入方向、终局回收 + 幂等分配）+ `base/comm/im.ts`（私信线程/未读/已读，pair 幂等）+ `lib/chat/*` git mv → `base/ai/chat/*`（8 import 全改，保历史）+ store 接线（acceptClaim 自动分配隐私会话 + sendIm/markImRead）+ transport union 合并 + ContactCard（掩码号+模拟拨号+私信气泡/未读红点）→ 单测 338 全绿。**2026-08-13 修复：** partialize 补隐私三字段（刷新即丢）、DEMO_POOL 改 11 位真掩码（原 7 位掩码空操作）、revokePrivacy 终局回收接线（完成/取消/争议结算/撤单）、ContactCard 倒计时实时刷新 | 2026-08-13 |
| ADR-0011 语义推荐+BI | ✅ 第三圈：`base/ai/embed.ts`（中文字 bigram TF 余弦，零依赖）+ `base/ai/bi.ts`（中文指标/类目/时间/TopN 解析 → 聚合，不依赖 LLM）+6 单测 + **宪法 #7 合规**（LLM 介入点评估已记录）。**2026-08-13 接线：** ChatPage 输入时实时语义推荐活跃局（chips 点击跳雷达）+ 统计类问题本地 BI 引擎直答（askBi store action，命中关键词不耗 LLM），单测 389 全绿 | 2026-08-13 |
| ADR-0012 鉴真+签章/保险 | ✅ 第三/四圈：`base/ai/forgery.ts`（五信号加权鉴真：EXIF/文件名/复用/时间/比例 + 文本重复 + LLM 复核降级链）+ `base/platform/signInsure.ts`（djb2 签章验签防篡改 + 履约险投保/理赔幂等壳）+9 单测。**2026-08-13 接线：** AcceptancePanel 争议凭证输入实时鉴真（checkTextEvidence 对比历史凭证，显示疑点分与级别）；**批次 B：** 验收签章（acceptFulfilment 生成 signedDoc 存证，争议面板验签展示 🔏 章号/签名者/时间）；履约险壳仍为库储备（保单/理赔待订单流接入） | 2026-08-13 |
| ADR-0013 危机+脱敏/遗忘 | ✅ 第五圈：`base/safe/crisis.ts`（级别 0-3 + EPA 通知递增：联系人→平台值班→警方通道 + 去重/处置闭环/SMS 模板）+ `base/safe/privacy.ts`（五类掩码 + 遗忘权请求幂等 + 按域匿名化）+5 单测。**2026-08-13 接线：** ProfilePage「安全中心」（SOS 三级别发起 + EPA 通知对象展示 + SMS 预览 + 处置闭环；数据脱敏五类掩码演示；遗忘权按域申请幂等 + 处理状态列表）；store 持久化 crisisRecords/forgetRequests（transport merge 已并入） | 2026-08-13 |
| ADR-0014 韧性四件套 | ✅ 第六圈库层落地：`base/platform/offlineQueue.ts`（幂等入队/重放/指数退避）+ `circuit.ts`（熔断 3 次→open→冷却→half-open 探测 + 供需杠杆信号）+ `resilience.ts`（degrades 降级链 + 数据湖哈希链存证/全链校验 + AB 分流/胜负判定）+7 单测。**2026-08-13 批次 B 接线：** ① 熔断器接入 clusterPushes（连续上游失败 → open 熔断 → 冷却后半开探测 → 熔断期直接本地抽取，回执 trip 状态持久化）② sendIm 弱网离线缓冲（navigator.onLine=false 入队，online 事件自动重放 + ContactCard 队列提示条/手动重发）③ 数据湖哈希链存证（验收/开争议/争议终局三事件 append，AdminPanel「数据湖存证」块：全链校验 ok/brokenAt + 最近 6 条）；AB 平台与 degrades 维持库层（无真实分流场景，不硬造） | 2026-08-13 |
| ADR-0015 表单+geo 适配 | ✅ 第一圈库层落地：`base/form/dynamicForm.ts`（六类型字段/校验/渲染描述器/submittable，弹药填表即出表单）+ `base/geo/geoAdapter.ts`（GeoSrc 接口 + Mock 演示 + 注入点，mobile location.ts 消费方声明）+3 单测。**2026-08-13 批次 B 接线：** 新增 `components/ui/DynamicFormView.tsx` 通用渲染端（input/textarea/picker/checkbox/group 全类型 + 校验错误 + submittable 门禁），ProfilePage「紧急联系人登记」真实消费（CONTACT_SCHEMA 三字段 schema 驱动，SOS 发起读取已存联系人）；geoAdapter 维持 Mock（RN 端注入留给 mobile 融合立项） | 2026-08-13 |
| ADR-0016 未成年人分级+免打扰 | ✅ 第五/六圈（对标 deep-research 吸收）：`base/safe/ageGate.ts`（分级 adult/teen/child：14 岁下监护人同意、18 岁下资金闸全拦且不因 guardMode=false 解除——法规对齐《未保条例》§31/§43 +《未保法》§72/§76，非法禁）+ `base/platform/quietHours.ts`（用户自主静音窗口、urgent 危机永推、合并/拆分窗口，不绑付费）+ `ammo/risk-rule` 新增 age-required 引信（全局默认开 + 夜骑/夜爬类目成人专属）+16 单测。**2026-08-13 修复：** 资金闸补齐响应押金（openClaim/joinSeat）与竞价（placeBid）真实资金入口（原仅发布端）；儿童「仅浏览」规格对齐（publish/respond 拦截） | 2026-08-13 |
| ADR-0017 组织者把关层 | ✅ 第二圈（Meetup 吸收项①）：`base/order/wave.ts` 加 `needApproval`/`joinRequests` + 纯函数 `requestSeat`/`approveRequest`/`rejectRequest`（不扩 ClaimStatus 状态机，宪法 #2 接口保守）+ store `joinSeat` 审批拦截 + `requestSeat`/`decideRequest` + UI（WaveCard「申请加入/待审批」态、WaveFeed 申请接线、MyWaves 发起人审批面板批/拒、PublishSheet 开放局审批开关）+5 单测 → 387 全绿。**2026-08-13 修复：** decideRequest 补发起人身份校验（防自批自申）+ no-show 欠款锁定 + 一人一位（与 joinSeat 同防线）；WaveCard 被拒态修复（待审批态改由 store 驱动，被拒可重新申请；接单失败不再污染 UI） | 2026-08-13 |
| P0 工程化（Zustand/Framer） | ✅ | 08-03 |
| P1 3D 栈（R3F 组件化） | ✅ | |
| P2 UI 设计系统 | ✅ | |
| P3 真实地图 | ⏳ 待办（先数据化 lat/lng → Leaflet+OSM 免费接入，见「六」） | |
| P4 Supabase 化 | ⚠️ 仅 `p2p_broadcast` 单表实时广播；**本地无 ANON key 时 transport 自动降级为 local**（同设备多 tab 仍可用，见 `base/platform/p2p/transport.ts`） | |
| P5 PWA 深化 | ✅ 已实测验证：`deviceMemory=2` 降级生效（DPR 锁 1/粒子减半）+ 离线全流程 5 屏可浏览 + lounge.glb 预缓存命中 |
| P1-P7 waves 撮合闭环 | ✅ `11f703e`（P2P 广播/磋商/鸽子险/评价/治理/信任，6 E2E 进 CI） | |
| 开放局/拼位 Open Match | ✅ `44aabe2`（拼位/满员成局/人均价，3 tab E2E） | |
| 跨 tab 广播竞态修复 | ✅ `51b2580`（union 写合并 + seed + portal） | |
| 信任闭环三缺口 | ✅（成团失败退款 / 24h 分级取消 / no-show 锁定） | ADR-0001 |
| 主 LLM 切 Zhipu | ✅ `7d73ef2` | |
| 发布费 + 每日免费配额 | ✅ `c5475c5` | |
| M3 验收/争议模块 | ✅ Slices 1-7 全落地，E2E 绿 | ADR-0002 / acceptance-module-tasks.md |
| Dev 进程守护 / 生产重启脚本 | ✅ `6de131a` / `af375da` | |
| 4 UI 死按钮接真实行为 | ✅ `ee4ea35` | |
| P3 数据化前置（lat/lng 建模） | ✅ `src/lib/geo.ts`（Haversine/附近排序/确定性兜底坐标）+ Wave 接口 `geo` 字段 + 5 单测 | |
| S1 匿名光点热力图 | ✅ `SpatialHeatMap.tsx`（活跃 waves 投影 CSS 网格地图，热度光点匿名聚合，浏览器实测 3 信号波渲染）+ 挂载 radar feed | |
| 场景模板 ×4 | ✅ `sceneTemplate.ts` 映射 + `SceneTemplate.tsx` 程序化舞台（球局→半场网格/约拍→取景光场/城市历史→室内起居，lounge.glb 保留）+ 浏览器实测 | |
| S2 AI 主动诊断 | ✅ 发布后无人响应 ≥2min 诊断卡（`diagnostic.ts` 纯函数 + `/api/diagnose` 复用 cluster 三级降级，Zhipu 实测生效）+ `DiagnosisCard.tsx` 挂载 MyWaves，浏览器实测 | |
| S3 关系沉淀（转友） | ✅ 72h 自动撤回转友状态机（`friends.ts` 纯函数）+ `FriendKit`/`FriendList` 挂载 ProfilePage/MyWaves/履约区，双 tab 实测：发布→接单→履约→72h 评价窗→转友→接受 | |
| 拼位裂变 ShareKit | ✅ 分享文案复制 + 伪二维码 + `fissionIncrement` 防自刷计数（回应/成交才 +1，按人去重）+ 分享链接 `?wave=` 直达置顶 | |
| 真机扫码（本地闭环） | ✅ `ScanMockSheet` 接 getUserMedia 环境后置摄像头 + jsQR 逐帧解码 ShareKit 链接 → 局详情 + 加入拼位直达；无摄像头/无权限自动降级回模拟扫码（演示可用）；`scan.ts` parseWaveUrl 纯函数 +7 单测 | |
| 撮合偏好可编辑 | ✅ `prefs.ts` 四维选项池（半径/预算/水平/时间）纯函数 +7 单测 + usePrefStore persist，ProfilePage 标签点击循环切换 + 重置 | |
| 分享真二维码 | ✅ ShareKit 伪码 → `qrcode` canvas 生成真二维码（离线本地画，无网络请求）；`qr.test.ts` 闭环单测 qrcode→pngjs→jsQR 还原分享链接（+4 单测，含中文/转义域名） | |
| 本地系统通知 | ✅ `systemNotify.ts` 纯 diff（成局/新报价/拼位占座/正式接单/好友申请 五类事件，+8 单测）+ 浏览器 Notification API 窗口安全封装；NotificationCenter 跨帧 diff → 弹系统通知 + 面板授权按钮 | |
| 本地上传头像 | ✅ `IdentityAvatar`（有头像显示图片 / 无则 emoji 兜底）+ `lib/avatar.ts` FileReader→canvas 居中裁切压缩 96×96 JPEG，Identity.avatar persist，三处展示（首页/雷达头/个人中心）同步 | |
| UI/UX 打磨九件套 | ✅ `docs/UI-UX-BACKLOG.md` 全清零：① 发布 CTA 主视觉（渐变+光晕+入场动画）② 竞价卡虚线玻璃+演示沙盒标识降权 ③ 空态三步引导（localStorage 记忆）④ AI 屏语音入口气泡 ⑤ 行程屏真实 bookings 优先入时间线 ⑥ 头部层级+状态圆点 ⑦ Dock layoutId 光晕滑块 ⑧ 竞价按钮触控尺寸 ⑨ 全局 Toast（lib/toast + ToastHost + 发布/拼位接入，MutationObserver 实测弹出）| |
| **8 维全息弹药 + AmmoFactory 流水线** | ✅ `src/types/ammo-schema.ts` 增 8 维契约 `IHolographicAmmoConfig`（D1 准入 / D2 计价护栏 / D3 引信 / D4 传感降级 / D5 正向钩子 / D6 逆向违约阶梯 / D7 清算仲裁 / D8 视界表单 + version 元数据）+ `ICancellationTier`/`ISplitRules`/`SensorKind` 等；`src/ammo/factory.ts` 四道工序流水线（validateAmmoConfig 静态语义审查：资金守恒 =1.0 容差 1e-9 / C2_IN_HOME 公安背调或安全分 ≥700 一票否决 / 加价熔断 ≤0.5 / 计价边界 / 阶梯边界 / 钩子白名单解析，红线 1 零 eval）+ `HOOK_OPERATOR_REGISTRY` 6 算子静态白名单 + `assembleAmmo` deepFreeze 全图冻结 + `registerDynamicAmmo` 热注册；`src/ammo/registry.ts` 增 `DYNAMIC_AMMO_POOL`（检索链：动态池→官方→四表聚合→默认保底；本池定义在装配层 factory.ts，registry re-export，循环依赖根治）；`src/base/ammo/runner.ts`（AdvanceInput.ammoSnapshot 快照优先调度 + ctx 透传）；白皮书 §十 归档（8 维字典 + 4 级工序 + car-wash/appliance-repair 声明式样例）→ **三大标杆弹药（housekeeping-v1 / meetup-social-v1 / companion-v1）已 100% 8D 全息化流水线出厂**（模块加载期强制门禁，dispatchRule/sop 显式保留，SensorKind 增补 5 降级成员）→ 单测 **1217/1217 全绿**（本次 +7）、tsc 0 错、build 通过 | 2026-08-16 |
| **长尾非标即时量产大考（DRONE_CROP_SPRAY 农田无人机植保）** | ✅ 全新类目零静态文件 100% 纯内存动态量产实证：`src/ammo/dynamic-production-exam.test.ts` 考卷 8 项全绿——① 口语化非标诉求运行时组装 8 维全息配置（D1 CAAC 飞手执照+无犯罪背调 / D2 起步 ¥500·地板 ¥300·天花板 ¥3000·熔断 50% / D3 💥碰炸引信 / D4 GPS+水印相机降级阶梯 / D5 三钩子白名单 / D6 三阶段违约阶梯 / D7 12h 代验收+85/10/5 分账 / D8 视界令牌+动态表单）；② Linter 真实质检通过 + 资金不守恒/未知钩子/熔断超限三探针全拦截；③ registerDynamicAmmo 热注入 DYNAMIC_AMMO_POOL → 检索链第一顺位 + deepFreeze 全图冻结；④ AmmoRunner 五态全链路实测（CAS 乐观锁 0→1→2→3→4、托管 ¥500 全款冻结、增项 +¥100 ≤ 50% 熔断放行、双向水印照片验收证据、SETTLED 微信收付通指令 服务者 ¥510/平台 ¥60/保险计提 ¥30 ≡ ¥600 严格守恒）；⑤ 熔断拒付（¥400>¥250 BLOCK）+ CAS 冲突阻断 + 碰炸引信双闸门拦截；⑥ 零静态文件实证（磁盘扫描无 drone 前缀文件 + OFFICIAL_AMMO 无键 + 池内唯一来源）。全程零 factory/registry 改动（无断线）→ 单测 **1217→1225/1225 全绿**（本次 +8）、tsc 0 错、build exit 0、收敛门禁 exit 0 | 2026-08-17 |
| **发布端动态长尾弹药直拨（P0 缺口闭环）** | ✅ `resolveAmmoIdForPublish` 接通 `DYNAMIC_AMMO_POOL`：registry.ts 新增 `resolveDynamicAmmoByInput` 纯函数（精确 class key → D8 中文类目别名 aliases 遍历只读匹配，全图冻结零写入），发布链检索链升级「动态池直拨 → 官方中文映射 → 聚合/保底」，PublishSheet 以口语化中文发单即写入动态弹药 ammoId（如「农田无人机植保」→ drone-crop-spray-v1，履约座舱 getAmmoById 反查装载闭环自洽）；契约增补（宪法 #2 只增补）：`IHolographicAmmoConfig.aliases?: string[]`（D8 声明式检索别名）+ factory 非法别名拒出厂；大考考卷增发布端接线环节（未注册中文回落 default-ammo / 双别名直拨 / 官方家政·组局·陪玩映射回归护栏 / 池删除后别名随删失效，检索位 100% 运行时）→ 单测 **1225→1226/1226 全绿**、tsc 0 错、build exit 0 | 2026-08-17 |
| **D8 视界与动态前端插槽深度装配** | ✅ 新增 `slots/DynamicAmmoSlot.tsx` 通用无头动态履约插槽（bizParams 参数快照 / WATERMARK_CAMERA Before·After 打卡区 / fuzePolicy 引信徽标 / 申请调解申诉，自包含 CSS + 48px 触控，弹药 theme 微氛围 dyn-* 主题类）；`FulfillmentCockpit` 扩展 `CockpitScenario` 第四态 `dynamic`（SCENARIO_THEME_META.dynamic + describeCompletionCta.dynamic 分支），新增 `dynamic?` 插槽透传 props（未提供时优雅降级不渲染不白屏）；`resolveCockpitScenario` 升级弹药声明 `cockpitSlot` 优先分发（COCKPIT_SLOT_SCENARIO 映射，DRONE_CROP_SPRAY → HousekeepingSlot 复用家政插槽），未命中制式回落 `dynamic`（原 meetup 兜底）；`DynamicDraftCard` 新增 `describeFormSchemaFields` 纯函数投影 formSchema fields（string/number/enum 类型归一 + options + required 星标 + 默认值），草稿卡渲染扩展字段行（可点击微调）+ 弹药 theme 主题类（draft-* 微氛围）；FulfillmentCenter 达成交付：dyanic 场景透传 ammo/bizParams/证据照片/上传与申诉回调接线 → 单测 **1226→1235/1235 全绿**（vitest 537→546，本次 +9）、tsc 0 错、build exit 0、收敛门禁 exit 0 | 2026-08-17 |
| **P2 战役战斗目标：L3-M5 对话式数据 BI 引擎与动态看板闭环** | ✅ `src/base/ai/bi.ts` 强化为**对话式报表引擎**（双链架构：① 确定性核心链——`parseConversationalBiQuery` 意图分流四路（品类违约 / 资金走势 / 服务者履约 / 全局兜底）+ 时间范围提取（近 N 天 / 全量）+ 聚合计算 + `IBiReportPayload` 图表载荷装配（BAR/LINE/PIE/TABLE），纯函数零依赖、结果可复现；② LLM 增强链——5-provider Gateway（`completeText` task: diagnose 顺序降级）仅增强 summary 归因诊断文案，动态 import + 全异常静默降级回规则摘要，**红线 1 离线/无 key 100% 兜底绝不抛异常**；数据结构守恒：chartData 数值全部来自输入行聚合，佣金 15% / 保险 5% 估算口径在 summary 显式标注）；既有 `parseBiQuery`/`runBi` 兼容保留零改动）+ 新建 `src/app/api/admin/bi/route.ts`（POST 管理端查数 API：withAuth + ADMIN 校验 + contracts/disputes 组装行级上下文）+ 新建 `src/components/admin/ConversationalBiView.tsx`（**对话看板**：自然语言输入 + 三枚快捷气泡 + AI 归因诊断卡 + KPI 指标网格（趋势箭头/环比）+ 零依赖 SVG/CSS 自适应图表（BAR 横条 / LINE 双序列折线+面积 / PIE 圆环 / TABLE）+ 追问引导 chips）+ 挂载 `src/app/admin/dashboard/page.tsx`；白皮书 §3.4.2 `L3-M5` 升级 🟢 100% 已闭环（文件落点 + 测试凭据）→ 单测 **1251→1266/1266 全绿**（vitest 562→569（+7 组件事例）、node:test 689→697（+8 bi 引擎事例））、tsc 0 错、build exit 0、收敛门禁 exit 0 | 2026-08-17 |
| **P1 攻坚战役·步骤一：地图技术栈全仓归一（Leaflet 彻底出清）** | ✅ 消除地图双轨（Batch 4 C15 收敛）：`provider/orders/[id]/OrderFulfillmentClient.tsx` 从 Leaflet `MapComponent`（动态 import + OSM 瓦片）迁移至 MapLibre 3D `MapView`——`MapView` 增可选 `focus?: GeoPoint` 契约位（宪法 #2 只增补：初始化相机锚定目标点 + zoom 15，缺省回落 MAP_CENTER 全城视角），履约页以 `focus` + 单点 hot=1 光晕（`target-${demand.id}`）呈现服务目标点 LBS 标记；`git rm` 物理删除 `src/components/MapComponent.tsx` + `npm uninstall` leaflet/react-leaflet/@types/leaflet 三依赖出清（全仓 grep 零 Leaflet 残留，仅注释字面词）；台账 DEBT_CLEANUP_REGISTER C15 勾销「已收敛归一」 | 2026-08-17 |
| **方案 A：OTO 专属空间毛玻璃登录抽屉（AuthSheet，消除前台跳老 /login 割裂）** | ✅ 新建 `src/components/oto-ui/auth/AuthSheet.tsx` 半屏毛玻璃抽屉：严格消费 `.oto-app` 样式体系（`--color-surface-subtle` 混合底 + glass 拟态 + 极光渐变标题 + 48px 触控靶区）；三通道登录——📱 手机号验证码（演示固定码 1234）/ ✨ 一键演示角色（雇主 Alex / 服务者王姐 / 组局主理人阿强）/ 🛡️ Web3 钱包（模拟连接 → 脱敏地址）；`useDragToDismiss` 顶部把手下拉 >35% 平滑收起（220ms 离场动画）+ 遮罩点按收起；登录态持久化 `oto-auth-account`（localStorage）+ `oto:auth-changed` 事件广播即时刷新；前台接电——`(oto)/page.tsx` 根挂载 AuthSheet（全局呼出 `oto:auth-open` 事件，五屏空间零整页跳出），`ProfilePage` 顶部访客卡改「登录 · 注册 / 切换账号」入口 + 登录态即时刷新（已登录显示昵称+角色）；老 `/login` 保留为后台与协议管理专区备用通道；单测 +12（打开/三 Tab/手机号全链/错误码拦截/钱包连接/手势 36% 触发·30% 复位/遮罩收起/已登录卡/切换账号回表单/退出/事件导出）→ 单测 **1235→1247/1247 全绿**（vitest 546→558）、tsc 0 错、build exit 0 | 2026-08-17 |
| **P2 战役·第一波攻坚：L4-M4 危机干预协议与 L4-M5 隐私全周期遗忘闭环** | ✅ ① 新建 `src/base/safe/crisis-tracker.ts` 危机干预增强引擎——**轨迹面包屑** `recordBreadcrumbPoint`（最近 N 处 FIFO 裁剪 + 非法坐标拒绝）+ `detectTrajectoryAnomaly`（≥120km/h 超速漂移预警，Haversine 纯数学 120km/h 边界实测）+ `buildPoliceTrajectoryPayload`（压缩 trail 载荷「lat,lng,acc,ts|…」+ 末点 + 异常标记供 110 通道）；**离线录音切片加密缓冲池** `AudioChunkBuffer`（chunkId/waveId/durationSec/sha256/encryptedBase64 元数据、条数 64/字节 4MiB 双上限 FIFO 逐出、SHA-256 完整性校验、drain 一键上抛）；**60s 危机升级状态机**（TRIGGERED(0s) ➔ ACKNOWLEDGED(≤30s 确认，超窗记 breach 不阻断) ➔ POLICE_ESCALATED(≥60s 未确认警方强升级) ➔ RESOLVED，每次跃迁生成紧急通知载荷，EPA 级别映射对齐 crisis.ts）；② 新建 `src/base/safe/privacy-erasure.ts` 隐私密态销毁引擎——**《个保法》§47 账户注销 Crypto-Shredding** `executeCryptoShredding`（姓名→ANON_USER_<hash>/手机号→138****xxxx/身份证→***/地址→***/邮箱→掩码/精确坐标→置空的不可逆覆写；财务对账流水 order_no/amount_cents/split_plan_json/paid_at **依法保留零删除**；输出 `IShreddingCertificate`：证书号/销毁时间戳/执行人签名指纹/SHA-256 数据摘要 64 位 hex 确定性固化/保留流水计数与总额）；**过期完工媒体自动清理调度器** `evaluateMediaRetention`（正常完工 90 天 / 争议案件 180 天，边界精确到期即删，输出 toPurge OSS Key 清单 + toRetain 到期台账）；③ 门面接线——`crisis.ts`/`privacy.ts` 挂增强导出，前端既有导入零回归；④ 新建 **DELETE `/api/profile/delete`** 注销路由——withAuth 登录态校验 → 读取 profile PII（实名/手机号/精确坐标）+ contracts 两侧身份财务流水 → 密态销毁 → 匿名 Profile 写回 + deleted_at + 证书返回；⑤ 单测 **+21**（crisis-tracker 13：面包屑裁剪/非法坐标/120km/h 边界/hav 慢帧/警方载荷确定性/音频 FIFO/字节上限/drain/篡改完整性/EPA 触发/30s 确认幂等/超窗 breach/60s 边界强升级/已确认不升级+RESOLVED 闭环；privacy-erasure 8：五类 PII 覆写/财务保留守恒/空入参/摘要确定性 64hex/签名证书幂等/90·180 天内边界分流/未来完工保留/空清单）→ 单测 **1328→1349/1349 全绿**（vitest 578 不动 + node:test 750→771）、tsc 0 错、build 待验、收敛门禁 exit 0（零 rename）；红线 1（纯确定性：轨迹/升级时序/密态摘要全部入参化，零概率 + 时间戳注入）、红线 2（密态销毁财务边界守恒实测）、红线 3（safe 纯函数引擎零 React 反向依赖）实测满足；白皮书 §3.4.2 L4-M4/L4-M5 🟡→🟢（22/26，🟡 4 = L2-M2/L4-M2/L5-M2/L6-M2） | 2026-08-17 |
| **P2 战役·第二波攻坚：L4-M2 终端反欺诈与 L5-M2 外部合规生态闭环** | ✅ ① 新建 `src/base/risk/anti-fraud.ts` 终端反欺诈与时空防作弊引擎——**GPS 瞬移/时空穿梭检测** `detectGpsSpoofing`（相邻采样点 Haversine 瞬时速度 >300km/h 或时间倒流 dt≤0 → TELEPORTATION_DETECTED；定位精度 0 或绝对死值（恒定且 ≤2m）→ MOCK_PROVIDER_DETECTED；输出 IGpsSpoofReport：riskScore 0~1 + 处置建议 PASS/CHALLENGE_LIVENESS/BLOCK，瞬移/倒流 1.0 阻断、模拟器定位 0.85 阻断）；**终端模拟器/Headless 探针** `detectTerminalRisk`（webdriver 标志 +0.5 / HeadlessChrome·PhantomJS UA +0.4 / Emulator·Simulator 环境词 +0.3 / 移动 UA 缺触控 +0.25，确定性加权封顶 1.0，≥0.6 BLOCK、≥0.3 CHALLENGE_LIVENESS，输出 ITerminalRiskReport）；② 新建 `src/base/platform/compliance-ecosystem.ts` 外部合规生态引擎——**《电子签名法》§14《电子商务法》§52 电子合同防伪签章** `generateEContractSeal`（规范化序列化固定字段序 → SHA-256 不可变 contractDigest 64 位 hex 同输入同摘要 → IEContractSeal 含 sealId/主体/金额/条款版本/时间戳/法定存证声明；`verifyContractSeal` 篡改 1 分/1 字符/时间戳即验签失败 + 签章镜像字段防伪）+ **场景微保险秒级保单** `issueMicroInsurancePolicy`（保费 = 订单总额（分）× insuranceRatio 严格取自 ammo.holographic.splitRules（家政 0.05/组局 0.02/陪玩 0.03，缺省固定费率 0.05），分单位 Math.round 精确取整与清结算计提对齐；保额上限类目映射家政 50,000 元/组局·兜底 20,000 元；保单号 POL-YYYYMMDD-orderNoHash（SHA-256 8 位指纹）+ 30 天有效期 + 理赔报案通道 claimGateway 绑定）；③ 门面接线——`base/platform/signInsure.ts` 挂 `compliance-ecosystem` 增强导出，既有导入零回归；④ 单测 **+21**（anti-fraud 11：步行/乘车 PASS/60km 巡航合法/5 秒 50km 瞬移 BLOCK/时间倒流/精度 0 模拟器/绝对死值/空·单样本不误报/瞬移+模拟器同现 1.0/桌面 PASS/webdriver+Headless BLOCK 封顶/Emulator CHALLENGE/移动 UA 缺触控加权 0.55；compliance-ecosystem 10：签章确定性 64hex+规范化等价/篡改 1 分验签失败/订单号·条款·时间戳篡改拦截/镜像字段防伪/家政 0.05 保费保额/组局 0.02/缺省费率兜底/保单号唯一性+UTC 日期/30 天有效期+理赔通道/保额类目映射中英归一）→ 单测 **1349→1370/1370 全绿**（vitest 578 不动 + node:test 771→792）、tsc 0 错、build 待验、收敛门禁 exit 0（零 rename）；红线 1（GPS 速度/终端加权/签章摘要/保费计算全入参化纯确定性实测）、红线 2（保费分单位与保险计提口径守恒实测）、红线 3（risk/platform 纯函数引擎零 UI 反向依赖实测）满足；白皮书 §3.4.2 L4-M2/L5-M2 🟡→🟢（24/26，🟡 2 = L2-M2/L6-M2） | 2026-08-17 |
| **阶段1 拟人全链路大考（多轮实测收官）** | ✅ 拟人诉求「我需要10点来人打扫房间」全链路双域考卷：node:test 17 用例 `src/ammo/real-user-sim-basic.test.ts`（A 注册表直拨/意图解析/MockEngine 真实口语识别 · B 草稿卡数据源/引信双闸 · C SHA-256 存证/双拍验收/AIGC 伪图 CRITICAL 阻断/增项报价 BLOCK · D 引擎对账守恒/D7 三比 85·10·5 ¥144.50·17.00·8.50≡170/微信收付通指令 · E let 守护扫描 · F 15s 窗口+报告）+ vitest 5 用例 `real-user-sim-store.test.tsx`（发布→托管全款 ¥120=12000 分→王姐 direct claim→MATCHED+隐私会话→HousekeepingSlot 座舱→增项 ¥170→INSPECTED→SETTLED 终局 🟢+指令），零新增生产代码；白名单最小接线：`--experimental-transform-types`（MockEngine 参数属性）+ `scripts/node-ts-loader.mjs` 解析钩子（无扩展名相对导入 + @/ 别名，基线零行为变化）；实测 6 断线点/观察项如实上报（①「10点」不在时间词表 ② 意图层发布关键词表仅「保洁」无「打扫」两域口径差 ③ CleaningCheckHook AFTER+SKIP 无照片不阻断（领域钩子直调为准入必填）④ 引擎对账 90/10 未消费弹药 D7 三比，放款 ¥153 vs 指令 ¥144.50 差 ¥8.50 保险计提仅弹药/指令侧 ⑤ 草稿卡「¥60/小时 ×1小时起」未消费 holographic minHours:2（起步 2h=¥120 只在弹药域）+「建议起价 ¥50」旧 scene 建议 ⑥ 新用户登录 500「创建用户资料失败」Supabase 依赖 + 发布弹层关钮需二次点击）；浏览器 CDP 实测 AI 对话链路全通（口语→家政品类命中→时间/频率/区域/预算追问→整理卡→时段卡，与引擎考卷逐条一致）；门禁：tsc 0 错 + eslint 0 error + `npm test` **1414/1414 全绿**（vitest 583 + node:test 831，1392 基线 +22）+ 收敛门禁 exit 0 | 2026-08-17 |

| **阶段1 拟人大考 4 缺陷定向修复（多轮实测闭环 → 修复合入）** | ✅ ① 意图层口语词表/时间精确化：`voiceIntent.ts` publishHit 补齐「打扫/做卫生/扫地/擦玻璃」口语同义词、matchTime 输出精确时分（10点→10:00、10点半→10:30、X点Y分→HH:MM），两域词表口径对齐（回归 +1 用例）；② 引擎缺省分账对齐弹药 D7：`runner.ts` buildSettlementLedger 未显式传 platformRate 时强制消费 `holographic.splitRules`（家政 85/10/5 → 服务者 ¥144.50/平台 ¥17.00/保险 ¥8.50 三比守恒，保险计提入账对账单 `split.insuranceFee` 新可选位，90/10 裂隙 ¥8.50 消除、引擎放款与收付通指令分角一致）；显式 platformRate 仍尊重显式值（既有调用零变化）；③ 草稿卡全息优先定价：`DynamicDraftCard.tsx` 新增 resolveDraftPricing（holographic 权威源缺省回落顶层，起步时长脱节兜底；实测「1小时起」为旧 dev server 陈旧产物，弹药本为 minHours:2）；④ 断言同步（人类裁决）：A3 断线点实证翻转为修复实证 + D1/D3② 三比守恒与指令一致性 + escrow.test esc-4/esc-5 期望对齐弹药契约（90/10 旧缺省废弃）；门禁：tsc 0 错 + eslint 0 error + `npm test` **1415/1415 全绿**（vitest 583 + node:test 832）+ build exit 0 + 收敛门禁 exit 0（CONVERGENCE-LOG 已登记） | 2026-08-17 |
| **阶段3 非标定制语义驯化与全链路动态风控闭环** | ✅ ① **语义归一化引擎**：新建 `src/base/ai/intent-normalizer.ts`（纯函数，确定性正则+区间算术，≤1ms）：女仆装→THEMED_MAID / JK·制服·Cosplay→THEMED_COSPLAY / 礼服·西装→FORMAL_UNIFORM / 工装→CUSTOM；年龄区间多形态（在…之间/到/至/单边界/乱序纠正）+ 性别偏好提取（着装词消费防「女仆装→FEMALE」误伤）；中性化展示文案（杜绝擦边词公海直显）；**两道防线分流**：涉黄涉赌涉暴（autoFlag 命中）→ blockedReason 硬阻断留痕 + 不产出公海文案；非标定制 → 100% 驯化禁杀；红线 1/3 合规（零概率、零 React/Store 依赖）；② **契约层**：`ammo-schema.ts` 增 ICustomRequirements（dressCode/ageRange/genderPreference）+ INormalizedCustomIntent（+cleanText/isSensitiveCustomization/blockedReason），IAmmoDefinition / ISubEventContext / ITriDimensionalCredit（+age）/ voice PublishWaveIntent 全链路可选透传零破坏；③ **意图集成**：voiceIntent mockVoiceIntent 发单自动驯化挂载 wave.customRequirements；④ **供给端年龄硬门禁**：tri-credit evaluateTriCreditAdmission 第三参 custom → AGE_MISMATCH 一票拦截（年龄未知零误杀）；WorkerWorkbench 档案 age + evaluateWorkerQualification ageRange 比对 + 「年龄条件不匹配」标签 + 接单按钮禁用（演示门禁 20-30：kail 25 放行 / wang 45 拦截）；⑤ **多因子动态风险评分 + 引信自适应升级**：runtime-monitor computeRiskScore（基础 20 + 敏感定制 30 + 夜间 20 + 任一信用<70 20，clamp 0-100）+ 风险 ≥50 → PROXIMITY_ENHANCED 强制 virtualNumber/tripGuard/chatModeration 三武装 + 徽标「🛡️ 强化安全守护中（虚拟号+实时存证）」；门禁：tsc 0 错 + `npm test` **1450/1450 全绿** + build exit 0 + 收敛门禁 exit 0（含阶段2 考卷翻转为修复实证 B5/B9 + 新增 B20-23 驯化闭环 4 例；组件徽标接线位输出已就绪，座舱 UI 消费接线待下批） | 2026-08-17 |
| **阶段4 方案 A：Wave 实体强类型契约扩列与履约座舱 UI 全量闭环** | ✅ ① **底座契约扩列（宪法 #2 增补不改义）**：`base/order/wave.ts` Wave + CreateWaveInput 增可选 `customRequirements?: INormalizedCustomIntent`（inline import 类型，红线 3 零反向依赖），createWave 透传缺省 undefined 向后兼容；② **发单端全量贯通**：ChatPage 语音意图 ref 缓存注入 createPendingWave（普通文本发单零影响）、PublishSheet 需求备注 note 经 normalizeCustomIntent 驯化后注单（违禁命中走既有治理闸门 2）；③ **座舱与插槽全量消费**：FulfillmentCenter 透传 wave.customRequirements + evaluateRuntimeSafety（家政入户基础 20 + 敏感定制 30 + 夜间 20，真实本地时刻）→ PROXIMITY_ENHANCED 注入 forceArmed/safetyBadge；FulfillmentCockpit 强化安全守护条「🛡️ 强化安全守护中（虚拟号通话+全程行程守护+敏感词实时监听）」（引擎文案优先 + 三强制副标）+ 中性化定制标签栏 describeCustomRequirementTags（`[工作着装: 女仆主题]`/`[期望年龄: 20-30岁]`/`[性别偏好: 女性]`，无定制零渲染）；HousekeepingSlot/DynamicAmmoSlot 参数快照区结构化标签卡（DRESS 中性投影，严禁原始粗糙词直显）；④ **测试矩阵**：FulfillmentCockpit.test.tsx +7 例（forceArmed 渲染/引擎文案优先/标准回退/双插槽透传/纯函数投影）；门禁：tsc 0 错 + `npm test` **1458/1458 全绿**（vitest 583→591 +8，node:test 867 不动）+ build exit 0 + 收敛门禁 exit 0（CONVERGENCE-LOG 条文 #2 已登记） | 2026-08-18 |
| **D-3 架构防腐战役：src/base/ 进家业务词汇彻底解耦与引信参数化** | ✅ ① **底座零业务词复核**：`src/base/risk/sentinel.ts` 无任何硬编码进家词表（`isHomeAccess(category, keywords = [])` 纯入参、`sentinelCheck` 消费 `input.homeAccessKeywords`，未注入默认 `[]` 零加权——a11d85e 已迁出，本战役复核确认）；② **弹药层权威装配（本战役核心）**：`src/ammo/risk-rule.ts` 新增 `HOME_ACCESS_KEYWORDS_MAP` 权威类目映射（housekeeping/家政保洁/厨师·上门做饭/遛狗遛弯/水电维修/陪诊陪护/按摩推拿 7 键，中英双键同表）+ `homeAccessKeywordsFor(category)` 升级为 MAP 命中优先 → 引信参数回落 → `[]`（向后兼容零断线）；`src/ammo/housekeeping.ammo.ts` 显式装配 `HOUSEKEEPING_HOME_ACCESS_KEYWORDS`（直取 MAP.housekeeping，D3 碰炸引信联动 sentinel ×1.2）；生产调用方 `useWaveStore.ts:413` 经 `homeAccessKeywordsFor` 显式装填不变；③ **测试凭据 +4**：sentinel.test 新增 2（中英双键同表/未装填回落全局引信+零误命中、housekeeping 弹药键注入 ×1.2=96）、housekeeping.ammo.test 新增 1（弹药显式装配=权威 MAP + 中英双键一致）→ 单测 **1465→1468/1468 全绿**（vitest 598 + node:test 870 本次 +3，P0-2 战役 +7 一并回填）；④ **白皮书销项**：`docs/ARCHITECTURE_TAXONOMY.md` §5.2 D-3 由 🔴 → 🟢 100% 完全闭合（进家词表域；decompose isOnsite / llmEngine·mockEngine 意图识别词残余如实标注另域追踪）+ §十一 收敛顺序勾销；⑤ **E2E 实测 12/12 PASS**（生产服务重启为新构建 pid 8580：上一轮 E2E 挂死在「清 SW 缓存」步骤，根因 = 旧 `next start`（pid 17988）在 `npm run build` 重建 `.next` 后 SW 更新检查卡死，`restart-prod.mjs` 重启后 12 条全绿）；门禁：tsc 0 错 + build exit 0 + 收敛门禁 exit 0（CONVERGENCE-LOG 已登记） | 2026-08-18 |
| **/api/tts 生产路由 Webpack 空桩崩溃与流挂起根治** | ✅ ① **打包层根治**：`next.config.ts` 追加 `serverExternalPackages: ['ws','bufferutil','utf-8-validate']`——webpack/turbopack 不再将 ws 的可选原生加速器 bufferutil 打成空桩（`()=>{}`：require 不抛错 → ws 内置 try/catch 不触发 → 掩码 wrapper 安装 → 发帧 `TypeError: b.mask is not a function` → msedge-tts 音频流永不产出/永不结束 → 路由无限挂起，全天刷屏 unhandledRejection，本机 prod*.err.log/p02.err.log 均实证）；外置后运行时 require 抛错 → ws 兜底纯 JS 掩码（与 dev 行为一致）；② **环境层双保险**：`restart-prod.mjs` 注入 `WS_NO_BUFFER_UTIL=1`/`WS_NO_UTF_8_VALIDATE=1`（PS 5.1 经 `$env:` 继承注入）；③ **请求层 8s 看门狗（红线 5 确定性时限）**：`route.ts` 单一 8s 总预算——GLM fetch 挂 AbortSignal、edge 消费走 `consumeTtsStreamWithDeadline`（Promise.race + 超时 `tts.close()` 释放套接字 + 504 交客户端 speechSynthesis 降级链），消费侧错误双分支收敛杜绝二次 unhandledRejection；④ **测试 +5**：`src/app/api/tts/route.test.ts`（vitest：停滞流确定性超时/快流拼接/空流/报错流/竞速不误伤）→ 单测 **1468→1473/1473 全绿**（vitest 598→603）；⑤ **实测闭环**：`npm run build` exit 0 + 产物复核（route.js 无 `b.mask` 包装器/无 bufferutil 字符串）+ 真实 HTTP 实测 POST /api/tts → **200 audio/mpeg 14544B 1.64s**（修复前同请求永久挂起）+ 验证实例错误日志 0 unhandledRejection/0 TypeError + 正式实例复测 200/26KB/1.99s；门禁：tsc 0 错 + build exit 0 + 收敛门禁 exit 0（CONVERGENCE-LOG 条文 #10 已登记） | 2026-08-18 |
| **E2E 拟人实测 4 大物理缺陷定点修复（全产品端到端验收）** | ✅ ① **P1 双拍校验闭环**：`FulfillmentCenter.tsx` `handleComplete` 注入 `evidencePhotos`（Before/After 相位沿用插槽既有约定）→ `advanceLifecycle` payload `photos` 载荷；WATERMARK_CAMERA 弹药（家政入户）缺双拍 → 阻断 INSPECTED 跃迁 + Toast「请先完成服务前后双拍存证」（红线 1 确定性闭环，CDP 实测滞留 IN_SERVICE）；② **P1 已结算槽位释放**：SETTLED（核销 + 争议调解 BREACH_SETTLED 双路径）后 `closeWave` 归档 `wave.status=closed`——刷新实测「已关闭」徽标 + 下一 MATCHED 单无缝载入座舱；③ **P2 履约状态持久化**：`partialize` 白名单追加 `fulfilment` + `WaveBundle` 契约扩列 + `mergeByIdLevel` 透传（transport read-merge-write 原子防护不丢键，宪法收敛：条文 #3）——刷新后 SETTLED/closed 终局不回退（localStorage 实测落盘）；④ **P2 争议按钮接电**：`AcceptancePanel.tsx`「提交争议 · 按原因自动判责」静默 return → Toast 指引（未选原因/未填凭证分级提示）+ 原因+凭证齐全真实开争议落库（CDP 实测「⚖️ 争议进行中」+ disputes 持久化）；⑤ 测试 **+3**（双拍拦截滞留/注入双拍三次直通+closed 断言/persist 白名单 + 争议按钮 Toast 闭环）→ 单测 **1473→1476/1476 全绿**（vitest 603→606 + node:test 870 不动）；门禁：tsc 0 错 + build exit 0 + 收敛门禁 exit 0（零 rename）；验收报告 `docs/E2E-ACCEPTANCE-REPORT.md` 全量实测归档 | 2026-08-18 |
| **首页 9 层千层饼 → 3 层黄金座舱重构（奥卡姆剃刀战役）** | ✅ `src/app/(oto)/page.tsx` HomePage + `src/components/waves/WaveFeed.tsx` 收敛：① **5 大冗余病灶剔除**——`WaveFeed` 内移除公开竞价沙盒 `BiddingSandboxCard`、组局加速广告 `OrganizerBoostCard` + 订阅生效横幅、三步开始新手引导条（`onboardGuide` 及其消费）、WaveFeed 发布 CTA；HomePage 移除 AI 对话条（想找什么，直接说）、AI 热门服务四卡、`SearchBar` 搜索栏、旅游分类标签（`CategoryPill`/`CATEGORY_ICON`/`OTO_CATEGORIES` 消费）、热门目的地卡网格、安全四件套 `SafetyKit`、`ScanMockSheet`/`DestinationHub` 渲染（组件文件保留未删，AR 屏 Dock 直达不受影响，心愿单面板/购物车按钮保留功能 0 丢失）；② **3 层座舱**——层1 顶栏（`StatusCapsule` 五态 + SOS + 问候 + 通知/心愿单）、层2 核心行动区（唯一全局 AI 拟物发单条 `aria-label="想找什么？一句话告诉我 · 发出你的需求"` + 三大官方标杆弹药胶囊 🧽家政保洁/🏸组局社交/👥陪伴交友，映射 `getAmmoDefinition` housekeeping/meetup/companion）、层3 雷达波浪视口（直达 WaveFeed，空态极简雷达波纹）；③ **发单链路 0 丢失**——新本地组件 `DraftSheet`：点发单条/弹药胶囊 **100% 呼出 `DynamicDraftCard` 拟物草稿卡**（胶囊带弹药 `data-ammo`，发单条挂 default-ammo），「扣动扳机·一键发布」→ `PublishSheet` 完整发布链（品类/时间/地点/预算 → 广播出去 → 托管支付 → WaveFeed 即时刷新新波卡，顶栏胶囊实时投影）；④ **E2E 断言同步**（关联测试文件白名单）：9 个发单流脚本（acceptance/governance/fulfil/review/openmatch/wave/trust-open/push/trust）「发出你的需求」后追加「扣动扳机·一键发布」一步（14 处，含 trust-open CR-only 换行特例）；`e2e-app.mjs` 热卡/搜索段重写为弹药胶囊草稿卡断言 + 全局发单条 → 完整发布面板断言 + 心愿单闭环改 Dock AR 直达；⑤ 实测（生产构建 CDP）：3 层 DOM 结构（`data-layer="action"/"wave-feed"`）+ 9 层残留 0 + 胶囊→housekeeping-v1 草稿卡 + 发单条→default-ammo + 扣动扳机→发布面板 + 对端视角（清身份存储模拟新用户 reload）WaveFeed 即时刷新见「羽毛球约局 · 今天 20:00」波卡 + 接单按钮；console 仅存量噪音（GoTrueClient/THREE.Clock/云端 p2p_broadcast 404 本地沙盒降级，E2E 过滤器本就容忍）；门禁：tsc 0 错 + 单测 **1476/1476 全绿**（vitest 606 + node:test 870 不动）+ build exit 0 + 收敛门禁 exit 0（零 rename，无登记需求） | 2026-08-18 |
| **E2E 12/12 全量实跑回归与 flake 根治（3 层座舱验收加固）** | ✅ ① **入口功能缺口修复（治本）**：重构移除首页 `SafetyKit` 渲染后「安全中心 + 平台治理后台（AdminPanel）」入口全局消失 → `ProfilePage.tsx` 安全中心区块前挂回 `<SafetyKit />`（治理后台/紧急联系人/见面兜底/安全面基点真实入口恢复，E2E 导航随之改走「我的」页 6 处：trust 2 + governance 3 + 注释 1）；② **连跑 flake 根因分析**：发布链路多一层「扣动扳机」后，脚本「广播出去/立即支付 → 固定 400~800ms → 断言 localStorage」的时序余量变小，连跑高负载（chrome 残留进程）时踩线 → **7 个脚本 12 处固定短等待全部轮询化**（`waitUntil` 等 wave 落库/能力声明落盘：acceptance 场景 A/B/C、openmatch 发布、wave 发布、push 能力声明、trust 进家门单、trust-open 局 4，15s 确定性上限）；③ **match console error 过滤器补容忍** Supabase Realtime WS 断连（`supabase.co/realtime|ERR_CONNECTION_CLOSED|WebSocket connection to`，与既有「云端 p2p_broadcast 404 本地沙盒降级」口径一致）；④ **trust-open 座位按钮定位修正**：「标记未到场」座位按钮为 `aria-label` 承载（textContent 无该字样）且位于「出勤档案（Meetup 组织者视图）」折叠区内（MATCHED 态默认折叠）→ 点击前先展开折叠区（已展开自动跳过）+ aria-label 轮询 ≥2 按钮；⑤ **实测**：`verify-prod.mjs` 全量连跑 **12/12 PASS × 2 轮**（含失败重跑稳定性）+ 独立计时轮 12/12（app 17s / acceptance 30s / match 20s / openmatch 39s / push 12s / wave 17s / trust 19s / trust-open 36s / review 20s / fulfil 18s / governance 21s / offline 8s，总耗时 4.3min）+ 服务端 HTML 实证新版 3 层产物 + 浏览器 SW 旧缓存清理后渲染正常；门禁：tsc 0 错 + 单测 **1476/1476 全绿**（vitest 606 + node:test 870 不动）+ build exit 0 + 收敛门禁 exit 0（零 rename） | 2026-08-18 |
| **第 4 枚官方标杆弹药 appliance-repair-v1 8D 全息化出厂（扩品实战 · 首枚 C3_TECH_B2B）** | ✅ `src/ammo/appliance_repair.ammo.ts` 家电上门维修弹药首填技术资产运力池（三大聚类全量收编）：① **8D 全息配置**（D1 C3_TECH_B2B：实名 + 安全分 ≥70 + 公安核验 + 电工/家电维修双证书 [ELECTRICIAN_CERT + APPLIANCE_MAINTENANCE_CERT] / D2 FORMULA 公式计价 ¥30 上门检测费·60min 基准 + 地板 30 元·天花板 3000 元·增项熔断 ≤50%·SKILL_LEVEL 技能分折抵 ≤30% / D3 💥碰炸引信 / D4 GPS+水印相机降级阶梯 / D5 三算子白名单复用 ArrivalCheck/OnsiteQuote/CleaningCheck「增项确认→双拍验收」两段式微流程 / D6 4 档违约阶梯 [100% 退·途退 80%+20 元·现场扣 30 元检测费退剩余·服务中 0%] / D7 0.82/0.13/0.05 资金守恒 + **48h 质保验收期** / D8 theme=default + HousekeepingSlot + 家电类型/故障描述表单 + 七连中文别名）经 `assembleAmmo` 流水线静态审查出厂 + deepFreeze 全图冻结；② **注册表双键挂载**——OFFICIAL_AMMO 同时挂 `appliance_repair`（中文归一化直拨）+ `APPLIANCE_REPAIR`（类目大写检索）指向同一出厂产物 + CATEGORY_TO_OFFICIAL 七连别名（家电维修/维修/修空调/修洗衣机/修冰箱/修油烟机/水电维修）全解析至 appliance-repair-v1；③ 复用而非新建（HousekeepingSlot 座舱 + 三算子复用零新增，宪法 #4）；④ 测试白名单扩列 `appliance_repair.ammo.test.ts` **16 例**（8D 契约完整性/工厂出厂 frozen/APPLIANCE_REPAIR 类目直拨/七别名/ C3 准入三画像 [双证书技师通过 · 无公安背调一票熔断 · 技能分 <70 类目隔离]/碰炸引信双闸/增项熔断 base 100 加价 50 放行·60 阻断·50% 边界/全流程五态三算子/82·13·5 分账守恒×2/违约阶梯四档资金维度）→ 单测 **1476→1492/1492 全绿**（vitest 606 不动 + node:test 870→886，突破 1490+ 预期）、tsc 0 错、build exit 0、收敛门禁 exit 0（零 rename，无登记需求） | 2026-08-19 |

## 四、验证基线

| 项 | 当前值 |
|----|--------|
| 单测 | **1492/1492 全绿**（`npm test` = vitest 606 + node:test 886；2026-08-19 第 4 枚官方标杆弹药 appliance-repair-v1 装配 +16 例） |
| Lint | ✅ exit 0（0 errors）；存量 726 warnings 待渐进式治理（其中 239 为原 error 降级：no-explicit-any 184 / react-hooks 35 / react 10 / prefer-const 5 / ts 5，12 条规则降级 warn；治理后逐条恢复 error 清零），不阻断主线开发 |
| TypeScript | tsc 全绿（根 + 子项目） |
| E2E 脚本 | 12 个就绪；CI `e2e-verify` job（push 至 master 时全链 12 条回归，PR 不阻塞） |
| 运行时错误 | 0（仅 THREE.Clock deprecation 噪音；TTS 生产 unhandledRejection 已根治并实测清零） |
| 语音链路实测 | ✅ 录音→ASR 真链留待真机验证（本机无麦克风）；**TTS 链路全绿 2026-08-18：修复 bufferutil 空桩崩溃后实测 POST /api/tts → 200 audio/mpeg 14544B 1.64s（edge-tts 合成），错误日志 0 unhandledRejection，8s 看门狗兜底 504（客户端 speechSynthesis 终极降级）**；GLM 429（余额不足）→ edge-tts 兜底实测合成 mp3，双链全灭才落 speechSynthesis |
| 生产服务器 | ✅ 运行中（pid 13772，端口 3000，HTTP 200；2026-08-18 E2E 验收重启为新构建——旧 `next start` 持有 build 前 manifest 导致旧 chunk 引用，重启后新构建全量生效（page-344a…）） |

## 五、遗留缺口

1. ~~CI 少挂 2 条 E2E~~ ✅ 已修复：`e2e-trustopen`、`e2e-acceptance` 已挂入 CI（11 条）
2. ~~生产服务器未运行~~ ✅ 已启动：pid 8084，HTTP 200，重启脚本验证通过
3. ~~本地未推 commit~~（本次批次已推 `f11e8cb` 起多笔，见 LAST_SYNC）
4. ~~P3 地图仍在设计稿~~ ✅ 已落地（MapLibre + OpenFreeMap，`f11e8cb` 起）

## 六、下一步（待办）

- [x] **P3 真实地图** ✅（本批完成 ADR-0004：**MapLibre GL JS + OpenFreeMap** 免费矢量瓦片（liberty 含 3D 建筑）+ 3D 透视（pitch 25°）+ 活动 wave 光点 + 点击 FlyTo；游戏化层后置留口；低配/无 WebGL 自动降级回 CSS 网格；瓦片源单点可切）
- [x] S1 匿名光点热力图 ✅（本批完成）
- [x] **S2 AI 主动诊断** · **S3 关系沉淀** ✅（本批完成，社交层闭环）
- [x] 场景模板 ×4 ✅（本批完成）
- [x] **Supabase 全量数据化**（在线真实数据 + 离线 mock 兜底，接口形态不变）→ 前置项「真机扫码」已提前本地闭环（见三），余项仍待数据化
- [x] 拼位裂变 ✅（本批完成：分享 + 防自刷计数 + 二维码）
- [x] **P8 商业化本地闭环** ✅（本批完成：多开风控阻断发布 + 裂变回报入通知中心 + 竞价佣金结算写回真实局）
- [x] **P8 钱包账本闭环** ✅（本批完成：竞价服务费/订阅扣费/服务收益统一入 identity.ledger + WalletView 正负分色）
- [ ] P8 商业化线上化：漫游入设备表 / PWA 真推（VAPID）/ 竞价接入真实支付（LAUNCH-GAP E 组）
- [ ] 更远：灵感漩涡（概念模糊未定 · 当前理解=发现功能：刷灵感 → 一段话生成新需求局，类「AI 帮你变出局」） / 短信兜底 / 组局者订阅线上支付
- [ ] **Meetup 吸收项（5，2026-08-13 用户裁决入 backlog）**：~~①组织者把关层 Request-to-spot~~ ✅ ADR-0017 已落地；~~②waitlist 候补转正（满员进候补、退出按序/信用分补位，配合 release）~~ ✅ 本批落地（三身份浏览器全链路实测）~~③review 低分强制解释（≤3 星必填理由）~~ ✅ 本批落地（ReviewSection 低分拦截：≤3 星必填理由 + 红框警示 + 拦截零副作用，浏览器双身份全链路实测）~~④organizer 出勤档案视图（复用 no-show/violation 数据）~~ ✅ 本批落地（AttendancePanel 组织者视图，浏览器实测）~~⑤guest +1 携伴（携伴者实名登记对齐 ageGate/privacyNumber）~~ ✅ 本批落地（GuestSection 携伴登记 + ageGate 合规 + 电话脱敏 + 组织者携伴徽章，浏览器双身份实测）——**Meetup 吸收项 5/5 全部收官**
- [ ] **Meetup 裁决记录（2026-08-13）**：Meetup+ 付费解锁通讯/成员名单 = ⏸️ 现阶段不上 · 记录为潜在赢利点（若启用走宪法 §3 冲突上报拍板）；群 dues（组织者向成员收会费）= 🚫 C 端不抄，归 B 端/场地商家销售场景；静态兴趣搜索 = 🚫 落后不抄（类微信群，与 match.ts 即时撮合冲突）
- [x] **ADR-0006 O2O 万能底座蓝图定稿** ✅（本批完成文档层：RPG 设计哲学 → 六层防御圈 22 模块 + 本地现状映射（🟢8 / 🟡11 / 🔴7）+ 融合顺序定策 = 先蓝图定稿 → 再融合 web/mobile 按圈切分底座 → 再功能层迭代；后续融合期任务按圈/模块粒度排布）
- [x] **ADR-0007 底座融合执行（第一批）** ✅（本批完成：嫁接映射表定稿（web 83 lib 文件切割归属 + C1-C5 接口契约）→ **`src/base/` 共享层落地**：money 11（ledger/pay/deposit/bidding/customPricing/organizerSubscription）× trust 12（reputation/trust/starRank/review/violation/friends）× order 10（wave/booking/fulfilment/moduleFulfilment/dispute）× dispatch 4 × risk 6 × geo 8 × notify 4 × platform 22（含 p2p）× ai 23（含 gateway/voice 目录）→ 全量 git mv 保历史 + 调用方 import 全改；**`src/ammo/` 弹药属性表落地**（scene-template/prefs 迁入 + pricing-formula/dispatch-rule/risk-rule 新建，C3/C4/C5 契约兑现，新增类目只填表）→ 单测 303 全绿（+5 ammo）、tsc/lint 0 错、build 通过（8 API 路由正常）

## 七、支付模型定稿（2026-08-05，已落地，历史参考）

- 三总纲：不设余额门槛，但**必须付单上金额才能上线**；钱包=留存工具（退款/补偿原路或入钱包）；**钱不到位动作不生效**（拼位即付才算占位）
- 场景服务型（1:1）：PaySheet 付全款→ 响应者锁单（暂不收质保金，见验收模块）→ 达标放款
- 场景组局型（开放局）：发起人付自己份 → 拼位者付自己份即可占位；MVP 只做 A（即时全款），B（定金+限时补）预建模不开放 UI
- 信任规则：发布费独立不清退（×3 免费）；退款车道=opt-in PayOrder

---

## LAST_SYNC

| 日期 | HEAD | 摘要 |
|------|------|------|
| 2026-08-19 | `PENDING` | **第 4 枚官方标杆弹药 appliance-repair-v1 8D 全息化出厂（扩品实战 · 首枚 C3_TECH_B2B 技术资产弹药）**：① 新建 `src/ammo/appliance_repair.ammo.ts`——家电上门维修 8D 全息配置（D1 C3_TECH_B2B 双证书准入 [电工+家电维修，安全分 ≥70 + 公安核验] / D2 FORMULA ¥30 上门检测费·60min + 30~3000 元护栏 + 增项熔断 ≤50% + SKILL_LEVEL 折抵 ≤30% / D3 💥碰炸引信 / D4 GPS+水印降级 / D5 三算子复用 / D6 4 档违约阶梯 [现场扣 30 元检测费退剩余] / D7 0.82/0.13/0.05 守恒 + **48h 质保验收期** / D8 default 主题 + HousekeepingSlot + 家电表单 + 七连别名）经 `assembleAmmo` 静态审查出厂 + dispatchRule/sop 显式保留 + deepFreeze；② `src/ammo/registry.ts` 双键挂载（appliance_repair + APPLIANCE_REPAIR 同一产物引用）+ CATEGORY_TO_OFFICIAL 七中文别名直拨；③ `src/ammo/appliance_repair.ammo.test.ts` **16 例**（8D 契约/出厂 frozen/别名直拨/C3 准入三画像/碰炸引信/增项熔断 base 100 加价 50 放行·60 阻断/全流程/82·13·5 分账/违约阶梯）+ `package.json` test:oto:units 扩列（仅追加）；④ 文档——ARCHITECTURE_TAXONOMY §10.4 新增（8D 四弹对照表 + 修订记录）+ PROJECT_STATUS 达成表行 + 验证基线 1476→1492；门禁：tsc 0 错 + 单测 **1476→1492/1492 全绿**（vitest 606 不动 + node:test 870→886，突破 1490+ 预期）+ build exit 0 + 收敛门禁 exit 0（零 rename，无登记需求） |
| 2026-08-18 | `c91dd9a` | **E2E 12/12 全量实跑回归与 flake 根治（3 层座舱验收加固）**：① 入口缺口治本修复——首页重构移除 `SafetyKit` 渲染后「安全中心 + 平台治理后台」入口全局消失 → `ProfilePage.tsx` 挂回 `<SafetyKit />`（真实功能恢复，非仅测试绕路）；② 连跑 flake 根因——发布链路多一层「扣动扳机」后固定 400~800ms 断言时序余量不足（连跑高负载踩线）→ 7 脚本 12 处固定短等待全部轮询化（waitUntil 等 wave 落库/能力落盘 15s 上限：acceptance A/B/C、openmatch、wave、push、trust、trust-open 局 4）；③ match console error 过滤器补 Supabase Realtime WS 断连容忍（与既有云端 404 降级容忍口径一致）；④ trust-open 座位按钮定位修正——「标记未到场」按钮为 aria-label 承载且位于出勤档案折叠区（MATCHED 态默认折叠）→ 先展开 + aria-label 轮询；⑤ 实测：verify-prod 全量连跑 **12/12 PASS × 2 轮** + 独立计时轮 12/12 全绿（12 脚本逐条 8~39s，总 4.3min）+ 服务端 HTML 实证新版产物 + SW 旧缓存清理后渲染正常；门禁：tsc 0 错 + 单测 **1476/1476 全绿**（606+870 不动）+ build exit 0 + 收敛门禁 exit 0 |
| 2026-08-18 | `36cce3c` | **工作区临时截图清理与 Git 忽略规则加固**：物理删除根目录残留 `e2e-app-final.png`/`e2e-match-final.png`（346KB/281KB，未跟踪临时产物）；`.gitignore` 追加 `e2e-*.png` / `test-results/` / `playwright-report/`（`.e2e/` 原有规则保留），杜绝后续测试产物污染工作区；`git status` 工作树完全干净 + 与 origin/master 同步；门禁：tsc 0 错 + 单测 **1476/1476 全绿**（606+870 不动）；`public/` 静态资产零触碰（精确清理原则） |
| 2026-08-18 | `8b450eb` | **D-3 架构防腐战役：src/base/ 进家业务词汇彻底解耦与引信参数化**：① 底座复核零业务词——`sentinel.ts` 已纯注入（`isHomeAccess(category, keywords = [])`，未注入默认 `[]` 零加权，a11d85e 迁出后本战役复核确认）；② 弹药权威装配——`ammo/risk-rule.ts` 新增 `HOME_ACCESS_KEYWORDS_MAP` 权威类目映射（housekeeping/家政保洁/厨师·上门做饭/遛狗遛弯/水电维修/陪诊陪护/按摩推拿 7 键中英双键）+ `homeAccessKeywordsFor` MAP 命中优先/引信参数回落/`[]` 兜底（向后兼容）；`housekeeping.ammo.ts` 显式装配 `HOUSEKEEPING_HOME_ACCESS_KEYWORDS`（直取 MAP.housekeeping 供 sentinel ×1.2 引信联动）；③ 测试 +3（sentinel 2 / housekeeping 1）→ 单测 **1468/1468 全绿**（vitest 598 + node:test 870）+ tsc 0 错 + build exit 0 + 收敛门禁 exit 0；④ 白皮书 §5.2 D-3 🔴→🟢（进家词表 100% 闭合；decompose/llmEngine 意图识别词残余如实标注另域追踪）+ §十一 顺序勾销；⑤ **E2E 12/12 PASS**（重建 `.next` 后旧 `next start` SW 更新卡死导致脚本挂起 → `restart-prod.mjs` 重启为新构建 pid 8580 后全链通过） |
| 2026-08-17 | `a8e3f85` | **P2 战役·第三波终局攻坚：L2-M2 潮汐动态溢价与 L6-M2 区域运力熔断 —— 六层防御圈 26/26 🟢 全绿大满贯**：① `src/base/money/surge-pricing.ts` 新建——潮汐动态与环境溢价算子 `calculateDynamicSurgePrice`（时段潮汐：早高峰 7-9 ×1.15/晚高峰 17-20 ×1.20/深夜 22-5 ×1.30/其余 ×1.0（边界 7·9·17·20·22·5 精确锁定）；天气：暴雨 ×1.25/暴雪·风暴 ×1.40/中轻雨 ×1.10；供需：ratio>2.0 线性平滑 ×1.50 封顶 + L6-M2 capacitySurgeFactor 显式注入直连联动；三因子连乘 → 分单位取整 → D2 护栏 [minFloor,maxCeiling] 双向钳制（先地板后天花板上限恒守 + 负数账单保护），输出 ISurgePricingResult 含费用拆解 breakdown.clampedByBounds）；② `src/base/platform/capacity-circuit.ts` 新建——区域运力四级状态机 `evaluateRegionalCapacityCircuit`（NORMAL(util≤0.80)→ALLOW；CONGESTED(≤0.95)→×1.15 微溢价；EXHAUSTED_SURGE(>0.95 或排队>30)→×1.35 价格杠杆+排队蓄水；TRIPPED_THROTTLE(排队>100 或等待>1800s)→熔断阻断普通新需求强制排队，保护核心履约与 SOS；短路判定按严重度降序，0.80/0.95/30/100/1800s 边界全测锁定）+ `capacityLeverageFor` 联动 L2-M2 直传潮汐引擎；③ 门面接线——`customPricing.ts` 挂 surge-pricing 导出、`resilience.ts` 新增 `mapRegionalCapacityToDegradation` 桥接五级容灾（EXHAUSTED→RATE_LIMIT_QUEUE、TRIPPED→PRESERVE_CORE 仅放行 SOS 与在途履约，与既有五级矩阵实测零回归）；④ 测试 +22（surge-pricing 12：平价 1.0/四时段系数/六边界/天气五态/供需 1.0·1.25·1.5 封顶/复合 2.73/天花板封顶/地板保底/矛盾护栏上限恒守/负数保护/L6-M2 注入 1.35/确定性；capacity-circuit 10：利用率除零保护/NORMAL·80%·95% 边界/CONGESTED 1.15/EXHAUSTED 队列 31 触发/TRIPPED 101·1801s/边界 100·1800s/价格杠杆联动 13500 分/四级映射五级容灾+PRESERVE_CORE 矩阵/空区域/确定性）→ 单测 **1370→1392/1392 全绿**（vitest 578 不动、node 792→814）、tsc 0 错、build 待验、收敛门禁 exit 0；红线 1（溢价/状态机全入参化纯确定性实测）、红线 2（分单位 + 护栏闭区间守恒实测）、红线 3（money/platform 零 UI 反向依赖实测）满足；白皮书 §3.4.2 L2-M2/L6-M2 🟡→🟢，清单 **🟢26/🟡0/⚪️0 —— 六层防御圈 26 核心模块 100% 全绿大满贯** |
| 2026-08-17 | `1d557c0` | **P2 战役·第一波攻坚：L4-M4 危机干预协议与 L4-M5 隐私全周期遗忘闭环**：① `src/base/safe/crisis-tracker.ts` 新建——轨迹面包屑（`recordBreadcrumbPoint` 最近 N 处 FIFO 裁剪/非法坐标拒绝 + `detectTrajectoryAnomaly` ≥120km/h 超速漂移预警（Haversine 纯数学，120km/h 边界实测）+ `buildPoliceTrajectoryPayload` 压缩警方载荷 lat,lng,acc,ts 串）＋离线录音切片加密缓冲池 `AudioChunkBuffer`（sha256 完整性校验/FIFO 双上限逐出/drain 上抛）＋60s 危机升级状态机（`triggerCrisisEscalation`/`acknowledgeCrisisEscalation`/`advanceCrisisEscalation`/`resolveCrisisEscalation`：TRIGGERED(0s)➔ACKNOWLEDGED(≤30s，超窗 breach 不阻断)➔POLICE_ESCALATED(≥60s 未确认强升级，60s 边界实测)➔RESOLVED，跃迁带紧急通知载荷，EPA 映射对齐 crisis.ts）；② `src/base/safe/privacy-erasure.ts` 新建——《个保法》§47 密态销毁 `executeCryptoShredding`（姓名→ANON_USER_<hash>/手机号→138****xxxx/身份证→***/地址→***/邮箱→掩码/精确坐标→置空不可逆覆写；财务流水 order_no/amount_cents/split_plan_json/paid_at 依法保留零删除；`IShreddingCertificate` 含销毁时间戳/执行人签名指纹/64 位 hex 摘要确定性固化）+ `evaluateMediaRetention` 过期媒体清理调度（正常 90 天/争议 180 天边界精确 toPurge/toRetain）；③ 门面——crisis.ts/privacy.ts 附加导出零回归；④ 新建 `app/api/profile/delete/route.ts`（withAuth + contracts 两侧财务流水 → 密态销毁 → 匿名写回 + deleted_at + 证书返回）；⑤ 测试 +21（crisis-tracker 13 项 + privacy-erasure 8 项入白名单）→ 单测 **1328→1349/1349 全绿**（vitest 578 不动、node 750→771）、tsc 0 错、build 待验、收敛门禁 exit 0；红线 1/2/3 实测满足；白皮书 §3.4.2 L4-M4/L4-M5 🟡→🟢，清单 🟢22/🟡4/⚪️0 |
| 2026-08-17 | `5c40707` | **P2 战役·第二波攻坚：L4-M2 终端反欺诈与 L5-M2 外部合规生态闭环**：① `src/base/risk/anti-fraud.ts` 新建——`detectGpsSpoofing` GPS 瞬移/时空穿梭检测（Haversine 瞬时速度 >300km/h 或时间倒流 dt≤0 → TELEPORTATION_DETECTED 阻断 1.0；定位精度 0/绝对死值（恒定 ≤2m）→ MOCK_PROVIDER_DETECTED 阻断 0.85）+ `detectTerminalRisk` 终端模拟器/Headless 探针（webdriver +0.5/Headless UA +0.4/Emulator·Simulator +0.3/移动 UA 缺触控 +0.25，≥0.6 BLOCK、≥0.3 CHALLENGE_LIVENESS）；② `src/base/platform/compliance-ecosystem.ts` 新建——《电子签名法》§14《电子商务法》§52 电子合同 SHA-256 存证签章 `generateEContractSeal`/`verifyContractSeal`（规范化序列化 64 位 hex 摘要、1 字节篡改验签失败、法定声明）+ 场景微保险 `issueMicroInsurancePolicy`（保费=总额分×splitRules.insuranceRatio（家政 0.05/组局 0.02/陪玩 0.03，缺省 0.05）精确取整、保额类目映射家政 50,000 元/兜底 20,000 元、保单号 POL-YYYYMMDD-orderNoHash、30 天有效期、claimGateway 绑定）；③ 门面——`base/platform/signInsure.ts` 附加导出零回归；④ 测试 +21（anti-fraud 11 + compliance-ecosystem 10 入白名单）→ 单测 **1349→1370/1370 全绿**（vitest 578 不动、node 771→792）、tsc 0 错、build 待验、收敛门禁 exit 0；红线 1/2/3 实测满足；白皮书 §3.4.2 L4-M2/L5-M2 🟡→🟢，清单 🟢24/🟡2/⚪️0 |
| 2026-08-17 | `2f131b9` | **P2 战役：L4-M4 深度 AIGC 图像伪造检测闭环（六层防御圈收官攻坚）**：① `src/base/ai/forgery.ts` 强化为五信号融合鉴真引擎——`IImageForgeryReport` 结构化契约（isAuthentic/overallConfidence/riskLevel LOW·MEDIUM·HIGH·CRITICAL/signals/tamperFlags/summaryDiagnosis）+ `detectImageForgery`（信号 1 EXIF 时空一致性（拍照时间 vs 服务窗口 ±1h、GPS vs 围栏 Haversine 偏差）；信号 2 SHA-256 存证指纹（纯 TS FIPS-180-4 手写实现，`sha256Hex('abc')=ba7816bf…` 官方向量实测一致，篡改命中整体 ×0.5 确定性衰减）；信号 3 水印完整性（缺失/编码不符/伪铸）；信号 4 ELA 像素平滑度/拼接边缘伪影（纯数学 ≤2ms）；信号 5 5-provider Gateway 视觉深度鉴真（动态 import task: diagnose 失败静默回退中性 0.9 不干扰，红线 1）+ 加权融合 0.25/0.3/0.15/0.2/0.1 + `AIGC_PHOTO_FORGERY_DETECTED` 阻断常量；存量 checkForgery/checkTextEvidence/withLlmReview 原样保留（零回归）；② 新建 `src/app/api/ai/forgery/route.ts`——POST 接收 `{ imageSource, expectedSha256, actualSha256, expectedWatermark, exif, orderContext, ela }` 经 withAuth 返回 IImageForgeryReport；③ 业务接线——`src/ammo/housekeeping.ammo.ts` 领域钩子 `cleaningCheckHook` 升级（BEFORE+BLOCK：payload.photoVerify.riskLevel===CRITICAL → 返回 ok:false + AIGC_PHOTO_FORGERY_DETECTED 阻断验收；非 CRITICAL 附 forgery 透传；无 photoVerify 向后兼容原有语义）+ `ArbitrationSheet.tsx` 物证链照片槽位新增 🔬 AIGC 鉴真徽标（置信 %·风险标签四色 + tamperFlags 疑点标签，无报告不渲染向后兼容）；④ 测试矩阵——新建 `src/base/ai/forgery.test.ts` 18 项（真实合规五信号全通 LOW≥0.9 / GPS 严重偏差 EXIF_GPS_MISMATCH / 时间错乱 EXIF_TIME_MISMATCH / EXIF 全缺失 / SHA-256 篡改 HASH_TAMPERED / 无水印 WATERMARK_MISSING / 水印编码不符 / ELA 过度平滑 / 拼接边缘噪点 / 离线无 Key 纯规则兜底无异常 / 分级边界 MEDIUM·CRITICAL / 空白非法输入永不抛异常 / 三信号级单测 / sha256Hex 官方向量 / 存量兼容）+ housekeeping.ammo.test.ts +3（CRITICAL 阻断 BLOCK / 非 CRITICAL 放行透传 / 无载荷向后兼容）+ ArbitrationSheet.test.tsx +3（徽标渲染/可信绿色/无报告兼容）→ 全部入白名单；⑤ 白皮书 §3.4.2 L3-M4 🟡→🟢（标注五信号落点 + 测试凭据 + 3.4.2 战果注记：本轮 L6-M3/L5-M1/L3-M4 三模块晋级，清单 🟢20/🟡6/⚪️0，28 口径差量与 🟡 模块如实标注为后续战役，不虚报大满贯）→ 全量单测 **1304→1328/1328 全绿**（vitest 575→578、node 729→750）、tsc 0 错、build 待验、收敛门禁 exit 0；红线 1（离线 100% 确定性判定不抛异常）+ 红线 3（纯函数引擎零 UI 反向依赖）实测满足 |
| 2026-08-17 | `7517bfe` | **P2 战役：L5-M1 多通道适配器与多厂商毫秒级动态热备总线闭环**：① 新建 `src/base/platform/multi-channel-gateway.ts`——通用故障转移调度器 `executeWithFallback`（按优先级探测，熔断通道跳过，Promise.race 超时控制，成功复位/失败计数，所有外部厂商全挂时执行 LOCAL_MOCK 确定性兜底）+ 三态健康机 `ChannelHealthStatus`（HEALTHY/DEGRADED/UNHEALTHY：连续失败 3 次熔断 → 冷却 60s → 半开探测放行一次 → 成功自愈/失败重新计时）+ 按 `channelKey::vendor` 独立熔断状态池（厂商级隔离，互不污染）+ `resetChannelCircuit`/`getChannelCircuit`/`listChannelCircuits` 治理接口；② 短信热备门面 `dispatchSmsWithFallback`（阿里云 ➔ 腾讯云 ➔ 华为云 ➔ 本地 Mock 存根，无凭证快速失败平滑下跳）+ LBS 距离热备门面 `calculateDistanceWithFallback`（MapLibre/OpenFreeMap ➔ 高德 WebService ➔ 腾讯地图 ➔ 本地 Haversine 纯数学兜底）；③ 底座接线：`src/lib/notification-ladder.ts` 的 rungSms 升级为多通道热备（SMS_PROVIDER/mock 分支收敛进总线，LOCAL_MOCK 时站内日志存根）+ `src/base/geo/geofence-watcher.ts` 新增异步热备入口 `checkGeofenceArrivalViaHotSwap`（距离经热备总线计算，外部全挂回落本地 Haversine，判定口径与同步入口一致，含 usedVendor/fallbackHops 治理字段）；④ 测试矩阵：新建 `multi-channel-gateway.test.ts` 17 项（首选直通 fallbackHops===0 / 一级失败降级 ===1 / 连续两级 ===2 / 全挂本地兜底无异常 / 超时降级 / 三连败熔断 / 冷却内跳过 / 冷却期满半开探测自愈 / 探测失败重新熔断 / 状态池按 key 隔离 / reset 清理 / SMS·LBS 门面全链路 / 乱序优先级排序）+ geofence-watcher.test.ts +2（外部全挂回落本地判定口径一致 + 精度漂移联动）→ 全部入 `test:oto:units` 白名单；⑤ 白皮书 §3.4.2 L5-M1 🟡→🟢 100% 已闭环（标注热备总线落点 + 测试凭据）→ 全量单测 **1285→1304/1304 全绿**（vitest 575、node 710→729）、tsc 0 错、build exit 0、收敛门禁 exit 0；红线 1（全挂确定性兜底不抛异常）+ 红线 3（platform 底座零 React/UI 反向依赖）实测满足 |
| 2026-08-17 | `7a45b84` | **P2 战役：L3-M5 对话式数据 BI 引擎与动态看板闭环**：① `src/base/ai/bi.ts` 强化——新增 `IBiReportPayload` 标准报表契约 + `BiRawDataContext` 行级上下文 + `parseConversationalBiQuery`（async，5-provider Gateway LLM 归因增强）与 `parseConversationalBiQuerySync`（纯确定性）双入口：意图分流四路（品类违约→BAR / 资金走势→LINE / 服务者履约→TABLE / 兜底概览→TABLE）＋近 N 天时间范围提取＋指标卡（含 trend/changePercent 环比）＋`suggestedFollowUps` 追问引导；LLM 增强经动态 import `gateway/engine.ts`（task: diagnose）失败静默降级回规则摘要，node:test 离线环境天然走兜底（红线 1）；佣金 15%/保险 5% 估算口径在 summary 显式标注；既有 parseBiQuery/runBi 兼容保留；② 新建 `src/app/api/admin/bi/route.ts`（withAuth + ADMIN 校验 + contracts/disputes 组装行级上下文，query ≤500 校验）；③ 新建 `src/components/admin/ConversationalBiView.tsx` 对话看板（输入框 + 3 快捷气泡 + 诊断卡 + KPI 网格趋势箭头 + 零依赖 SVG/CSS 图表 BAR/LINE/PIE/TABLE + 追问 chips）+ 挂载 `src/app/admin/dashboard/page.tsx`；④ 测试——新建 `src/base/ai/bi.test.ts`（node:test 8 例：意图分流/时间范围/三类聚合数学守恒（违约 2 单/订单 6/GMV·佣金 15% 口径可复现）/兜底/空数据/async 离线兜底结构）入 `test:oto:units` 白名单 + 新建 `ConversationalBiView.test.tsx`（vitest jsdom 7 例：渲染/快捷气泡全链路/表单提交/LINE/PIE/TABLE/追问重查/API 失败态）；⑤ 白皮书 §3.4.2 L3-M5 🟡→🟢 100% 已闭环（标注文件落点 + 测试凭据）→ 全量单测 **1251→1266/1266 全绿**（vitest 562→569、node 689→697）、tsc 0 错、build exit 0、收敛门禁 exit 0 |
| 2026-08-17 | `9e23bb3` | **P1 攻坚战役·步骤二：旧垂直协议旧轨完全收敛与物理删除**：① `registry.ts` 重构为 **ammo 投影适配器**——删除对 `./protocols/*` 三个旧文件的引用，改从 `src/ammo/`（`OFFICIAL_AMMO` 直挂三枚官方弹药）投影标准 `ProtocolDef`：动态数值全取 ammo 八维配置（D7 `splitRules` → `funding.fees.platform_commission`（1-providerRatio，浮点洁净 round 1e6）/ `autoAcceptanceTimeoutHours` → `completion.autoTimeoutSeconds` 与 `funding.autoReleaseTimeout` / D6 `cancellationTiers` → `refundRules`（demanderRefundRatio → providerRatio、车马费 → providerMax，缺档由 engine.calcRefund 较低档兜底）/ D4 `requiredSensors` → evidence / 派单硬门槛 `requiresVerified` → classificationKeywords），静态行业语义（roles/资金状态机/评价维度/争议通道/guards）为适配器合表，模块级投影常量（HOUSEKEEPING_REFUND_RULES / MEETUP_REFUND_RULES / HOUSEKEEPING_EVIDENCE / MEETUP_EVIDENCE）直引 ammo 文件；② 三协议 id 兼容——`protocol_housekeeping` ← housekeeping-v1、`protocol_dating` ← companion-v1（工厂测试锁定 dating→companion-v1）、新增 `protocol_meetup` ← meetup-social-v1（来源 dating.ts 双押金 AA 保障金资产）；`protocol_base` 内联 `BASE_PROTOCOL_DEF` 仅作继承父级（与旧 registerBase 同行为，不对外注册）；`protocolRegistry` 接口零变化，**新增导出 `PROTOCOLS` 字典 + `getProtocol(id)`**（任务书契约，旧签名兼容）；③ `git rm` 物理删除 `protocols/base.ts`（83 行）+ `housekeeping.ts`（250 行）+ `dating.ts`（202 行）共 535 行旧码 + 空目录清理；④ 新增 `src/lib/protocol/registry.test.ts` 4 例契约锁定（三协议注册/base 不注册/PROTOCOLS·getProtocol/7 态 17 转换/分账 0.15/refundRules 6 档/stages 6 态/commitment 模式/dating 2h·meetup 6h 超时）→ 单测 **1247→1251/1251 全绿**（vitest 558→562）、tsc 0 错、build exit 0、收敛门禁 exit 0；台账 `DEBT_CLEANUP_REGISTER.md` §4.2 P1-P3 全部勾销「🗑 已出清」+ Batch 4 418 项勾选 |
| 2026-08-17 | `09d07be` | **方案 A：OTO 专属空间毛玻璃登录抽屉（AuthSheet，与步骤一地图归一合并提交）**：① 新建 `src/components/oto-ui/auth/AuthSheet.tsx` 半屏毛玻璃抽屉（consumes `.oto-app` tokens `--color-surface-subtle` + glass 拟态 + 极光渐变标题 + 48px 触控靶区）——三通道登录（📱 手机号验证码演示固定码 1234 / ✨ 一键演示角色雇主·服务者·组局主理人 / 🛡️ Web3 钱包模拟连接脱敏地址）+ `useDragToDismiss` 下拉 >35% 平滑收起 + 遮罩收起 + 登录态持久化 `oto-auth-account` + `oto:auth-changed` 事件广播；② 前台接电——`(oto)/page.tsx` 根挂载（`oto:auth-open` 全局事件呼出，五屏零整页跳出），`ProfilePage` 顶部访客卡「登录 · 注册 / 切换账号」入口 + 登录态即时刷新；③ 老 `/login` 保留后台备用通道；④ 实测中修复「切换账号」缺陷（清会话回演示表单）并补单测锁定；⑤ 单测 +12 项（`AuthSheet.test.tsx`）→ 单测 **1235→1247/1247 全绿**（vitest 546→558）、tsc 0 错、build exit 0、收敛门禁 exit 0 |
| 2026-08-17 | `e5e44d3` | **D8 视界与动态前端插槽深度装配**：① 新建 `src/components/waves/slots/DynamicAmmoSlot.tsx` 通用无头动态履约插槽（bizParams 参数快照 / WATERMARK_CAMERA Before·After 打卡区 / fuzePolicy 引信徽标 / 申请调解申诉，自包含 CSS + 44px 触控 + dyn-* 弹药 theme 微氛围主题类，零品类硬编码）；② `FulfillmentCockpit.tsx` 分发器升级——`CockpitScenario` 增第四态 `dynamic`（SCENARIO_THEME_META.dynamic + describeCompletionCta dynamic 分支）+ `dynamic?: DynamicAmmoSlotProps` 透传 prop（未提供时优雅降级不白屏）；③ `FulfillmentCenter.tsx`——`resolveCockpitScenario` 升级弹药声明 `cockpitSlot` 优先分发（COCKPIT_SLOT_SCENARIO 映射：DRONE_CROP_SPRAY → 家政插槽；未命中制式回落 dynamic）+ 动态插槽装配透传（ammo/bizParams/证据照片/上传与申诉接线）；④ `DynamicDraftCard.tsx`——新增 `describeFormSchemaFields` 纯函数（formSchema.fields → string/number/enum 类型归一 + options + required + defaultValue 投影）+ `resolveDraftThemeClass`（弹药 theme → draft-* 主题类，缺省 default 回落），草稿卡渲染可点击微调扩展字段行；⑤ 单测增补 9 项（FulfillmentCockpit +5 / DynamicDraftCard +4），全量单测 **1226→1235/1235 全绿**（vitest 537→546）、tsc 0 错、build exit 0、收敛门禁 exit 0；全程零 rename（新增 1 + 修改 5，无既有断言改动） |
| 2026-08-17 | `09d07be` | **P1 攻坚战役·步骤一：地图技术栈全仓归一（Leaflet 彻底出清，Batch 4 C15）**：① 履约页迁移——`src/app/provider/orders/[id]/OrderFulfillmentClient.tsx` 地图由 Leaflet `MapComponent`（OSM 瓦片单标记）切换为 MapLibre 3D `MapView`：动态 import 改为 MapView（loading Skeleton 保留），目标点以 `focus` 锚定（`targetPoint = {lat: demand.latitude, lng: demand.longitude}`）+ 单点 hot=1 光晕 dots（`target-${demand.id}`，category 履约目标）；② `src/components/waves/MapView.tsx` 契约增补（宪法 #2 只增补）——可选 `focus?: GeoPoint` 位 + `focusRef`（初始化相机 center/zoom 15 直达目标点，缺省回落 MAP_CENTER/MAP_ZOOM 全城视角，既有雷达 feed 调用零影响）；③ `git rm src/components/MapComponent.tsx` 物理删除 + `npm uninstall leaflet react-leaflet @types/leaflet` 三依赖出清（package.json/lock 同步，全仓 grep 零 Leaflet 引用残留）；④ 台账 `DEBT_CLEANUP_REGISTER.md` 资产表 MapComponent 行标 🗑 已出清、C15 行勾销「已收敛归一」、Batch 4 清单 419 项勾选；门禁：tsc 0 错 + 1235/1235 全绿（基线不动）+ build exit 0 + 收敛门禁 exit 0 |
| 2026-08-17 | `a3ca5e6` | **长尾非标业务即时量产大考实证**：全新类目 DRONE_CROP_SPRAY（农田无人机植保）在严禁静态 `.ammo.ts` 业务文件前提下，仅凭口语化非标诉求纯运行时组装 8 维全息配置 → AmmoFactory Linter 审查出厂 + 热注入 DYNAMIC_AMMO_POOL → AmmoRunner 五态全链路（CAS 0→4 / 托管 / 增项熔断 / 双拍 / 微信收付通 85·10·5 守恒）+ 零静态文件实证；新增 `src/ammo/dynamic-production-exam.test.ts` 考卷 8 项、`package.json` test:oto:units 扩列（历史「测试清单扩列」惯例）→ 单测 **1217→1225/1225 全绿**、tsc 0 错、build exit 0、收敛门禁 exit 0；全程零 factory/registry 改动 |
| 2026-08-07 | `2748a36` | 建立状态档案体系（PROJECT_STATUS.md 替代 NEXT_STEPS，AGENTS.md 挂 sync 规则），已推送 origin |
| 2026-08-07 | `1f118be` | CI 补挂 trustopen/acceptance（11 条 E2E）+ 生产服务器启动验证（pid 15900，HTTP 200） |
| 2026-08-07 | `bdfda92` | 调整 P3 路线：数据化先行（lat/lng + 真实成交）→ Leaflet+OSM 免费接图，保留 CSS 降级 |
| 2026-08-07 | `ae10c5d` | P5 实测验证通过：deviceMemory 沉浸降级 + 离线全流程 5 屏 + lounge.glb 预缓存命中 → P5 标 ✅ |
| 2026-08-07 | `356d794` | 本地批次四件套：P3 数据化前置(geo.ts) + S1 匿名热力图 + 场景模板 ×4 + 拼位裂变 ShareKit（防自刷计数）→ 单测 153 绿，浏览器实测通过 |
| 2026-08-08 | `1cb0408` | S2 AI 主动诊断（无人响应 ≥2min 实时建议，Zhipu 真降级实测）+ S3 关系沉淀（72h 自动撤回转友状态机，双 tab 全链路实测）+ 修复两缺陷：DiagnosisCard 建议列表 key 兜底、friendRequests union 合并墓碑化（删除跨 tab 落盘）→ 单测 175 绿 |
| 2026-08-09 | `11d141d` | ADR-0004 落档：P3 地图选型定为 MapLibre GL JS + OpenFreeMap（免费无 Key，初期 3D 透视层次 + 点渲染，游戏化后置留口，瓦片源可切换） |
| 2026-08-09 | `f11e8cb` | P3 地图首批落地：MapView（MapLibre+OpenFreeMap 动态引入、pitch 25°、建筑立体、wave 光点、点击 FlyTo）+ mapConfig 纯函数层（tier/点数据）+ 低配降级 → 单测 181 绿（+6），tsc/lint 全绿，浏览器实测瓦片加载、3D 面板渲染 |
| 2026-08-09 | `9b993cb` | fix: tier 探针改用 useSyncExternalStore（server 快照=false）消除 SSR/客户端不一致 hydration 错误；mapConfig.test 纳入 test:units → 单测 181 绿，浏览器 CSS 降级 + 3D 双分支实测无报错 |
| 2026-08-09 | `068a4cb` | 纯本地批次①（地图观感）：地图 3D/CSS 手动切换（mapPref localStorage 持久，SSR 安全 useSyncExternalStore）+ 冷启动氛围 POI 密度层（3D 灰点 source + CSS 18 点）→ 单测 184 绿，浏览器实测循环切换/持久/无错 |
| 2026-08-09 | `8d9a401` | 纯本地批次②（商业化前哨）：组局者订阅状态机（organizerSubscription 纯函数 + zustand persist + 两段式模拟收银台 OrganizerBoostCard）→ 单测 189 绿（+5），浏览器实测开通→生效→刷新持久、无 hydration 报错 |
| 2026-08-09 | `e3400df` | 纯本地批次③（商业化前哨）：公开竞价沙盒（bidding 纯函数状态机：保留价/覆盖出价/低价排序/8% 佣金结算包 + BiddingSandboxCard 演示完整闭环）→ 单测 199 绿（+10），浏览器实测出价¥61→中标→佣金¥4.88→净得¥56.12 |
| 2026-08-09 | `6871005` | 纯本地批次④（商业化前哨）：账号漫游 + 多开风控（roamGuard 设备指纹矩阵纯函数：1 安全 / 2 家庭共机关注 / ≥3 冻结建议 + useRoamStore persist + SafetyKit 内 RoamGuardPanel 演示）→ 单测 207 绿（+8），浏览器实测多开升级/漫游回落/刷新持久、无报错 |
| 2026-08-09 | `dc22594` | 收尾①雷达局收藏：favorites 心形 + FavoritesSheet「我关注的局」面板（store persist + p2p transport 对齐）→ 单测 207 保持绿，浏览器实测收藏/计数/面板/移除 |
| 2026-08-09 | `05ec324` | 收尾②扫码识别：ScanMockSheet（模拟相机横线扫描 → 识别分享的开放局 → 加入拼位直达 /?wave&via=scan）挂接 SearchBar onScan → tsc/lint 绿，浏览器实测扫→识别→加入拼位 |
| 2026-08-09 | `d1ce09b` + 本档 | 收尾③模拟上线：e2e-offline（生产 SW shell 五屏兜底 + 恢复在线）实测 PASS；verify-prod 编排（build → 生产 start → 12 条 E2E，一键演练）；上线前缺口清单落档 `docs/LAUNCH-GAP.md`（G 本地可做 4 / D 依赖数据化 5 / E 外部 4）⇒ 本地侧已全部闭环 |
| 2026-08-09 | `cbfdd36` | G-1/G-2 目的地中心：筛选抽屉（价位档 / 仅 AR / 评分 / 价格↑↓ / 离我最近）+ 全部列表；HoloCard 卡片抽为共享组件；destFilter 纯函数层 → 单测 217 绿（+10），浏览器实测档位过滤与升降序 |
| 2026-08-09 | `254a9fd` | G-3 通知中心：铃铛 + 未读角标 + 聚合动态（报价/正式接单/雷达推送/好友/举报回执），已读本地持久化（notify 纯函数 8 单测）→ 单测 225 全绿，浏览器实测开合/空态 |
| 2026-08-09 | `b08f406` + 本档 | G-4 数据源徽章 + PWA 安装引导（本地沙盒/离线双态 + beforeinstallprompt 一键安装实测可用）→ LAUNCH-GAP G 组 4 项全部清零；本地无占位缺口，D 组（数据化）待命 |
| 2026-08-09 | `f3c1dc5` + 本档 | G-5 × 商业化真实化：竞价卡接入「我发出的真实开放局」（保留价=局预算、种子报价跟随、真实需求局徽章）；组局加速订阅 → 雷达优先曝光实时横幅；我的屏访客引导（本地模式说明 + 数据模式弹层 + 回雷达）→ 浏览器实测保留价¥100 报价 100/108/115；单测 225 全绿 |
| 2026-08-09 | `77d2a35` + 本档 | 本地数据自主权三件套：① 快照导出/导入（lib/snapshot 纯函数 +6 单测 + DataPortCard 个人中心，实测 8 键往返 reload 恢复）；② 地图光点详情（CSS/3D 双端点击信号点 → 局详情卡：时间/地点/¥/名额/关注/复制分享直达；MapView 增 onDotClick）；③ 演示座舱（需求方/服务者/多开风控三剧本 + 复位，store 既有 API 零数据爆炸）→ 单测 231 全绿；纯本地可做项至此全部清零，主线仅剩 D 组数据化 |
| 2026-08-10 | `c4899f3`+ 已推 | 本地收尾双件：① 真机扫码（ScanMockSheet 接 getUserMedia + jsQR 逐帧解码，无摄像头自动降级模拟，jsqr@1.4 依赖；scan.ts parseWaveUrl +7 单测，浏览器实测发布→扫（降级）→识别局→/?wave&via=scan 跳转）；② 撮合偏好编辑（prefs.ts 四维池纯函数 +7 单测 + usePrefStore persist + ProfilePage 点击循环/重置，实测切换/重载持久/重置闭环）→ 单测 245 全绿；LAUNCH-GAP D 组「真机扫码」移入已闭环（本地零服务端依赖），D 组余 4 项待数据化 |
| 2026-08-10 | 已推（ee4ea35） | 存量 lint error 修复：NotificationCenter.tsx setState-in-effect（`react-hooks/set-state-in-effect`）→ 已读集合改为 readKeys.ts 外部 store（useSyncExternalStore，同 mapPref 同构模式：server 快照空集、subscribe 时 warm 存储值，无水合不一致）+ markAllRead → lint 0 errors、tsc/245 单测绿、浏览器面板开合/空态无报错 |
| 2026-08-10 | 已推（3e84982） | 本地化新三件：① 分享真二维码（ShareKit 伪码→qrcode canvas 真码；qr.test 闭环 qrcode→pngjs→jsQR 还原链接 +4）② 本地系统通知（systemNotify 五类事件 diff +8、NotificationCenter 挂载授权按钮与跨帧 diff→notify）③ 本地上传头像（IdentityAvatar 图片/emoji 兜底 + avatar.ts 96×96 压缩 + 三处展示 + persist）。附：FloatingDock `bottom-[calc(env(…))]` 触发 tailwind v4 arbitrary 解析 bug（仅全新编译暴露，旧缓存掩盖）→ 改为自定义 CSS 类 `o-safe-bottom/o-safe-pb`（globals.css 手写 safe-area 工具，与既有 .pb-safe 同范式）→ build 成功、prod 模式实测正常。单测 257 全绿（+12），tsc/lint（src）0 错；浏览器实测（prod 模式）：真码 canvas 224px/PNG 5.2KB/黑占比 45.7%、系统通知按钮存在、头像上传→JPEG 压缩→reload 持久→首页+雷达头+个人中心三处同步 |
| 2026-08-11 | `21c4e0e` | LLM Gateway（ADR-0005）全部落地：provider 表单一来源 + llmGuard per-provider 配额 + /api/gateway 聚合入口 + **五路由全薄层化（cluster/decompose/diagnose 手写链收敛，completeText 非流式统一链/超时/降级）** + 修复存量孤儿依赖（qrcode/jsqr/pngjs 补声明+@types）+ TTS edge-tts 兜底 → 单测 287 全绿，tsc/lint 0 错，六端点实测全 200（cluster/decompose/diagnose source=zhipu 真 LLM） |
| 2026-08-11 | `d356f02` | 语音闭环三件套（L1 输入/输出 + L2 意图 + 留证）：① `lib/voice/` 纯函数层（audioStore IndexedDB 留存 + queryClips/summarizeEvidence 取证；voiceIntent 结构化意图校验/关键词 mock 降级/播报文案；asrClient GLM-ASR→Web Speech 降级；ttsClient GLM-TTS→speechSynthesis 降级 + 文本哈希 IndexedDB 缓存）② `/api/asr`+`/api/tts`（智谱音频代理，无 key 503，不留服务端缓存）+ `/api/voice-intent`（zhipu→gemini 结构化 JSON，围栏容错）③ VoiceBar（按住说话→MediaRecorder→ASR→语音留证）、ChatPage 语音入口（L2 意图→发布局走既有确认卡支付闭环/查局势/对话 + 回复自动播报 TTS 开关 + 气泡重播）→ 单测 275 全绿（+18），tsc/lint 0 错（src），build 通过；浏览器实测：voice-intent 真 LLM 识别 publish-wave 字段全对齐、IDB 留存冒烟、无麦克风降级 Web Speech 错误提示不崩溃；本机无麦克风+智谱 TTS 无余额（429）故录音真链/播报出声留待真机验证 |
| 2026-08-12 | `8b8cfc4` | UI/UX 打磨九件套（`docs/UI-UX-BACKLOG.md` 建档全清零）：P1 ①发布 CTA 主视觉（渐变+光晕+入场）②竞价卡虚线玻璃+「演示沙盒·无真实资金」降权 ③空态三步引导（localStorage 记忆）；P2 ④AI 屏语音入口气泡（首次+持久）⑤行程屏真实 bookings 优先入时间线+副标题动态 ⑥头部昵称层级+状态圆点；P3 ⑦Dock layoutId 光晕滑块 ⑧竞价按钮 min-h-10 触控 ⑨全局 Toast（lib/toast zustand + ToastHost layout 挂载 + 发布/拼位接入）→ tsc/lint 0 错、单测 287 保持绿；浏览器实测：CTA 渐变/引导条/竞价卡降权/语音气泡/localStorage 持久/Dock 光晕/发布成功 toast（MutationObserver 捕获「需求已上线·正在雷达广播」）| |
| 2026-08-12 | `34d55b6` | UI/UX 第二批 8 项（`docs/UI-UX-BACKLOG.md` 重写为第二批清单 + React #418 根因落档）：P1 ①触控尺寸全面达标 ②hydration 修复（根因：useState 直读 localStorage 水合不一致 → `lib/clientFlags.ts` useSyncExternalStore 同构 server 快照恒 false + subscribe warm；顺带修 readKeys.ts getServerSnapshot 未缓存 warning→常量 EMPTY）③在线切换 toast + 文案去重；P2 ④行程屏英文残留中文化（otoActivities.location Maldives→马尔代夫/Bali→巴厘岛 4 处）⑤AR 取景框角标 AR VIEWFINDER→「AR 取景框」⑥发布弹层分组（核心表单常显 +「更多选项」折叠：定制/磋商留言/AI 拆解/开放局/鸽子险/有效期/开始时间/配额）；P3 ⑦行程屏导航按钮 ⑧发布品类 chips min-h-8 → tsc/lint 0 错、单测 287 绿、浏览器实测：行程屏/AR 指南全中文、发布弹层折叠展开完整、0 console error、无触控不达标按钮 | |
| 2026-08-12 | `b08afc5` | **P8 商业化本地全流程闭环（1/2）：多开风控阻断业务 + 裂变回报入通知 + 竞价结算写回真实局**——① `createPendingWave` 高危闸门（同设备 ≥3 身份 `blocked:"roam"`）+ PublishSheet 拒检提示；`simulateMultiOpen` 递增序号身份（连点 2 次即 high）+ RoamGuardPanel 高危生效提示 ② `fissionStamp`（真实增量才刷时间戳）+ Wave.fissionUpdatedAt + useWaveStore 三处换用 + systemNotify diff「邀请裂变 +1」+ notify/NotificationCenter 🪃 条目 ③ Wave.biddingSettled + settleBidding + BiddingSandboxCard 开标真写回 + MyWaves 结算徽章 → 单测 292 全绿（+5）、tsc/lint 0 错；浏览器实测：多开高危→发布被拒、真实局开标→「微笑保洁中标 ¥100 · 佣金 ¥8 · 净得 ¥92」、另一身份拼位→发起人通知中心「🪃 邀请裂变 +1」 | |
| 2026-08-12 | `824e00e` | **P8 商业化本地闭环（2/2）：钱包账本统一 + 漫游落盘 + 治理可见 + 响应方收益**——① `lib/ledger.ts`（applyLedger 出账不下穿 0 / makeLedgerEntry 防撞 id，+6 单测）+ useIdentityStore.book 通用入账（kind 扩 commission/subscription/income）② BiddingSandboxCard 开标按成交价 8% 记「竞价服务费」（幂等 note 含局 id）③ OrganizerBoostCard 开通/续费扣 ¥9.9 「组局加速订阅」④ WorkerWorkbench 完成订单按 service 金额入账「服务收益」（响应方商业化）⑤ 漫游过滤掉 persist 补 events（刷新不丢）+ AdminPanel 新增「漫游安全监控」区块（风险等级 + ⚠ 事件流）⑥ WalletView 流水改正负分色（入绿 +¥ / 出红 −¥，修旧统一 −¥ 缺陷）→ 单测 298 全绿（+6）、tsc/lint 0 错；浏览器实测：开标→账面 −¥8（余额 92）→开通订阅 −¥9.9（82.1）→工作台完成 2 单 +¥499/+¥80（661.1）、流水正负分色、漫游事件 3 条刷新生效 + 治理后台高危监控展示、复位后方复安全；生产 build + restart-prod（PID 22156）实测订阅入账通过 | |
| 2026-08-13 | 已推（7754884 链） | **ADR-0006 定稿（文档层）**：O2O 万能底座六层防御圈蓝图（RPG 设计哲学 → 22 模块分层：①触达 ②业务核心 ③AI 神经 ④生态网关 ⑤安全风控 ⑥生存基建）+ 本地现状映射（🟢8 已实现可复用 / 🟡11 需抽象 / 🔴7 未实现）+ 融合顺序定策（蓝图定稿 → 融合 web/mobile 按圈切分底座 → 功能层迭代）+ 阶段建议（MVP 融二圈/A 轮砸三圈五圈/B 轮补六圈）→ 详见 `docs/adr/0006-universal-base.md` | |
| 2026-08-13 | 已推（7754884） | **ADR-0007 底座融合执行（第一批落地）**：嫁接映射表 + C1-C5 接口契约定稿 → `src/base/` 共享层全量落地（money/trust/order/dispatch/risk/geo/notify/platform/ai 九域 100 文件 git mv 保历史 + 调用方 import 全改 + 测试路径同步）→ `src/ammo/` 弹药属性表（scene-template/prefs 迁入 + pricing-formula/dispatch-rule/risk-rule 新建 + ammo.test +5）→ 单测 303 全绿、tsc/lint 0 错、build 通过；lib/ 仅剩业务保留（mockData/mockResponders/chat/dial/scan/qr）→ 详见 `docs/adr/0007-base-merge-map.md` | |
| 2026-08-13 | 已推（e19f3d8） | **设计宪法定稿（最高指导思想固化）**：`docs/DESIGN_CONSTITUTION.md` 落档（哲学出处 + 10 条文可裁定判据 + §2 效力分层 + §3 冲突上报用户拍板禁静默 + §4 ADR 模板两栏 + 修订记录）；根 `AGENTS.md` 与 `oto-spatial-web/AGENTS.md` 各挂 `<BEGIN:design-constitution>` 指针节（改前备份 .bak-20260813）；新增 `docs/adr/TEMPLATE.md`（六圈定位声明 + 宪法条文对照强制字段）；ADR-0006/0007 标注「派生自宪法 #n」→ 后续新设计/新 ADR 必须先读宪法、冲突必上报由用户裁决 | |
| 2026-08-13 | 已推（c74b20f） | **ADR-0007 v2 契约全兑现（执行手册收口）**：v2 重写为执行手册（§0 范围防发散 + §1 现状盘点 diff G1-G5 + §2 C1-C5 契约 + §3 Phase 0-5 + §5 缺口清单 N1-N16）；Phase 1 C1 OrderCore（`base/order/orderCore.ts` 投影桥 OrderStatus↔WaveStatus，Wave 语义零改动、22 调用方零回归，+5 单测）→ Phase 2 ammo/sop.ts 补建 + dispatch-rule hardGates 结构化对齐（+1 单测）→ Phase 3 e2e「填表即新弹药」：遛狗遛弯四表登记（pricing/dispatch/risk/sop）全读 + base 引擎消费 + 零 base 修改（+5 单测）→ Phase 4 mobile 归属登记（location→base/geo RN 候选/api→家政弹药/types→弹药类型/DynamicForm→N2；mobile tsc 存量 slider 依赖错单独排期）→ Phase 5 总验收：单测 **314 全绿**（303+11）、tsc/lint 0 错、build 通过、生产模式浏览器冒烟 0 console error；C1-C5 全部有落地 anchor 文件 | |
| 2026-08-13 | 已推（5cb0d44） | **ADR-0008 智能争议小法官落地（缺口 N5 关闭）**：纯函数层 `src/base/ai/judge.ts`（证据硬伤→责任升档 / 反驳补救→降档 / 承诺落空→升档；全责 100%、demander 0%、partial/shared 钳制到原因上限；+9 单测）；`src/app/api/judge/route.ts`（judge task 注册进 Gateway 增补 providers 链 gemini/zhipu/groq/openrouter，prompt 内嵌原因赔付上限防 LLM 拍脑袋，解析失败/毒丸/超时回落到规则引擎 source=mock）；`components/waves/JudgePanel.tsx` 挂载争议视图（工作人员反驳输入→小法官判定→建议赔付 ¥+话术+置信度→一键采纳走 settleDispute 协商结算闭环）；API 双场景实测：全责无反驳 ¥200→¥200 100%、反驳补救→partial 36% ¥36；单测 314→**323 全绿**、tsc/lint 0 错、build 通过、prod 冒烟 0 console error | |
| 2026-08-13 | 已推（004bb23） | **ADR-0009 多因子反欺诈探针落地（缺口 N9 关闭）**：`src/base/risk/sentinel.ts` 纯函数聚合探针——四路信号（设备多开 roamGuard / 信用·新号大额 / 行为·高频低完成 / 图·多身份裂变）+ 加权归一 × 引信（进家类目 ×1.2）+ 宪法 #9 高危单因子不被稀释兜底（max(加权, 最高因子分)，实测中发现 80 分被 40 分拉低到 64→放行的稀释 bug 并修复）+ 宪法 #10 缺数据剔除重归一，+9 单测；`useWaveStore.createPendingWave` 发布闸门接入（替代原 roam 单点拦截）：high → `blocked:"sentinel"` 冷拒 + sentinelEvents 事件流（跨 tab transport union 合并），watch → 放行留痕；PublishSheet 拒检提示；AdminPanel 新增 SentinelDashboard（分数环 + 四因子触发条 + 事件流时间线）；浏览器实测：正常发布放行→安全中心模拟多开至 3 身份→再发布被「反欺诈探针甄检到高危信号」拒绝、治理后台仪表盘「拒绝发布：设备多开、关联图谱」双因子触发；单测 323→**332 全绿**、tsc/lint 0 错、build 通过、prod 冒烟 0 console error | |
| 2026-08-13 | 已推（4da1c5e） | **§5 缺口 N1-N16 全部关闭（功能层收口）**：八张 ADR（0010-0015 + 0008/0009）逐项落地——②圈 N1 隐私号中枢（48h 双向号码池/掩码/回收）+ N15 IM 私信归位（chat/→base/ai/chat 保历史、base/comm/im 私信线程/未读，ContactCard 挂成交卡片）；③圈 N3 语义推荐（字 bigram 余弦）+ N6 自然语言 BI（规则解析聚合）；③④圈 N4 AIGC 鉴真（五信号加权+LLM 复核降级）+ N7 签章/履约险壳（djb2 验签 + 幂等投保理赔）；⑤圈 N8 危机干预（EPA 级别递增：联系人→值班→警方）+ N10 脱敏/遗忘权（五类掩码 + 按域匿名化）；⑥圈 N11 离线队列（指数退避）+ N12 熔断/供需杠杆 + N13 降级四部曲 + N14 数据湖哈希链存证/AB；①圈 N2 动态表单引擎（Schema→渲染描述器）+ N16 geo RN 适配接口（GeoSrc 注入）；transport union 合并新状态、package.json 测试清单扩列；单测 332→**365 全绿**、tsc/lint 0 错、build 通过 | |
| 2026-08-13 | 已推（4da1c5e 起多笔） | **ADR-0011 宪法 #7 合规补记**：语义推荐（N3）与 BI（N6）评估过 LLM 接入并记录不接入理由（延迟敏感需即时反馈 / 可解释可审计优于向量黑箱 / 封闭域规则可穷尽 / 零外部依赖免断供），真实 embedding API 与 LLM 意图改写留后续 ADR；ADR-0011 命中条文补 #7 + 单独「LLM 介入点评估」小节，符合宪法 §4 两栏强制 | |
| 2026-08-13 | 已推（a6edc57） | **宪法出处文档外链修复**：RPG 哲学原文从桌面断链 → `docs/RPG设计哲学与生命力.md` 入仓 git 跟踪（700 行），宪法 §5 引用路径同步修正 | |
| 2026-08-13 | 已推（08d6305） | **宪法收敛门禁落地**：`docs/CONVERGENCE-LOG.md` 登记表（追认 6 笔历史 rename）+ `scripts/convergence-check.mjs` 机器拦截（未提交 rename 即时拦截、已提交未登记 rename 报错、登记 commit 存在性校验，实测抓出 4da1c5e/7754884 两笔）+ root package.json `check:convergence` + 根/sub AGENTS.md 强制规则（修复 Windows 下 cmd 管道吞 `|` 误报通过的隐患） | |
| 2026-08-13 | 已推（97d1381） | **ADR-0016 对标 deep-research 吸收（未成年人分级 + 推送免打扰）**：年龄策略由「一刀切禁」修正为「分级模式」（ageGate：<14 须监护人同意、<18 资金闸全拦且不因 guardMode=false 解除——法规对齐《未成年人网络保护条例》§31/§43 +《未保法》§72/§76）；每日上限定位为防骚扰而非付费玩法；废弃 C 端置顶（伪需求）仅留 B 端推广；免打扰 quietHours 用户自主静音窗口不绑付费、urgent 危机永推；`ammo/risk-rule` 新增 age-required 引信（全局默认开 + 夜骑/夜爬类目成人专属）；单测 365→**381 全绿**、tsc 0 错 | |
| 2026-08-13 | 已推（619a332） | **ADR-0016 接线收口**：`useIdentityStore` 补 birthYear/guardianConsent + setAge；ProfilePage 新增「未成年人分级」（出生年输入 + <14 监护人同意勾选 + 模式展示）与「推送免打扰」（开关 + 二段默认静音窗口）设置区块；PublishSheet 发布路径接入 ageGate 分派（青少年免费局可发、鸽子险拦、超发付费拦）；`useQuietPrefStore` 持久化免打扰偏好；NotificationCenter 系统通知按 urgent（报价/接单/好友/裂变永推）vs normal（静音窗口内跳过）分流；381 全绿复验、tsc/lint/build 通过 | |
| 2026-08-13 | 已推（7c1bfae） | **对齐审计补记：CONVERGENCE-LOG 追溯登记 3 笔历史 SW rename**（`382663d` sw.ts→sw.js / `0a64fbd` sw.js→root / `7f868c3` sw.js→sw.ts，07-23 PWA 构建修复，宪法定稿前 §2 历史遗留）——深扫门禁 `--since` 全历史验证：登记前 exit=1 精确抓出 3 笔、登记后 exit=0 放行；登记册「唯一事实来源」闭环 | |
| 2026-08-13 | 已推（21a0a10） | **Meetup 实地调研对标**：基于 Meetup 官方 Help Center + Blog 一手资料，将 `docs/oto-competitor-matrix.md` 深化为 §五 Meetup 专项对标（12 维度现状对照 + 结论）——确认我方 5 项更强（签到 signInsure/fulfilment、缺席治理、反欺诈 Sentinel、匹配即时性 match.ts、合规 ageGate/quietHours/privacyNumber）；梳理 5 项吸收进 backlog（组织者审批 Request to spot、waitlist 候补转正、低分强制解释、organizer 出勤档案、guest +1 携伴）；3 项不抄（Meetup+ 付费解锁通讯/群 dues/静态兴趣搜索，均与宪法 #10 冲突） | |
| 2026-08-13 | 已推（a982d67） | **Meetup 对标用户裁决落地**：5 项吸收项正式登记 PROJECT_STATUS backlog（组织者审批★优先 + 候补转正 + 低分强制解释 + 出勤档案 + guest+1）；3 项裁决定调写入矩阵 §5.2 记录在案——Meetup+ 付费解锁 = ⏸️ 现阶段不上·潜在赢利点（若启用走宪法 §3 冲突上报）；群 dues = 🚫 C 端不抄·归 B 端/场地商家销售场景；静态兴趣搜索 = 🚫 落后不抄（类微信群） | |
| 2026-08-13 | 已推（fda4f0d） | **ADR-0017 组织者把关层（Meetup 吸收项①★落地）**：`base/order/wave.ts` needApproval/joinRequests 字段 + 纯函数 requestSeat/approveRequest/rejectRequest（不扩 ClaimStatus 状态机，宪法 #2 接口保守）+ store joinSeat 审批拦截 + requestSeat/decideRequest + UI（WaveCard 申请/待审批态、WaveFeed 接线、MyWaves 发起人审批面板、PublishSheet 开放局审批开关）；单测 381→**387 全绿**、tsc/lint/build 通过；竞品矩阵 §5.2 ①勾销、backlog 同步 | |
| 2026-08-13 | 已推（ab97bd7） | **非 max 模式质量修复批次（接线断裂七缺陷）**：① useWaveStore partialize 补 privacySessions/imThreads/imMessages（原刷新即丢）② ageGate 资金闸补齐响应押金（openClaim/joinSeat）+ 竞价（BiddingSandboxCard placeBid）真实资金入口 + 儿童「仅浏览」规格对齐（publish/respond 拦截）③ ADR-0017 decideRequest 补发起人身份校验（防自批自申）+ no-show 欠款锁定 + 一人一位（与 joinSeat 同防线）；WaveCard 被拒态修复（待审批态改 store 驱动、被拒可重试，接单失败不污染 UI）④ privacyNumber DEMO_POOL 改 11 位 + 兜底号格式统一（原 7 位掩码空操作泄露）⑤ revokePrivacy 终局回收接线（完成/取消/争议结算/撤单四路径）⑥ ContactCard 倒计时实时刷新（原挂载后冻结）⑦ BI 违约统计改数 violation 行（原数全部行）→ 单测 387→**389 全绿**（+2）、tsc/lint/build 通过；ADR-0011~0015 在状态档如实降级标注「库储备」（生产消费方未接线） | |
| 2026-08-13 | 已推（b02db44） | **库储备接线批次 A（ADR-0011/0012/0013 消费方补齐，第三圈+第五圈）**：ChatPage 本地 BI 直答（askBi store action：wave/claim/breach/review/dispute 五源聚合，统计关键词命中不耗 LLM）+ 输入实时语义推荐活跃局（embed 余弦 chips，点击跳雷达）；AcceptancePanel 争议凭证实时鉴真（checkTextEvidence 对比同 claim 历史凭证，疑点分/级别展示）；ProfilePage「安全中心」（SOS 三级别发起 + EPA 通知对象 chips + SMS 预览 + 处置闭环；数据脱敏五类掩码演示；遗忘权按域申请幂等 + 状态列表）；useWaveStore 新增 crisisRecords/forgetRequests（transport merge 并入 + partialize 持久化 + removeItem 兜底）；→ 单测 389 全绿、tsc/lint/build 通过；剩余库储备：signInsure/circuit/offlineQueue/resilience/dynamicForm/geoAdapter（批次 B 待排期） | |
| 2026-08-13 | 已推（3f762f0） | **库储备接线批次 B（ADR-0012/0014/0015 消费方补齐，第四+六+一圈）**：① 熔断器接 clusterPushes（fail≥3 → open 熔断、30s 冷却后半开探测、熔断期本地 mock 抽取、回执持久化）② sendIm 弱网离线缓冲（离线入队 + online 自动重放 + ContactCard 队列提示条/手动重发）③ 数据湖哈希链存证（验收/开争议/争议终局 append，AdminPanel 存证块全链校验 + 最近事件）④ 验收签章（acceptFulfilment 生成 signedDoc，争议面板验签展示）⑤ DynamicFormView 通用渲染端 + ProfilePage 紧急联系人 schema 表单（SOS 发起读已存联系人）；store 新增 circuitBreaker/offlineQueue/lake/signedDocs（transport 本地缓冲语义 next 优先防哈希链断裂、partialize 持久化）；geoAdapter 留 mobile 融合、AB/degrades 无真实场景不硬造；→ 单测 389 全绿、tsc/lint/build 通过 | |
| 2026-08-14 | 已推（2ce4c48） | **批次 B 浏览器冒烟 + 两处接线缺口修复**：① 生产实测验收/争议/离线缓冲全闭环 —— 需求方视角验收（signedDoc 签章 + lake 哈希链 genesis→dispute 链式校验通过）+ 争议面板（结果/模块不符自动判责 + 48h 申诉窗 + 验签展示「有效（me-2jt @ …）」）；② **React #185 无限渲染修复**：AcceptancePanel DisputeForm 的 useWaveStore selector 每次渲染返回新数组（filter+map 内联），触发 zustand v5 useSyncExternalStore 无限循环 → 行程页（me-20o2 含已申报单）白屏崩溃；改为 selector 取稳定引用 s.disputes + useMemo 派生，dev 生产双端复现验证修复；③ **ADR-0010 N1 接线缺口修复**：claimDirect 直接接单（甲方案）锁定后未分配隐私会话（仅 acceptClaim 磋商路径分配）→ ContactCard 永不渲染、离线队列 UI 不可达；补齐 openClaim 直接接单分支 allocatePair，实测会话分配（48h/138****0001）+ 断网发私信入队（done:false）→ 恢复在线 online 事件自动重放（imMessages 落库 + doneAt），ContactCard 消息气泡展示 ✅；单测 389 全绿、tsc/lint/build 通过 | |
| 2026-08-14 | 已推（2ce4c48） | **ammo 四表 → base 引擎真实业务接线（任务 2，宪法收敛 #3/#4/#5）**：① **dispatch-rule** → broadcastMatches/passesHardFilter/requiresVerification 增 rule 参数（DEFAULT_BROADCAST_RULE = 原常量，行为零变化），WaveFeed/useWaveStore 按品类取表注入 → 家政 distance 40 生效、遛狗进家认证硬门槛表驱动 ② **risk-rule** → riskOf/extraLogin 增 params 参数（DEFAULT_ROAM_PARAMS = 原阈值），useRoamStore/RoamGuardPanel/AdminPanel/useWaveStore sentinel 聚合统一注入（引信关 → high 封顶 watch）③ **pricing-formula** → BiddingSandboxCard reserve 地板价（max(预算,minPriceYuan)）+ PublishSheet 品类建议起价/保修文案展示 ④ **sop** → PublishSheet 选品类预填（鸽子险/有效期/容量，有效期首档表驱动动态生成），补「羽毛球约局」别名行（原与「羽毛球」键不一致 → 预填回退默认）；浏览器实测：家政保洁（1 人/鸽子险开/2 小时/¥50·满意度保障）、羽毛球约局（4 人/鸽子险关/6 小时）全链路生效；→ 单测 389→**394 全绿**（+5：broadcast 规则注入 2 + roamGuard 引信参数 3）、tsc/lint/build 通过、收敛门禁 exit 0 | |
| 2026-08-14 | 已推（f57feb2） | **Meetup 吸收项 ③ review 低分强制解释（≤3 星必填理由）**：ReviewSection 提交拦截——最终分 = 三维均值（punctual/attitude/professional），`explanationRequired`（≤3 星且正文空/不含理由触发词）→ 红框警示「低分评价必须填写理由（防恶意差评）」拦截、零副作用；星标变化即重评（改高/补文后自动解除警示）；浏览器双身份全链路实测（发布→接单→申报→验收→打 1 星→无理由提交被拦（不入库/面板保持/红框警示）→填理由「迟到 25 分钟且工具没带齐」→入库 ★1 + reviewedBy 幂等位 + UI「已评价 ★1」）；PROJECT_STATUS backlog ③勾销；单测 394→**397 全绿**（+3 explanationRequired）、tsc/lint/build 通过 | |
| 2026-08-14 | 已推（8fe344c） | **Meetup 吸收项 ② waitlist 候补转正（满员进候补、退出按序/信用分补位、配合 release）**：纯函数层 `wave.ts` 增 `waitlist` 字段（挂 Wave，不扩 ClaimStatus，宪法 #2）+ `sortWaitlist`（FIFO 主键 + 同刻信用分 tie-break）+ `joinWaitlist`（幂等，active/assembled 均可入队）/`leaveWaitlist`/`promoteFromWaitlist`（支持 acceptDirect：成局让位 → 补位者直接 accepted 锁定席位，局保持 assembled；鸽子险联动 held）；store `joinWaitlist`/`leaveWaitlist` actions（no-show 欠款锁定 + 一人一位 + 未成年人资金闸同 joinSeat 防线）+ `withdraw` 席位释放双通道：① 成局前 joined 退出 → 补位者 joined 占座 ② **成局后让位（accepted 履约前）→ 按 24h 档位退让位者份额（refundByTier 同 cancelOpenWave 车道）+ 补位者直接转正 accepted + 占位即付（capturePayOrder）+ 拼位裂变计数 + push 通知**；notify 聚合「🎟️ 候补转正」文案（waitlist-promoted: 前缀）；UI：WaveCard 满员/已成局 →「进入候补」入口 + 候补中第 N 位态 + 候补人数徽章、WaveFeed 保留已成局开放局在 feed（座位数按 accepted 计）、MyClaims 候补队列卡片（退出候补）+ 成局让位按钮；浏览器三身份全链路实测（发起 3 人局→拼 1 席→提前成局→me-hay 候补第 1 位→me-pr 让位→**me-hay 自动转正 accepted + 立即扣款 + me-pr 全退 + 候补清空 + 通知「🎟️ 羽毛球约局 候补转正」**）；单测 397→**407 全绿**（+10：waitlist 幂等/拒绝/退出/无候补原样/FIFO 补位/信用分 tie-break/鸽子险联动/acceptDirect/闭环）、tsc/lint/build 通过 | |
| 2026-08-14 | 已推（a95ec5b） | **Meetup 吸收项 ④ organizer 出勤档案视图（复用 no-show/violation 数据）**：纯函数 `base/order/attendance.ts`——`attendanceLedger`/`attendanceFor` 跨局聚合出勤档案（joinedWaves 参与局数 / shown 到场履约 serviceDoneAt / noShows breached / withdrawn 退出让位 / waitlisted 候补在队 / showRate 出勤率），数据全部从现有 claim 状态机 + wave.waitlist 派生（宪法 #2 不扩状态机、#6 信任数据沉淀，纯只读无写路径）；UI `AttendancePanel.tsx`（组织者视图）：MyWaves 已成局开放局卡片「出勤档案（Meetup 组织者视图）」入口，roster = 本局曾占位者（accepted/breached/withdrawn 让位者可见 + 候补中）+ 昵称映射（responders/identity），彩色出勤率 + 鸽 N/退 N/候补 N 徽章；单测 **407→412 全绿**（+5 attendance）、tsc/lint/build 通过；浏览器实测（成局局面板显示 me-pr「参与 1 局 · 退 1」+ me-hay「参与 1 局 · 出勤率 0%」）；backlog ④勾销 | |
| 2026-08-14 | 已推（65b6113） | **Meetup 吸收项 ⑤ guest +1 携伴（携伴者实名登记对齐 ageGate/privacyNumber）**：纯函数 `base/order/guest.ts`——`GuestInfo`（称呼 + 出生年 + 监护人同意 + 联系方式）、`addGuest`（上限 1 人/座、claim 锁定校验、名称必填）、`removeGuest`（幂等）、`maskPhone`（138****0001 脱敏）、`visibleGuests`（脱敏展示视图）；`Claim.guests` 字段增补（宪法 #2 增补不扩状态机）；store `addGuest`/`removeGuest` actions——**ageGate 合规**：携伴者 <14 无监护人同意 → 拦（未保法 §72）；儿童有同意仍拦（分级 §43 仅浏览围观，宪法 #8 默认保护）；UI `MyClaims GuestSection`（座位锁定后「+1 携伴」登记表单：称呼/出生年/电话/监护人同意勾选，脱敏展示 + 移除）、`AttendancePanel` 组织者视图成员行「+1 ×N」徽章；单测 **412→420 全绿**（+8 guest）、tsc/lint/build 通过；浏览器双身份实测（me-hay 登记成人携伴「小美 · 1995 年生 · 138****0002」脱敏展示 ✓ → 儿童 2019 无同意被拦「§72」✓ → 有同意仍拦「§43」✓ → 组织者面板 me-hay「+1 ×1」✓）；backlog ⑤勾销——**Meetup 吸收项 5/5 收官** | |
| 2026-08-14 | 已推（ef4bac5） | **库储备接线批次 C（ADR-0012 N7 履约保险 + ADR-0015 N16 WebGeoSrc 真实定位）**：① **履约保险接线**——signInsure `insure`/`claim` 纯函数从库储备变生产消费方：store 新增 `policies`（WaveBundle 字段：接口 + partialize 持久化 + transport merge 按 id 合并且本地优先防 claimed 回退 + removeItem 兜底 + transport.test fixture 同步）+ `insureClaim` action（仅本人座位可投保、claim 锁定校验、保费 = 座价 10% 幂等、保费走 `book("insure", -premium)` 本人钱包账本——LedgerKind 增补 "insure"）；resolveNoShow 违约自动理赔（breach 时 holder 未理赔保单标 claimed）；UI：MyClaims InsureBar（投保/保单有效/已理赔三态）、MyWaves LockedSeatFlow 保单徽章 + 「不谅解」handler 理赔入账（`receivePayout` 幂等键重构为 kind+claimId——修复鸽子险与履约险同 claim 互斥跳过的缺陷）+ verdictMsg 联动文案；实测：投保 -¥5 账本 ✓ → 保单跨 tab 同步 ✓ → 「未到场」resolveNoShow 自动标 claimed ✓ → 裁决不谅解 **「履约保险理赔到账 +¥50」入账且鸽子险 +¥5 不重复** ✓ ② **WebGeoSrc**——geoAdapter 新增真实定位实现（navigator.geolocation 封装：8s 超时/拒绝 → current() null 降级 mock，宪法 #10 永不裸奔）+ WaveFeed 顶部 GeoSourceBadge（按需授权「启用 ›」/已授权「📍 真实定位」/拒绝「定位未授权 · 演示坐标」三态，实测授权拒绝 → 降级态正确流转）；单测 420→**425 全绿**（+5 signInsure）、tsc/lint/build 通过 | |
| 2026-08-14 | 已推（e4b5607） | **P8 线上化 · PWA 真推（LAUNCH-GAP E 组 2/3，表待建）**：web-push@3.6.7 依赖 + VAPID 密钥对生成脚本（`scripts/generate-vapid.mjs` 幂等写 root/.env.local）+ sw.js 注入 push 渲染/notificationclick 聚焦/pushsubscriptionchange 重订阅三事件 + PushEnableBar 客户端（授权 → pushManager.subscribe → /api/push/subscribe 后端幂等 upsert by endpoint + 「发送测试」调 send API；表未配置/权限拒绝/订阅失败分态文案）+ /api/push/subscribe（Supabase upsert，表缺失映射 501 push-table-not-configured 降级）+ /api/push/send（web-push VAPID 签名发送，410/404 自动清理失效订阅）+ 迁移 `20260814_push_subscriptions.sql`（endpoint UNIQUE + RLS + service_role 授权 + delete RPC）；**.env.local 服务端 env 供给打通**（Next 不读 root/.env.local——API 服务端密钥须放 oto-spatial-web/.env.local，gitignore 保护）；实测：env 注入 ✓、subscribe API 通到「表不存在」✓、501 降级 ✓；**阻塞：Supabase 建表**——MGMT_TOKEN 401 失效 / DB 密码直连失败 / 无 exec_sql RPC，用户裁决「表先不建本地降级」，待管理员在 SQL Editor 执行迁移后即可用（代码侧全链路就绪） | |
| 2026-08-14 | `ae3f764` | 对齐审计回填：PROJECT_STATUS 状态列全量对齐 git 实况（8-10~8-14 全部批次标记为已推并回填 commit——此前 20 余行「待提交/未推」实为文档滞后，代码早已推送：ADR-0016 双行→97d1381/619a332、审计补记→7c1bfae、Meetup 对标 21a0a10/裁决 a982d67、ADR-0017→fda4f0d、批次 B→3f762f0、waitlist 8fe344c/出勤 a95ec5b/guest 65b6113、批次 C→ef4bac5、PWA→e4b5607、ADR-0006→7754884 链、宪法定稿→e19f3d8、N1-N16→4da1c5e、lint ee4ea35/扫码本地三件 3e84982）；实测 c4899f3/ee4ea35/3e84982 均在 origin/master；git 实况 ↔ 文档完全对齐（唯一遗留 = 88 行历史说明） | |
| 2026-08-14 | 已推（5439439） | **e2e 全量回归（12/12 PASS）+ 契约漂移修复**：`scripts/e2e-*.mjs` 全量跑通（app/match/wave/openmatch/review/push/fulfil/governance/trust/trust-open/acceptance/offline）；修复 4 类 e2e↔近期 UI 契约漂移（非功能回归）：① 8b8cfc4 发布表单「更多选项」折叠 → 9 脚本发布前展开（含 trust-open `publishOpen` 函数、acceptance 场景 B 二次展开）② 「在线 · 正在接收信号」文案 →「正在接收信号」（UI 批次后状态点+短文案）③ `getByLabel("我的")` 子串歧义（命中「我的报价」input）→ `{ exact: true }` ④ 「🚫 未到场」按钮按 aria-label「标记未到场」匹配（aria 优先，原 🚫 文案匹配自始为 0）；另 2 处 reload 等待 networkidle → domcontentloaded（上游 LLM 流式响应慢时 networkidle 永不达成，headless 实测网络 1s 静默、无循环请求）；单测 **425 全绿**（基线复验）、tsc/lint/build 通过 | |
| 2026-08-14 | 5439439（文档同步修复批次·待提交） | **文档/外围同步修复（三方审计收口）**：README.md 重写（五 provider Gateway 架构 + 任务路由/降级链 + 环境变量表 + PWA 推送配置指南，旧路径 `lib/chat/llmDirective.ts`/`lib/match.ts` 修正为 base/ 实际路径）；新建 `.env.example`（全 10+ env 键分组注释 + 必填/降级语义；`.gitignore` 补 `!.env.example` 例外）；LAUNCH-GAP.md E 组更新（LLM 聚类推送标已真实化、推送渠道标「✔ 代码侧全链路就绪 e4b5607，阻塞=建表」）；架构口径修正（11 API / E2E 12 个 / P4 补 ANON key 本地降级说明）；CONVERGENCE-LOG ADR-0016 登记回填 `97d1381`+`619a332`；清理 `.bak-20260808` 残留 ×2；单测 **425 全绿**（复验）、收敛门禁 exit 0 | |
| 2026-08-15 | `afce408`（+`8274f40` 登记） | **单仓融合 Phase 5：测试体系合流 + 存量缺陷清零 + 全量 Build 验证**：① **根 vitest 426/426 全绿**（11 存量失败清零——AI 组 4 测试入参对齐 `NegotiateInput`、SLA 组 mock `getServiceClient`+`getNextFundStatus`+`single` 链、Milestone 组 mock 链补 `select/single/maybeSingle` + confirm 实现补清 `auto_confirm_at`、credit-engine mock 补 `getNewbornProtectionFactor/getWeekendMultiplier`）；② **D-05 测试体系统一并轨**：src/base|ammo|lib 57 个 node:test 幂等保留（实测 vitest 收集 `node:test` 语法 57/57 "No test suite found"），根 `npm test` 组合 `vitest run`(426) + 子 `test:units`(425) **一键 851/851 全绿**；vitest.config.ts 补 `@/base`/`@/ammo`/`@/store` alias；12 个 `e2e-*.mjs` 留存无损；③ **OTO 路由缺陷修复（Phase 4 遗留）**：`(oto)` 路由组不参与 URL 路径 → `(oto)/page` 与根 `page.tsx` 同为 `/` 冲突，5 屏 SPA 构建期被根页面吞并、`/oto` 生产 500；rename 为 `src/app/oto/` 真实目录后 `/oto` 生产 200 输出 SPA HTML；④ 全链路验证：根/子 tsc 0 错 + `next build` exit 0（121 静态页 + serwist /sw.js）+ 收敛门禁 exit 0；基线：根 vitest 426 / 子 node:test 425 / e2e 12 | |
| 2026-08-15 | `b879e32` | **元宪法确立（纯文档批次·待提交）**：人类创始人下达最高哲学架构元宪法（RPG 火箭筒微内核模型）→ 全仓勘测产出 `docs/ARCHITECTURE_TAXONOMY.md` 白皮书（四层定义 + 6 大工程红线 + 全仓归属映射表 + 六圈投影 + 落差审计：已落地重器 / 偏差 D1-D8（含 4 处 base 反向 import 实证）/ 缺口 P0-P3）；`DESIGN_CONSTITUTION.md` 加元宪法声明节 + 修订记录行；本批次为纯文档改动，零代码变更，856 测试基线不受影响 | |
| 2026-08-15 | 本批收口 | **终验 Gap 全量收口（G-1~G-7，健康度 89→100 基线）**：① G-2 测试归位——删除融合期占位 `src/base/ai/gateway/probe.test.ts`；核心资金测试 `src/base/money/pay.test.ts` 纳入 `test:oto:units` 清单 → node:test 425→**430**、全仓 **856 全绿** ② G-1 CI 门禁恢复——ci.yml 重建四 job 真实检验链（TypeCheck&Build / UnitTests&Lint(856) / E2E Regression push 全链 12 条 / Vercel Smoke），根治 b0f16af 替换导致的「文档声称 CI 无实现」 ③ G-3 db-migration.yml 触发分支 main→master（原永不触发）④ G-4/G-5 文档与范本——README/STATUS 同步 856 口径与 CI 真实规格、.env.example 补 GROQ_MODEL + 清 oto-spatial-web 过时路径注释与无消费者键、修正验证基线表单测行历史残留（389→856）⑤ **G-7 lint 存量债务如实登记**——实测根 eslint 239 errors/249 warnings（融合前遗留，PROJECT_STATUS 旧「Lint exit 0」声称不实）：12 条规则（no-explicit-any 184 / react-hooks 35 / react 10 / prefer-const 5 / ts 5）降级 warn 不阻断 CI → lint exit 0 / 0 errors / 存量 726 warnings，治理窗口开启后逐条恢复；⑥ **A-1 灵感漩涡经人类裁决暂缓**，保持 Backlog 挂起状态，本次不启动 | |
| 2026-08-15 | 反腐手术批次 | **底座反腐外科手术（D1~D3 治理）**：① D1 拔除底座对 UI Store 反向依赖——`llmEngine`/`mockEngine` 移除 `useAppStore` 运行时 import，新增 `ChatEngineContext` 契约（`getChatMessages`/`isWorkerOnline`），ChatPage 显式注入；② D2 越级契约上收——`WaveBundle` 从 `store/useWaveStore` 提升至 `src/types/wave-bundle.ts`（transport/supabase 改引该契约，store extends 不改）；③ D3 业务词剥离至弹药层——sentinel `HOME_ACCESS_KEYWORDS` → ammo/risk-rule `home-access-verification` 引信 `params.homeAccessKeywords`（新增 `homeAccessKeywordsFor`）；broadcast `DEFAULT_BROADCAST_RULE.requiresVerified` 置空（ammo dispatch-rule 装填）；booking `ICON_RULES` → ammo/scene-template `CATEGORY_ICON_RULES`（iconFor/applyBooking/bookingWorkerOrder 增 rules 注入参数）；生产调用方 CapabilityPanel/WaveFeed/useWaveStore/useAppStore 全量改注入；④ 验证：tsc 0 错 + `npm test` **856/856 全绿**（vitest 426 + node:test 430，测试适配弹药注入语义并补「未装填→通用兜底」断言）+ 收敛门禁 exit 0 | |
| 2026-08-15 | 核心设计模型批次 | **人类创始人注入底层核心设计模型（白皮书 §二 + 代码契约）**：① **五态原子状态机**——`PUBLISHED➔MATCHED➔IN_SERVICE➔INSPECTED➔SETTLED` 固化（不可分割、跃迁矩阵唯一、底座主状态机绝对封闭）+ 伴生事件 Sub-Events 插拔（BEFORE/AFTER + SKIP/BLOCK/DEFER 降级）+ 与现有 wave 状态机过渡映射表；② **三类动态风控引信矩阵**——💥碰炸（背调+保证金+留痕+财产险）/ ⏳延期（预付冻结+LBS 围栏+反赌反诈）/ 📡近炸（虚拟号+模糊定位+AI 敏感词+SOS 联动），引信跟弹药走；③ **数字人格信用飞轮**——资产通兑 + 低风险履约沉淀信用 → 降低高风险准入门槛；④ 契约落位：新建 `src/types/ammo-schema.ts`（AtomicFiveState/FIVE_STATE_TRANSITIONS/ISubEventHook/PricingModel/IAmmoDefinition）与 `src/types/fuze-policy.ts`（FuzeType/IFuzePolicy + 三类预置模板 + DEFAULT_FUZE_POLICY），纯类型零业务依赖；⑤ 验证：tsc 0 错 + `npm test` **856/856 全绿**（纯类型新增，基线不变）+ 收敛门禁 exit 0 | |
| 2026-08-15 | 28 模块主蓝图批次 | **平台架构全景梳理注入（白皮书 §三 3.4 + 宪法模块编号标准）**：① 六层防御圈 × 核心模块职责矩阵固化——标准模块编号 `L1-M1`～`L6-M4`（L1 触达×4 / L2 业务核心×6 / L3 AI 神经×5 / L4 风控防御×5 / L5 生态网关×2 / L6 生存基建×4），定为全项目永久统一编号标准（宪法元宪法声明 + 修订记录双登记）；② 28 行代码落位与成熟度对照表——每模块标注代码路径 + 测试覆盖 + 🟢🟡⚪️（实测 🟢13 已闭环 / 🟡12 有雏形 / ⚪️1 待建设 = L4-M3 物理履约闭环）；③ **口径核查**：注入清单逐项清点实为 26 模块（与标题「28」差 2 席，编号体系完整成立，差量已在白皮书标注待人类裁决）；④ 验证：纯文档批次零代码变更，856 基线不动 + 收敛门禁 exit 0 | |

| 2026-08-15 | 三阶段路线图批次 | **平台落地推进路线图注入（白皮书 §四）**：三阶段研发与业务演进节奏固化——① **Phase 1 MVP 验证期（0➔1）**：单一重信任场景（家庭深度保洁）打磨底座，L2-M1 五态机 + L1-M2 LBS 围栏 + L2-M4 担保托管 + L2-M5 隐私虚拟号 + L4-M1 碰炸引信，首枚官方标准弹药 `housekeeping.ammo.ts`（现场增项报价 Hook，衔接 P0-1 AmmoRunner）；② **Phase 2 体系成熟期（1➔10）**：轻履约/组局社交品类（麻将、同城搭子），动态表单 + 统一计价 AA + LLM 意图转单/小法官/向量撮合/AIGC 鉴真，标杆弹药 `meetup.ammo.ts`（⏳延期引信）+ `companion.ammo.ts`（📡近炸引信）；③ **Phase 3 规模壁垒期（10➔100）**：全品类开放，弱网离线深度同步 + 多云多活降级 + 运力熔断限流 + 司法级哈希存证；④ 每阶段绑定模块编号 + 缺口衔接（P0-1/P0-3/P1-1/P1-5/P2-2）+ 现状标注；⑤ 顺带修复白皮书小节编号一致性（§三 2.x→3.x、落差审计 3.x→5.x，宪法引用同步）；⑥ 纯文档批次零代码变更，856 基线不动 + 收敛门禁 exit 0 | |
| 2026-08-15 | 本批（P0-P3） | **AmmoRunner 弹药体系落地（人类裁决：终止事件携带结算载荷流转 SETTLED / IAmmoDefinition 增补 dispatchRule? / 纯函数投影桥）**：P0 契约增补——`types/ammo-schema.ts` 增 `buffSeats?`/`maxRounds?`（对齐 SopParams）+ 内联 `IDispatchRule` + `ITerminationEvent`（CANCELLED/EXPIRED/BREACH_SETTLED）+ `IAmmoDefinition.dispatchRule?`；P1 弹药注册表——`ammo/registry.ts` 纯函数聚合四表（`getAmmoDefinition`/`toPricingModel`/`toFuzePolicy`/`toDispatchRule`/`DEFAULT_AMMO` 保底，7 类目全聚合零表改动）；P2 运行引擎——`base/ammo/runner.ts`（红线 3 纯函数：`toAtomicFiveState` 投影桥 + `advanceLifecycle` 跃迁矩阵校验/BEFORE·AFTER 钩子调度（BLOCK 阻止/SKIP 忽略/DEFER 暂存）/终止事件捕获 + `evaluateAmmoFuze` 引信核验（三类模板并集拦截））；P3 标杆重弹——`ammo/housekeeping.ammo.ts` 双轨升级存量 `lib/protocol/protocols/housekeeping.ts` 资产（六阶段 STAGE/分级退款规则/照片证据契约投影导出），装填 💥IMPACT 引信 + HOURLY 计价 + `OnsiteQuoteHook`（IN_SERVICE BEFORE，未确认增项 BLOCK）+ `CleaningCheckHook`（INSPECTED AFTER 双向拍照）；新测试入 `test:oto:units` 清单；验收：tsc 0 错 + `npm test` **897/897 全绿**（856 基线 +41：registry 10 / runner 20 / housekeeping 11）+ 收敛门禁 exit 0 | |
| 2026-08-15 | `ef5197e` | **第 2 号官方标杆弹药 meetup-social-v1 装填（组局社交，⏳延期 + 📡近炸双引信并联）**：① `ammo/meetup.ammo.ts`——双轨升级存量 `lib/protocol/protocols/dating.ts`（202 行：commitment 双押金 / dualDeposit 1:1 / confirm_arrival / gps_track / 4 维评价）投影导出 `MEETUP_STAGES` 六阶段 + `MEETUP_REFUND_RULES`（未成局全退 / 爽约押金全失归守约方 / 到场互不罚）+ `MEETUP_EVIDENCE`（到场定位/扫码/活动照），`MEETUP_DUAL_FUZE` 双引信并集（DELAY：30% 预付冻结 + 500m 围栏解锁 + 反赌反诈；PROXIMITY：STANDARD 背调 + 虚拟号 + 模糊定位 + 敏感词干预 + SOS 全联动，逐段取模板防 spread 覆盖）+ `ArrivalCheckHook`（IN_SERVICE BEFORE BLOCK：LBS 围栏/扫码验真解锁冻结）+ `AASplitSettleHook`（SETTLED AFTER SKIP：AA 多退少补分账 + 守约/违约信用回写 m07 飞轮）+ PER_SEAT 计价 + 防鸽子（MATCHED 前置引信核验）；② `ammo/registry.ts` 新增 `OFFICIAL_AMMO` 官方直挂表（housekeeping/meetup/dating/social 四键优先于四表聚合，未配置类目仍保底）；③ `base/ammo/runner.ts` 终止路径 `termination.payload` 合并进钩子 ctx（AA 钩子可读违约赔付载荷，既有断言兼容）；④ `ammo/meetup.ammo.test.ts` 19 用例（装备完整性/双引信并集/防鸽子拦截/五态全流程/到场验真 BLOCK/扫码放行/超时爽约 BREACH_SETTLED/EXPIRED 全退/AA 混合补缴/SKIP 降级/注册表直挂）；⑤ 验收：tsc 0 错 + `npm test` **916/916 全绿**（897 基线 +19）+ 收敛门禁 exit 0 | |
| 2026-08-15 | `7e8239d` | **前端微内核交互架构注入（纯文档批次·零代码变更）**：人类创始人下达前端 UI/UX 哲学架构，与后端「40mm 发射筒 + 插拔弹药」全栈大统一——① 白皮书 §五 5.4 新增「前端微内核与系统级交互架构」：容器心智模型（**不变的外骨骼 Universal Shell**：五态灵动状态胶囊 `toAtomicFiveState` 直接投影 + 全局 SOS 安全中心 + 底部万能 CTA + AI 语音浮窗 × **流动的动态视口 Dynamic Scenario Core**：`IAmmoDefinition`+JSON-Schema 毫秒动态渲染 + 场景专属微氛围 家政素雅蓝/组局霓虹橙/陪玩星云紫）+ 前端微内核 **5 大交互法则**（外骨骼锁定肌肉记忆·视口渲染场景灵魂 / 五态灵动胶囊 / 多数字人格流体双模态下滑切换 / AI 意图转单拟物草稿卡 / 隐形防御显性物理触感锚点 50m 微震+NFC 水波纹）+ 组件三层挂载映射图谱（外骨骼层 4 / 视口层 5+弹药面板待建 / 物理感知层 3）；D-8 判定挂接 5.4 与 P2-1 主题 Token；② 元宪法 `DESIGN_CONSTITUTION.md` 追加【前端交互工程红线】两条（外骨骼锚点神圣不可侵犯 / 禁止品类硬编码全套页面，一切业务走弹药+动态视口），违反即 §3 冲突上报；③ 验收：`npm test` **916/916 全绿**（纯文档零代码）+ 收敛门禁 exit 0 | |
| 2026-08-15 | `c100890` | **UI/UX 全景系统架构注入（4 层体系与 5 态镜像视口标准）**：① 白皮书 §五 5.5 新增四层体系——**第一层 设计令牌与微氛围层**（全局灰阶栅格 + 三大场景微主色 `theme-housekeeping` 专业蓝 / `theme-meetup` 活力橙 / `theme-companion` 夜幕紫 + 适老化高对比度排版引擎）；**第二层 全局通用交互骨架层**（顶部灵动状态胶囊：五态同步+弱网预警+LBS 指示 / 全局多重人格坞：雇主·服务商·组局者单手极速切换 / 悬浮智能中枢 AI Copilot Orb：语音转单+小法官直达 / 全局底线防护栏：一键红色 SOS+隐私行程分享）；**第三层 动态视图与插槽渲染层**（5 态镜像视口标准：`AtomicFiveState` ↔ `ViewportStage` 一一镜像——A 需求发布视口 PUBLISHED（对话流草稿卡+JSON-Schema 动态表单）/ B 撮合匹配视口 MATCHED（雷达波纹+抢单大厅+向量打分卡）/ C 履约时空视口 IN_SERVICE（LBS 3D 轨迹+虚拟通信条+离线事务栏）/ D 验收对账视口 INSPECTED（弹药特化插槽：前后照片比对·AA 滑块·增项弹窗 + NFC/动态码全屏核销）/ E 结算信用视口 SETTLED（多方分账抽屉+六维雷达打分+跨场景积分动效））；**第四层 极端场景与无障碍容灾层**（弱网离线半透明提示条 / 大字大触控适老模式 / 防暴力伪装计算器：高危场景标准计算器掩护+特定数字组合静默报警与音频回传）；② 契约落位 `src/types/ui-viewport.ts`——`ScenarioTheme`/`ViewportStage`/`VIEWPORT_STAGE_BY_STATE` 镜像映射/五视口插槽 `IDraftingSlotProps`~`ISettlementSlotProps`/四骨架件 `IStatusCapsuleState`~`ISafetyGuardState`/容灾三态 `IOfflineBannerState`/`ISeniorModeTokens`/`IStealthCalculatorState` + `IViewportSlots` 聚合，纯类型零业务依赖（红线 3）；承接 P1-5（视口插槽）+ P2-1（微主色 Token）落地形态；③ 验收：tsc 0 错 + `npm test` **916/916 全绿**（纯类型新增零测试影响）+ 收敛门禁 exit 0 | |
| 2026-08-15 | `d9121eb` | **端到端三大核心页面拓扑 + 外骨骼/视口首件组件落地**：① 白皮书 §五 5.6 新增「三大核心页面拓扑与交互流转标准」——**页面一 动态发布页 Dynamic Launchpad**（ASCII 线框：StatusCapsule + Copilot Orb + DynamicDraftCard 拟物草稿卡 + 弹药切换 housekeeping↔meetup；CTA 发射 ➔ PUBLISHED；字段构成 = IAmmoDefinition 整弹解析）+ **页面二 通用五态履约主屏 Universal Fulfillment Cockpit**（B~E 五态镜像视口区按态切换、D 视口弹药特化插槽、advanceLifecycle 跃迁驱动胶囊+视口联动、ITerminationEvent 分支直入结算视口）+ **页面三 争议调解与小法官半屏抽屉 Dispute & AI Arbitration Sheet**（半屏上滑、证据链展列、小法官 Advisory 裁定卡、双出口 采纳结算/人工仲裁，红线 1 写入由用户确认）；每屏标注状态机驱动关系 + 宪法落位；② 代码落地——`oto-ui/StatusCapsule.tsx`（顶部五态灵动胶囊：五态自适应色/呼吸脉冲/🟡🔵🟣🟠🟢 文案 + 显性红色 SOS 按钮 + 📴 弱网离线徽标 + LBS 距离指示，自包含 CSS 零外部依赖）+ `waves/DynamicDraftCard.tsx`（拟物磨砂玻璃草稿卡：getAmmoDefinition 整弹驱动 → SOP 参数行可微调 + PricingModel 计价文案 + IFuzePolicy 安全徽章投影 🛡️🔒⏳📍📞🆘 + 扣动扳机一键发布 CTA）；③ 单测 15 用例（StatusCapsule 9 + DynamicDraftCard 6：五态渲染/离线/SOS/距离锚点/双弹药差异渲染/保底弹药零徽章/注入覆盖优先），vitest node 环境 react-dom/server 静态渲染；④ 验收：tsc 0 错 + `npm test` **931/931 全绿**（916 +15）+ 收敛门禁 exit 0 | |
| 2026-08-15 | `85fdf82` | **三大场景 UI 插槽特化组件集 + 通用五态履约主屏落地**：① 白皮书 §五 5.7 新增「三大典型业务场景 UI 插槽特化全景对比矩阵」——家政保洁（重入户/清洁蓝）/ 组局社交（轻履约/活力橙）/ 同城陪玩（高人身风险/夜幕紫）6 大交互维度特化标准（主题微色调 / 发布页动态组件 / 匹配等待动效 / 履约核心特化插槽：增项双拍 vs 座次 AA 围栏 vs 隐私盾+伪装电话 / 核销完工：NFC 碰碰 vs 组织者解冻 vs 300m 脱离自动停表 / 争议售后：损坏直赔 vs 放鸽子申诉 vs 一键拉黑敏感词）+ 弹药映射（getAmmoDefinition 整弹解析 → 视口按 ammoId 装载插槽）；② 契约增补 `src/types/ui-viewport.ts` `ICompanionSlotProps`（isPrivacyShieldArmed / onTriggerFakeCall / departureDistanceMeters 默认 300m / onBlockUser）+ `IViewportSlots.companion` 挂载位；③ 组件集 `src/components/waves/slots/`——`HousekeepingSlot`（现场增项改价确认单：增项/金额/确认/拒绝 + Before/After 双拍池 + 损坏包赔理赔直连）/ `MeetupSlot`（实时座次表到场/未到场 + 500m 签到围栏扫码验真 + AA 多退少补对账卡 + 放鸽子申诉）/ `CompanionSlot`（隐私防骚扰盾 + 📱伪装假电话一键脱身 + 300m 安全距离指示条 + 敏感词一键拉黑）；④ 通用履约主屏 `waves/FulfillmentCockpit.tsx`——外骨骼顶栏（StatusCapsule 五态/LBS/离线/SOS）+ 服务者通用卡（头像/实名/六维信用分 trustScore/虚拟通话/隐私聊天）+ 场景主题微色 label（theme-housekeeping/meetup/companion）+ 动态插槽区毫秒切换 + 底部三场景特化核销 CTA；⑤ 单测 11 用例（FulfillmentCockpit.test.tsx 高密度矩阵：三场景精准渲染/外骨骼集成/服务者卡/六维拆分/SCENARIO_THEME_META 令牌/事件回调——伪装假电话/增项确认/AA 分摊确认/核销 CTA 触发），引入 jsdom 30 devDependency 事件模拟；⑥ 验收：tsc 0 错 + `npm test` **942/942 全绿**（931 +11）+ 收敛门禁 exit 0 | |
| 2026-08-15 | `7240bcf` | **UI/UX 第四层极端状态与特殊人群 UX 兜底策略全量落地**：① 白皮书 §五 5.8 新增三大容灾交互标准——**弱网断网离线态**（灵动胶囊变灰 📴 + 按钮排队文案 + 网络恢复自动追回 Toast）/ **适老化极简模式**（1.4× 字阶 / WCAG AAA 7:1 黑 #000·白 #fff·黄 #ffd60a 三色系 / ≥56×56pt≈75px 巨型热区 / 仅双主按钮：大麦克风语音发单+24h 客服热线 / 关键操作超大确认弹窗）/ **极端危险静默伪装防护**（标准计算器界面掩护 + 真实四则运算 + `911=`/`110=` 暗号静默触发报警零视觉闪烁 + 后台加密录音录像直传安全中心 + 顶栏双击/长按 800ms 紧急脱身）；② 组件落地 `oto-ui/`——`StealthCalculator.tsx`（真实加减乘除运算 computeResult/除零 Error/C 清零 + detectPanicCode 按键流暗号检测 + onTriggerSilentAlarm(audioPayload recordingReady:true) + 长按 800ms/双击脱身）+ `SeniorModeView.tsx`（SENIOR_HOTSPOT_PX=75px 双主按钮 + 波纹麦克风 + 超大确认/取消弹窗 + 1.4x 字阶 AAA 三色系）+ `OfflineQueueIndicator.tsx`（离线琥珀提示条暂存笔数 + true→false 恢复触发绿色追回 Toast 3s 自动消失，修 prevPending 被覆盖真 Bug）；③ 单测 14 用例 `Tier4EdgeCases.test.tsx`（真实运算 12+34=46/9×8=72/除零 Error/暗号 911=·110= 静默触发+载荷完整+零视觉闪烁断言/C 清零/PANIC_CODES/适老双按钮+AAA 样式+热区尺寸+确认弹窗回调/离线条+恢复 Toast 转换），jsdom 事件模拟；④ 验收：tsc 0 错 + `npm test` **956/956 全绿**（942 +14）+ 收敛门禁 exit 0 | |
| 2026-08-15 | `d43a3ed` | **W1~W6 全链路接线通电总装（第一批次）**：① W6 全局容灾——`oto/layout.tsx` 挂 `OnlineStatusBridge`（navigator.onLine + online/offline 事件桥 → OfflineQueueIndicator：断网琥珀条「本地加密队列暂存笔数」/ 联网绿色追回 Toast，取 store.offlineQueue 计数）；`ProfilePage` 安全中心新增「👵 长辈模式」（全屏 SeniorModeView 覆盖 + 大麦克风语音发单 + 24h 热线）与「🛡️ 应急伪装」（StealthCalculator 呼出，911=/110= 静默报警直连 raiseCrisis 红色危机流程，双击/长按脱身）；② W1 发布链路——`PublishSheet` 接入 `DynamicDraftCard` 弹药草稿预览卡（ammoId/计价/安全徽章投影，「扣动扳机·一键发布」= publish 真实发射链路），新增 `resolveAmmoIdForPublish` 中文品类归一化（家政保洁→housekeeping-v1 / 羽毛球约局→meetup-social-v1）并随单写入 `Wave.ammoId`（wave.ts 契约增补可选字段，createWave 透传，零破坏）；③ W2 顶栏状态感知——`oto/page.tsx` HomePage 顶部挂 `StatusCapsule`，取进行中 Wave 经 `toAtomicFiveState` 实时投影五态（🟡广播 ➔ 🔵就位 ➔ 🟣履约 ➔ 🟠待验收 ➔ 🟢已结算），无单不渲染；④ 接线顺带暴露并修复 3 处 base 层预存类型 bug（runner.ts：ISubEventResult 未 import + HookOutcome.fallbackUsed 缺 BLOCK——tsconfig exclude src/base 致其长期未被 tsc 检查，接线拉入编译图后暴露）+ 修复 OfflineQueueIndicator 快速切换时恢复 Toast 覆盖离线条缺陷；⑤ 新增连线测试 10 例（wiring.test 6 + OnlineStatusBridge.test 4：离线/恢复/快速切换状态可逆），**960/960 全绿**（vitest 470 + node:test 490）+ tsc 0 错 + 收敛门禁 exit 0 | |
| 2026-08-15 | d43a3ed | W1/W2/W6 生产接线总装：launchpad（DynamicDraftCard→PublishSheet→ammoId 落库）+ 顶栏五态胶囊 + 全局离线桥 + 长辈/伪装入口；修复 base/runner 3 处预存类型 bug；960/960 全绿 |
| 2026-08-15 | `c2cab31` | **W3~W7 全链路接线通电总装 · 终极闭环（第二批次）**：① W7 争议调解——新建 `ArbitrationSheet` 半屏上滑抽屉：物证比对链（客户投诉诉求 / 履约完工照片 + AI 视觉标注 / 关键聊天记录，数据湖存证锚点）+ AI 小法官建议卡（退款金额 / 平台补偿券 / 责任认定 / 信用扣减 / 理由链，仅 Advisory 红线 1）+ 隔离墙双出口（🤝 接受调解方案 → advanceLifecycle BREACH_SETTLED 载荷流转 SETTLED / 🧑⚖️ 申请人工客服 → 资金冻结进入人工仲裁队列横幅）；② W3 履约座舱挂载——新建 `FulfillmentCenter` 挂载 Trip 屏顶部主视口：进行中 Wave（claimed/locked/assembled + accepted/joined）经 toAtomicFiveState 投影，仅 MATCHED/IN_SERVICE/INSPECTED 三态渲染 FulfillmentCockpit，ammoId（housekeeping-v1 / meetup-social-v1 / companion-v1）毫秒自适应装载家政/组局/陪玩插槽；③ W4 插槽事件接线——保洁现场增项改价确认单 → 确认后订单总额动态更新（budget+增项）；组局 500m 围栏扫码到场验真 → 定金解冻横幅（DELAY 引信放行）；陪玩伪装假电话 → 全屏模拟来电遮罩（接听/挂断脱身）；④ W5 核销 CTA 生产首次调用引擎——「📱 双方碰一碰/扫码确认完工」真实调用 `AmmoRunner.advanceLifecycle(ammo, current, next, ctx)`（MATCHED→IN_SERVICE→INSPECTED→SETTLED 分步核销），流转结果 `setFulfilment` 同步回 useWaveStore（新增 fulfilment 回写位，只增不改）→ toAtomicFiveState 投影 → 顶栏 StatusCapsule 实时流转 🟢 已结算；⑤ 争议入口联动——座舱右上角 ⚖️ 有争议·申诉 + 三插槽申诉入口（损坏直赔/放鸽子申诉/一键拉黑）统一呼出 ArbitrationSheet；registry 新增 `getAmmoById` 反查整弹；⑥ 新增 E2E 装配回归测试 18 例（ArbitrationSheet 6 + scenario/挂载纯函数矩阵 4 + PUBLISHED→MATCHED→Cockpit→核销→SETTLED 端到端状态机同步 4 + 插槽事件接线 4），**978/978 全绿**（vitest 488 + node:test 490，突破 975 目标）+ tsc 0 错 + 收敛门禁 exit 0 | |
| 2026-08-15 | c2cab31 | W3~W7 终极闭环：Trip 屏挂载 FulfillmentCockpit（ammoId→scenario 插槽）+ ArbitrationSheet 争议半屏抽屉 + advanceLifecycle 生产首次调用（核销/调解双流转）+ setFulfilment 回写胶囊实时流转；978/978 全绿 |
| 2026-08-15 | `companion-v1` | **第三枚官方标杆弹药 companion-v1 · 三大标杆大满贯（Phase 3 收官）**：① 新建 `src/ammo/companion.ammo.ts`——纯 📡 近炸引信（PROXIMITY_FUZE_TEMPLATE 直挂：STANDARD 背调 + 虚拟号掩码 + 模糊定位 + AI 敏感词干预 + 一键 SOS 四联动）；计价 HOURLY（默认 ¥100/h 起步 1 小时，超时系数 1.2）；② PrivacyShieldHook（IN_SERVICE 前置 BLOCK）：虚拟号绑定 / 实时行程守护 / 敏感词监听三闸校验，未就绪阻止进入服务（宪法 #4/#8）；③ DepartureFinishHook（SETTLED 后置 SKIP）：核验双方离开 300m 安全距离（CompanionSlot 默认 300m 契约闭环）→ 自动结账停表（含超时 ×1.2）→ 信用分奖励载荷（L2-M6 飞轮）；④ 注册表正式挂载：OFFICIAL_AMMO 新增 companion/dating/escort 三键 + CATEGORY_TO_OFFICIAL 新增陪玩/交友/约会；**契约修订：dating 类目自 meetup 迁移至 companion**（同人风险归近炸引信，meetup.ammo.test.ts 断言同步修正）；⑤ 新增 18 例 node:test 白名单注册（装备完整性/近炸引信核验/双钩子执行/五态全流程/违约终止/注册表全键），**996/996 全绿**（vitest 488 + node:test 508）+ tsc 0 + 门禁 exit 0 | |
| 2026-08-15 | e6c6ef | companion-v1 大满贯：三大标杆弹药（家政/组局/陪玩）注册表全量挂载；996/996 全绿 + tsc 0 + 门禁 exit 0 |
| 2026-08-15 | 资金引擎深水区大收敛 | **L2-M4 账户清结算 100% 闭环（1024/1024 全绿）**：① `src/base/money/escrow.ts` 统一资金托管引擎（六模式托管 / 三阶段阶梯退款+违约罚金 / AA 多方分账 / 资金安全底线 / release 放款收敛）——提炼 m13-payment 与 api/payment/release 内联实现，成为全仓唯一资金托管计算权威真理源；② AmmoRunner 五态资金挂接：MATCHED 托管校验（余额安全底线 BLOCK）+ SETTLED 清结算对账清单（四大弹药统一产出）；③ api/payment/release 收敛调统一引擎（响应契约不变）；④ 28 例新单测，全仓单测正式突破 1000+（vitest 488 + node:test 536 = 1024）+ tsc 0 + 门禁 exit 0 | |
| 2026-08-15 | db04491 | L2-M4 资金引擎大收敛：escrow.ts 统一底座 + AmmoRunner 五态资金挂接 + api/payment 收敛；1024/1024 全绿 + tsc 0 + 门禁 exit 0 |
| 2026-08-15 | L4-M3 物理履约闭环 · 阶段三 | **零空白项大圆满（1061/1061 全绿）**：① `base/geo/geofence-watcher.ts`——50m 高精 GPS 围栏（Haversine + 精度漂移过滤 + 停留时长防刷 + 300m 陪玩脱离判定）；② `base/platform/nfc-adapter.ts`——Web NFC 碰碰（HMAC 防重放 + 动态码降级）；③ `base/platform/rpc-client.ts`——6 大 Supabase 原子 RPC 通道 + Mock 降级；④ ArbitrationSheet 司法存证包导出（SHA-256 审计证书）；⑤ 白皮书 L4-M3/L1-M2/L1-M3 升 🟢，**全仓 28 模块 0 个 ⚪️**；⑥ 37 例新单测，**1061/1061 全绿** + tsc 0 + 门禁 exit 0 | |
| 2026-08-15 | dd895dd | L4-M3 物理履约闭环 + 28 模块零空白：geofence/nfc/rpc 三底座 + 司法导出；1061/1061 全绿 + tsc 0 + 门禁 exit 0 |
| 2026-08-15 | 代码洁癖级大扫除（阶段四） | **Lint 治理 100% 纯净**：① 白名单核心目录（src/base / src/ammo / src/types / components/waves / components/oto-ui）ESLint 警告清零（全仓 744 → 739，白名单 6 → 0）；② 类型加固：StealthCalculator 新增 buildSilentAlarmPayload 模块级工厂（impure 边界封口）；③ HousekeepingSlot 两张照片位 img → next/image（Image fill + position:relative，alt 语义保留）；④ 未用变量真实清理（SeniorModeView onVoiceEnd / JudgePanel claimId）；⑤ 修复 nfc-adapter 测试时序脆弱（注入 now 时钟参数）；⑥ **1061/1061 全绿** + tsc 0 + lint 0 error + 门禁 exit 0 | |
| 2026-08-15 | 3c242c3 | 代码洁癖大扫除：白名单目录 lint 清零 + 类型加固 + img→next/image；1061/1061 全绿 + tsc 0 + 门禁 exit 0 |
| 2026-08-15 | 1510d05 | 4 阶段总攻战役收官：LAST_SYNC 对齐最终 HEAD；工作区干净，1061/1061 全绿基线确认 |
| 2026-08-15 | `40b253e` | **全仓资产与技术债全景台账建档（纯文档批次）**：新建 `docs/DEBT_CLEANUP_REGISTER.md`（全仓资产与技术债单一事实源）——99 个 API 路由逐行全量建档（65 生产活跃 + 26 孤儿 A1-A26 + 9 基础设施 B1-B9）、126 个生产组件（110 在用 + 16 待审 C1-C16）、14 个模块目录（m02-m14 + mM 镜像，m09/m13 冗余待裁）、3 个垂直协议双轨（base/housekeeping/dating ↔ ammo）与 7 项级联工具库成对台账（L1-L7）、分批出清路线图（Batch 0-4）；零代码变更，1061/1061 基线不受影响；物理出清待创始人裁决后按 Batch 指令执行 |
| 2026-08-15 | `680bce8` | **5 大集群物理出清（创始人裁决执行）+ 台账全量销项**：`git rm` 删除 47 个文件——集群 1 早期 AI 淘汰（llm-test / ai/* / 3 级联 lib / SmartRequest 等 10 项）、集群 2 旧争议全家桶（DisputeModal/ReviewModal/AIArbitrationCard/PeerJuryPanel 等 9 项）、集群 3 CRUD 碎片（demands/orders 重复子路由、profile/delete、pricing/estimate、admin/disputes 旧版、protocols 根+[id] 等 16 项）、集群 4 老旧父组件与 .gitkeep（RealtimeChat/NotificationCenter/VoiceInput 等 11 项）、集群 5 孤儿模块（m09-content-audit、m13-payment 2 项）；空目录清理（llm-test/predict-intent/llm-classify/m09/m13 等 13 个）；**测试同步清理**：m09×2 测试文件删除 + 5 文件 17 断言失效块删除（e2e-integration 资金段收敛至 escrow 引擎）；免死金牌全数保留（cron×3/webhook×3/gateway/health/MapComponent/老控制台 4 件套/mM02-mM13/协议双轨/v1-agent）；台账 Batch 1-3 勾销 45 项、A14 核实从未存在、A15/A21 保留观察；门禁：tsc 0 错 + `npm test` **1042/1042 全绿**（1061 - 19 失效测试）+ 收敛门禁 exit 0 |
| 2026-08-15 | `707b83f` | **全栈无损吸收与商业防脆弱升级（S1~S4 闭环）**：契约层 mmo-schema.ts 增补 IWorkerRequirement（S1 准入三闸：实名/安全分/证书）+ ICreditWaiverRule（定向信用折抵闸门）+ maxSurchargeRatio（默认 0.5）；新建 ase/safe/runtime-monitor.ts（S3 SAFE_MONITOR 三信号聚合器：LBS 围栏漂移/敏感词/虚拟号 → GUARDED/ATTENTION/THREAT 三态，纯函数红线 1）+ 9 项单测；scrow.ts 落地 generateComplianceSplitInstruction（S4 防二清：服务商商户号 + 幂等指令号 + 金额守恒）+ 4 项单测，buildSettlementLedger 可选挂载（缺省不产出零回归）；unner.ts 植入 50% 防坐地起价熔断（ANTI_GOUGING_LIMIT_EXCEEDED，基准价取 escrowPayload.amount 或 baseAmountYuan）+ 6 项单测；弹药装配：housekeeping 三件套 + meetup 守时折抵；前端连线：HousekeepingSlot 50% 上限提示卡 / FulfillmentCockpit 安全守护徽标（describeSafetyPill）/ WorkerWorkbench R_AUTH 资质拦截（evaluateWorkerQualification）；白皮书新增 §六 商业战略映射与防脆弱工程论证（原六/七顺延）；门禁：tsc 0 + node:test 592 + vitest 469 = **1061/1061 全绿**（1042 基线 + 19 新增）+ 收敛门禁 exit 0 |
| 2026-08-15 | `2f87043` | **5 大商业与法律合规漏洞 1:1 闭环落地**：V1 插件微状态（ISubEventHook 微工作流契约固化）+ V2 三维信用（新建 ase/trust/tri-credit.ts BCS/PQS/ESF 引擎：强合规一票熔断/技能类目隔离/定向押金折抵，16 项单测）+ V3 运力聚类（SupplyCluster 契约 + housekeeping C2_IN_HOME / meetup C1 / companion C1 装配）+ V4 资金合规（escrow 二级虚拟子账户 masterAccountId/providerSubWalletId + djb2 确定性签名 + isMirrorLedgerOnly 镜像声明，3 项单测）+ V5 三级仲裁（ArbitrationSheet resolveArbitrationLevel：L1 ≤30 秒赔零扣罚 / L2 AI+人工双轨 / L3 >500 或红色报警法务直通，21 项单测）；白皮书 §六 6.5 实现对照表；门禁：tsc 0 + vitest 488 + node:test 612 = **1100/1100 全绿**（1062 基线 + 38 新增）+ 收敛门禁 exit 0 | |
| 2026-08-15 | `ea27089` | **商业路演母档建档（纯文档批次）**：新建 `docs/business/PITCH_DECK.md`（Pitch Deck Master，与白皮书物理隔离）——10 页幻灯片母稿全量归档（① 封面·通用操作系统 ② 烟囱式陷阱 ③ 万能底座与积木式扩张 ④ 试错成本 90 天→3 天 ⑤ 跨场景数字资产飞轮 ⑥ 极轻人力运营·客诉 -75% ⑦ 合规护城河·零二清+哈希链存证+场景险 ⑧ 四层变现矩阵 ⑨ 12 周三阶段落地 ⑩ 投资价值三底线），每页含宣讲完整稿/核心金句/视觉建议三要素；投资人 3 大核心拷问攻防底牌（Q1 冷启动→运力聚类 70% 复用+B2B2C 影子供给 / Q2 巨头为何不做→部门墙 P&L+轻人力 vs 重佣金模式对立 / Q3 信用飞轮道德风险→BCS/PQS/ESF 三维解耦·ESF 一票熔断·社交信用绝不越权），含支撑事实（回指白皮书 §六 与 tri-credit 16 项单测等代码落点）与最坏情况退守话术；零代码变更，1100/1100 基线不受影响 | |
| 2026-08-15 | `1c7f2a8` | **MVP 系统设计与执行 SOP 100% 物理代码级闭环**：① 新建 `supabase/migrations/20260815_mvp_core_tables.sql`——4 张核心标准表完整 DDL（orders：六态 + CAS 乐观锁 version + 金额分单位 chk_amounts 守恒 / order_state_logs：状态机审计轨迹含 version_at_trans 版本快照 / pricing_configs：版本化计价 + uniq_cat_active_version 部分唯一索引（同类目同版本码仅一 ACTIVE）/ split_records：分账执行台账 + uniq_split_out_order 幂等唯一（防重复分账））；② `runner.ts` advanceLifecycle 植入 CAS 乐观锁——currentVersion/expectedVersion 双载比对，不等即 BLOCK（OPTIMISTIC_LOCK_VERSION_CONFLICT）阻断跃迁（含终止路径），成功回写 nextVersion = version + 1，未传版本号跳过校验（缺省兼容零回归）；③ `escrow.ts` calculateSplitRetrySchedule 指数退避——1/5/15/60/120 分钟阶梯（SPLIT_RETRY_LADDER_MINUTES 常量），上限 5 次（SPLIT_RETRY_MAX_ATTEMPTS），第 6 次起 shouldAbandon + isP0AlertTriggered（P0 财务告警转人工，不无限重试），时间注入纯函数；④ 契约层 `ammo-schema.ts` 增补——ISubEventContext.currentVersion/expectedVersion + ISubEventResult.nextVersion + IAmmoDefinition.autoAcceptanceTimeoutHours（超时自动代验收契约）+ 导出 ISplitRetrySchedule；⑤ `housekeeping.ammo.ts` 显式装配 autoAcceptanceTimeoutHours: 24（24h 自动代验收固化）；⑥ 白皮书 §七 新增「MVP 系统设计与工程执行 SOP」——阶段 1~4 端到端 Mermaid 时序图（网关→状态机→LBS→OSS→微信收付通）+ 4 表 DDL 约束规范 + 5 个 Sprint（10 周）WBS/DoD + 6 大核心链路故障防范矩阵（LBS 宕机/重复 Webhook/弱网离线/分账失败重试/一键 SOS/超时自动代验收），原 §七→§八、§八→§九 顺延；⑦ 新增 11 项单测（runner CAS 5 + escrow 退避 6：版本匹配/冲突/缺省兼容/终止路径/版本透传/阶梯 1~6 全分支/防御钳制）；门禁：tsc 0 错 + `npm test` **1111/1111 全绿**（vitest 488 + node:test 623，1100 基线 +11）+ 收敛门禁 exit 0 | |
| 2026-08-16 | `cd56d6c` | **PWA Native-Like UI/UX 架构 100% 物理代码级闭环（白皮书 §八）**：① `src/base/platform/watermark-canvas.ts` Canvas 时空防伪水印引擎（时间/坐标/订单哈希格式化 + 4:3 中心裁剪 + 右下角遮罩 + SHA-256 存证指纹 + 无 DOM 确定性降级红线 5）；② `src/base/platform/useEdgeSwipeBack.ts` 左边缘手势返回（24px 边缘带 / 60px 阈值 / 1.5 垂直比纯函数 + touch Hook passive 抢占 + history.back 回退）；③ 三组件：`PrePermissionSheet.tsx`（GEOLOCATION 200m 围栏语义 / CAMERA 防伪物证链 + 永久拒绝「锁形图标」重置指引 + 48px 触控）、`A2HSPrompt.tsx`（beforeinstallprompt 捕获延迟到价值时刻 + Android prompt() + iOS Safari 分享气泡）、`controls/ProofCamera.tsx`（capture=environment 禁相册 + 自动水印注入 + SHA-256 标签）；④ 白皮书新增 §八（原八→九、九→十顺延）；⑤ 新增 50 项双端单测（node:test 28 + vitest jsdom 22，测试清单挂载 `test:oto:units`）；门禁：tsc 0 错 + `npm test` **1161/1161 全绿**（vitest 510 + node:test 651，1111 基线 +50） | |
| 2026-08-16 | `6e546ab` | **PWA Handoff 与 Design QA 规范（白皮书 §九）**：① `src/app/oto/globals.css` 注入 1:1 Design Token 字典（`:root` + `.oto-app` 双写：色彩 9 / 间距 6 / 触控 44·48px / 圆角 / 阴影 / 动效，Figma 变量 1:1 映射）+ `.interactive-tap` 全局原生触控类（min 44px / tap-highlight 透明 / touch-action manipulation / 150ms 按压反馈 scale(.97)+opacity(.85)）；② `src/app/layout.tsx` 导出 Next 16 viewport（userScalable=false + maximumScale=1 + viewportFit=cover + body WebkitTapHighlightColor 透明消除点击高亮）；③ `A2HSPrompt.tsx` 7 天防骚扰静默期（`a2hs_dismissed_until` localStorage + `isA2HSSuppressed`/`suppressA2HS` 纯函数，showInstallPrompt 前置拦截，存储异常静默降级不阻断挂载）；④ 新建 `src/base/platform/useDragToDismiss.ts` 半屏抽屉下拉关闭（`shouldDismissSheet` 纯函数：仅向下滑动且位移/容器高 >35% 触发（=35% 不触发）、向上/零位移/非法高度防御性拒绝、未绑定容器回退 400px 基准 + touch 三事件 passive Hook + enabled 开关）；⑤ 白皮书新增 §九 Design QA 与 PWA Handoff（Sprint -0.5 双轨 W1~W10 里程碑 / Figma 变量→CSS Token 1:1 映射字典 / Figma 图层→Atomic 组件映射 / Lighthouse 硬性基准 LCP≤2.0s CLS≤0.05 a11y≥95 / A2HS 7 天 + 抽屉 >35% 关闭验收标准），原 §九→§十、§十→§十一 顺延；⑥ 新增 19 项双端单测（node:test 9 阈值矩阵 + vitest 10 静默期/手势集成）；门禁：tsc 0 错 + `npm test` **1180/1180 全绿**（vitest 520 + node:test 660，1161 基线 +19）+ 收敛门禁 exit 0 | |
| 2026-08-16 | `add2f4c` | **CSS 注释省略号字面量清理（审计性合规）**：`src/app/oto/globals.css` Token 字典注释中列举式省略号改为明确族名列举（Color 族 / Spacing 族 / Radius 族 / Shadow 族 / Motion 族）；全仓复核：无非法 `env(...)` 语法、无 Tailwind 任意值类占位符；1180/1180 基线不变 | |
| 2026-08-16 | `5d652a2` | **PostCSS 解析报错根治（Tailwind v4 扫描白名单 + manifest 冲突清理）**：① 根因——`@tailwindcss/postcss` 自动内容检测扫描全部非忽略文件（含 docs/），把字面量提取为非法任意值类名编译 → `Unexpected token Delim('.')`；② 治本——`src/app/globals.css` 与 `src/app/oto/globals.css` 双入口 `@import "tailwindcss" source(none)` 彻底禁用自动检测 + 显式 `@source src/` UI 源码白名单；③ 防再犯——docs 历史记录字面量全角化（`…`）；④ 顺带根治既有错误配置 `conflicting-public-file-page`：删除 `public/manifest.webmanifest`（与 `src/app/manifest.ts` 动态路由同路径冲突 500），manifest 恢复 200；⑤ 验证——删 `.next` 全新 build exit 0、产物非法类名归零、`next dev` + Chrome CDP 实测 `/` 与 `/oto` 均 200 零错误；1180/1180 全绿 + 收敛门禁 exit 0 | |
| 2026-08-16 | `1ec5018` | **路由置换与 OTO 主力产品全面转正**：① `src/app/oto/` → `src/app/(oto)/` 路由组（git mv 3 文件，路由组不占 URL 段）——OTO 5 屏空间应用**直接呈现于根路径 `/`**（Spatial OTO Platform metadata / `.oto-app` 容器 / ToastHost·PwaServiceWorker·OnlineStatusBridge 随组生效）；② 老协议门户 `src/app/page.tsx` → `src/app/dp/page.tsx`——完整保留至 **`/dp`**（页面内「首页」链接同步归位 /dp，Hero/悬赏大厅/我的契约/玩家中心入口零丢失）；③ `next.config.ts` 增 `redirects`：**`/oto` → `/` 307**（permanent:false，老路径兼容不断链）；④ `Header.tsx` 导航适配——桌面+移动双端：「OTO空间」指向 `/`（主站）、新增「协议后台」指向 `/dp`，悬赏大厅/我的契约/控制面板/管理后台保留；Header 隐藏面移除失效的 `/oto` 前缀判断；⑤ 验证——tsc 0 错 + `npm test` **1180/1180 全绿** + 删 `.next` 全新 build exit 0（产物含 /dp）+ `next dev` 冷启动 Chrome CDP 实测：`/` 200 渲染 OTO（.oto-app + Hello Alex 空间 + 雷达/履约舱壳）、`/dp` 200 渲染老协议门户（新导航两入口就位）、`/oto` **307 → Location: /** | |
| 2026-08-16 | `929c20d` | **控制台零红：Hydration Mismatch 与 Supabase P2P 404 双根治**：① Hydration 根因实锤（CDP 组件栈 + diff：SSR 多渲染 `oq-banner` 离线条）——`OnlineStatusBridge.tsx` 状态初始化用 `typeof navigator !== "undefined"` 守卫在 **Node 22 SSR 下失效**（Node 全局 navigator 存在但 `onLine` 为 undefined，`!undefined === true` → SSR 渲染"离线模式"琥珀条、客户端在线不渲染 → mismatch）；治本——初始状态统一 `true`（首帧在线）、真实网络状态移入 `useEffect` 挂载后接管（`typeof window !== "undefined" && navigator.onLine`），首屏 HTML 完全匹配；② Supabase P2P 根因——`src/base/platform/p2p/supabase.ts` `write()` 的 `.then` 内 `console.error("[p2p] supabase upsert failed")` 在云端表未建（PGRST205/404）时直接抛红，且 Realtime 订阅失败无兜底；治本——新增 `isMissingTable` 判定（PGRST205/404/relation/表名字面量）+ `degrade()` 幂等降级（退订 Realtime 通道 → `createLocalTransport()` 本地通道接盘 → 既有监听器转发 storage 事件），pull/write/订阅三路全部静默吞错（红线 5 确定性降级），写路径不再打印任何 console.error；③ 验证——tsc 0 错 + `npm test` **1180/1180 全绿**（vitest 520 + node:test 660）+ 删 `.next` 全新 build exit 0 + `next dev` 冷启动 Chrome CDP 实测：`/` 打开 **0 exception（0 Hydration）0 console-error**，p2p_broadcast 仅 1 次首次探测 404（触发降级后零重发）、`/dp` 回归 0 错误、`/` 二次刷新 0 错误稳定 | |
| 2026-08-16 | `18df535` | **P2 战役：PWA 原生外骨骼与手势全装配（白皮书 §八/§九 总装收口）**：① A2HS 全局价值时刻外骨骼——新建 `A2HSPromptHost.tsx` 挂载 `(oto)/layout.tsx`，订阅 useWaveStore 真实数据（fulfilment 首次 isSettled → FIRST_ORDER_COMPLETED「首单结算」；claims 首条 accepted/joined 且归属我 → PROVIDER_VERIFIED「服务者上岗准入」），firedRef 每里程碑仅触发一次，showInstallPrompt 内置 7 天 localStorage 静默期（冷启动绝不弹，防骚扰铁律）；`A2HSPrompt.tsx` 新增 onPromptChange 可见性回调；② 屏幕左边缘滑动返回——`page.tsx` Home 根挂 `useEdgeSwipeBack`（enabled = 非 Home 屏且全局互斥锁未锁），onSwipeBack → setScreen("home") 平滑回主屏；新建 `edgeGestureLock.ts` 全局单例锁（useSyncExternalStore + getServerSnapshot SSR 安全）：AR 全屏相机 photoOpen / 扫码 scanOpen / 心愿单 / 目的地中心 / A2HS 引导卡可见 全部互斥禁用（手势无冲突保证）；③ 抽屉下拉 >35% 平滑收起——`PublishSheet` 加拖拽把手（data-testid=publish-drag-handle）+ `ArbitrationSheet` grip 绑定 `useDragToDismiss(enabled=open)`（enabled 兼作 open 时重绑到已挂载把手），dismissing 过渡态（motion 下滑离场 210ms / CSS transition 200ms）后 onClose，重开清零残留；④ 测试——既有 `PwaNativeUx.test.tsx`（31 用例）并入追加 7 个 P2 用例（Host 冷启动不渲染 / 首次结算触发+锁手势 / 锁订阅反射 / firedRef 去重 / 静默期不触发 / 服务者占座触发 / lock 幂等）；门禁：tsc 0 错 + `npm test` **1197/1197 全绿**（vitest 537 + node:test 660，基线 +6）+ 删 `.next` 全新 build exit 0（prerender static）+ `next dev` 冷启动 Chrome CDP touch 手势实测：冷启动 0 A2HS 卡（防骚扰 ✓）→ 打开发布抽屉 → 拖拽把手下拉 180px（>35%）**平滑收起关闭** ✓ → 进 AI 屏 → 左边缘（x=5）右滑 140px **平滑回 Home** ✓ → 小位移（<35%）不误关（base 单测兜底）✓；整页加载 0 exception 0 console-error，唯一既有噪音为 three 上游 `THREE.Clock deprecated` 一次性 deprecation 警告（非本战役引入，功能零影响，上报待裁决） | |
| 2026-08-16 | `eb3e3c8` | **P1 战役：AI 对话直通弹药发单闭环 + 争议物证链真实化（动态数据装配）**：① ChatPage 转单闭环——success 卡新增【📡 转为正式订单】按钮（human-in-the-loop，红线 1），一键走 PublishSheet 同款 `createPendingWave` 校验链（bans / no-show 违约 / 设备风险 roam / sentinel 反欺诈四闸门）+ `payWave` 资金托管 → Wave 入 ACTIVE 广播，顶部 StatusCapsule 五态流转；对话卡回写「弹药单号」幂等防重复落库；确认卡不再过滤「服务」行（保真服务类目映射）；② FulfillmentCenter 争议物证链真实化——拔除静态演示桩 DISPUTE_EVIDENCE/DISPUTE_PROPOSAL，改由 `buildDisputeEvidence`（品类+金额+negotiableNote 诉求+customs 验收约定+已上传存证照片）与 `buildDisputeProposal`（确定性比例规则：≤30 全退 / 30% 折算，驱动 ArbitrationSheet 三级分流，红线 1 合规）动态装配；③ AR 证据跨屏上链——ProofCamera 拍摄结果从 AR 屏状态提升至根视图（proofShots），经 TripPage 传入 FulfillmentCenter 进入争议照片物证链（修复照片只活在 AR 屏的断线）；④ 测试——FulfillmentE2EIntegration 追加 P1 动态物证链 5 断言（金额/诉求/照片来自活动 Wave、旧桩文案消失、纯函数矩阵）+ 新建 ChatPage.test 3 用例（转单落库 ammoId=housekeeping-v1 + status=active + 切 home + 幂等防重）；门禁：tsc 0 错 + `npm test` **1191/1191 全绿**（vitest 531 + node:test 660，1183 基线 +8）+ 删 `.next` build exit 0 + `next dev` Chrome CDP 实测：AI 多轮对话（单次保洁/浦东新区/200 元）→ 时段卡 → 服务者卡（陈阿姨/小张）→ 确认预订 → **转为正式订单 → toast「已转为正式弹药订单」+ 切 home 胶囊 📡 + Trip 屏「家政保洁 · 广播中」波卡 +「我的预订 · 小张 ¥120/次」入行程**，0 exception 0 console-error；争议调解链浏览器实测受环境限制（本地 Supabase REST 不可达、无第二身份可接单 → 座舱需 accepted claim 才挂载），以 26/26 集成测试受控覆盖 | |
| 2026-08-16 | `d4c7b23` | **8 维全息解构模型 + AmmoFactory 工业级弹药流水线**：① 契约层——`src/types/ammo-schema.ts` 增补 8 维全息契约 `IHolographicAmmoConfig`（D1 供给准入 supplyCluster/workerRequirement · D2 计价与护栏 pricingModel/pricingParams/minFloorPrice/maxCeilingPrice/creditWaiverRule/maxSurchargeRatio · D3 引信 fuzePolicy · D4 传感降级 requiredSensors/sensorFallbackLadder · D5 正向钩子 forwardHooks · D6 逆向违约阶梯 `ICancellationTier`（stage × 退款比例 × 车马费 × 扣金比例）· D7 清算仲裁 splitRules（`ISplitRules` 三比）/autoAcceptanceTimeoutHours · D8 视界表单 theme/formSchema/cockpitSlot + version 元数据）+ `SensorKind`/`SensorFallbackLadder`/`PricingParams` + `IWorkerRequirement.isPoliceVerified` + `ISubEventContext.ammoSnapshot` + `IAmmoDefinition.holographic` 镜像（宪法 #2 只增补）；② 装配引擎——新建 `src/ammo/factory.ts` 四道工序流水线（红线 1 零 eval）：`validateAmmoConfig` 静态语义审查（**资金守恒 =1.0 容差 1e-9 硬校验** / **C2_IN_HOME 入户一票否决（公安背调或安全分 ≥700）** / 加价熔断 ≤0.5 / 地板价≤天花板价 / 违约阶梯边界 / 钩子名静态白名单）+ `HOOK_OPERATOR_REGISTRY` 6 算子（ArrivalCheck/CleaningCheck/OnsiteQuote/AASplitSettle/PrivacyShield/DepartureFinish 全部已编译纯函数）+ `assembleAmmo` 沙箱组装（deepFreeze 全图冻结不可变发布）+ `registerDynamicAmmo` 运行时热注册；③ 注册表连通——`src/ammo/registry.ts` 导出 `DYNAMIC_AMMO_POOL`，getAmmoDefinition/getAmmoById 检索链升级「动态池→官方→四表聚合→默认保底」（未命中回落零回归）；④ 快照冻结——`src/base/ammo/runner.ts` `AdvanceInput.ammoSnapshot` 快照优先调度（矩阵/钩子/熔断/资金全链基于快照，ammo 仅缺省回落）+ `ISubEventContext.ammoSnapshot` 透传钩子闭包——**在途订单免疫线上弹药热更新**；⑤ 白皮书 §十 归档（8 维字典+正交关系 / 4 级工序 / 6 算子 / car-wash-v1 + appliance-repair-v1 声明式样例，原 §十→§十一、§十一→§十二 顺延）；⑥ 单测——新建 `src/ammo/factory.test.ts` 12 用例（合法出厂+全图冻结 / 资金不守恒 / 浮点容差 1e-9 / 入户一票否决×4 / 加价熔断 / 计价边界 / 未知钩子+白名单枚举 / 违约阶梯越界 / 版本守卫 / 引信显式装填守卫 / **热注册即时生效+审查不过不入池** / **快照冻结免疫热更新+ctx 透传**）注册 `test:oto:units`；门禁：tsc 0 错 + `npm test` **1210/1210 全绿**（vitest 537 + node:test 673，本次 factory 矩阵 +12，node:test 661 → 673）+ 收敛门禁 exit 0 | |
| 2026-08-16 | `db13baf` | **三大标杆弹药 8D 全息化流水线出厂（方案 A 收官）**：① 三枚官方弹药（`housekeeping.ammo.ts` / `meetup.ammo.ts` / `companion.ammo.ts`）重构为 8 维全息配置（D1 C2_IN_HOME 入户公安核验一票否决 / C1_MOBILITY 移动轻履约；D2 计价护栏 HOURLY¥60·起步2h / PER_SEAT¥80·2人 / HOURLY¥100·起步1h超时×1.2 + 地板天花板 + 加价熔断 ≤0.5 + 信用折抵；D3 碰炸 / 双引信并联 / 纯近炸；D4 GPS+水印 / GPS+NFC / GPS+录音 + 降级阶梯；D5 算子钩子白名单；D6 违约阶梯 4/2/2 档；D7 分账三比 0.85·0.10·0.05 / 0.88·0.10·0.02 / 0.85·0.12·0.03 守恒 + 超时 24h/6h/2h；D8 theme+cockpitSlot），经 `assembleAmmo` 模块加载期强制门禁出厂（任一不合格抛错拒出厂），`dispatchRule`/`sop` 存量字段显式 spread 保留（入户实名关键词硬门槛与发布页 SOP 预填零丢失），`deepFreeze` 全图冻结发布；② 契约增补——`SensorKind` +5 降级成员（CELL_TOWER_COARSE_GEO / MANUAL_BASE_PHOTO_AUDIT / HTML5_NATIVE_FALLBACK / QR_SCAN_VERIFICATION / PROXIMITY_DEPARTURE_MANUAL_CHECK，创始人裁决）；③ 依赖收敛——`factory.ts` 本地定义 `DYNAMIC_AMMO_POOL` + `registry.ts` re-export，打断 factory→registry→ammo→factory ESM 循环（TDZ 根因），依赖图无环 DAG；④ 钩子契约——引擎级五态流转由算子接管（operator.onsite-quote/cleaning-check/arrival-check/aa-split-settle/privacy-shield/departure-finish），存量富钩子保留导出直测（AA 多退少补 / 违约金归守约方 / 300m 停表计费 220/280 元语义零丢失）；⑤ minSafetyScore 双量表裁决——恪守 base 0-100 契约（tri-credit ESF/PQS 门槛），housekeeping 60 + isPoliceVerified:true（factory policeOk 通道）、companion 65，base 层零改动；⑥ 测试同步——三弹药测试文件 8D 契约断言（装备完整性 / 全息字段 / 算子载荷 + 富钩子直测迁移），base 层 tri-credit/escrow/runner 全部零改动保持绿；门禁：tsc 0 错 + `npm test` **1217/1217 全绿**（vitest 537 + node:test 680，node:test 673 → 680）+ 删 `.next` 全新 build exit 0 + 收敛门禁 exit 0 | |
| 2026-08-17 | `1d76763` | **阶段1 拟人全链路大考（"我需要10点来人打扫房间"双域考卷收官）**：引擎域 17 例（src/ammo/real-user-sim-basic.test.ts：注册表/意图/MockEngine 口语/草稿卡数据源/引信双闸/SHA-256/双拍验收/AIGC 伪图阻断/增项报价/引擎对账/D7 三比守恒/收付通指令/五态投影/let 守护/15s 窗口）+ Store 域 5 例（real-user-sim-store.test.tsx：发布→托管 12000 分→王姐 MATCHED→座舱→增项 170→INSPECTED→SETTLED）；白名单接线 --experimental-transform-types + scripts/node-ts-loader.mjs（基线零行为变化）；6 断线点/观察项如实上报（词表缺口×2/钩子 SKIP 宽容/90·10 vs D7 85·10·5 差 ¥8.50/草稿卡 minHours 未消费/登录 Supabase 依赖）；浏览器 CDP 实测 AI 对话全链路全通；1414/1414 全绿 + tsc 0 + eslint 0 error + 收敛门禁 exit 0 |
| 2026-08-17 | `5cc281d` | **阶段1 拟人大考 4 缺陷定向修复**：voiceIntent 口语词表+时间精确化 / runner 缺省三比分账（insuranceFee 入账，裂隙 ¥8.50 消除）/ DynamicDraftCard 全息优先定价 / esc-4·esc-5 断言对齐弹药契约（人类裁决）；**1415/1415 全绿**（node:test 831→832 +1，vitest 583 不动）+ tsc 0 + build exit 0 + 收敛门禁 exit 0（CONVERGENCE-LOG 已登记） | 已推 origin/master |
| 2026-08-17 | `0b34faf` | **阶段2 敏感定制边界拟人对抗实测**：19 例考卷（src/ammo/real-user-sim-custom-boundary.test.ts）+ test:oto:units 白名单 +1 文件；4 大防线真实判定实证 + 4 盲区如实上报（女仆装词表漏检/定制属性零契约位/准入无年龄维度/涉敏不触发引信升级），零生产代码改动；浏览器 CDP 重启 dev 后实测（涉敏单发布进市场未被下架 D5D54230，0 异常 0 Hydration）；**1434/1434 全绿**（node:test 832→851 +19）+ tsc 0 + build exit 0 + 收敛门禁 exit 0 | 已推 origin/master |
| 2026-08-17 | `0b34faf` | **阶段3 非标定制语义驯化与全链路动态风控闭环**：新建 intent-normalizer.ts + 测试（8 例）、ammo-schema 契约（ICustomRequirements/INormalizedCustomIntent + 全链可选透传）、voiceIntent 驯化接入、tri-credit AGE_MISMATCH 硬门禁、WorkerWorkbench 年龄门禁 UI、runtime-monitor 多因子评分 + PROXIMITY_ENHANCED 升级（新 4 例）、阶段2 考卷 B5/B9 翻转为修复实证 + B20-23；**1450/1450 全绿**（node:test 851→867 +16）+ tsc 0 + build exit 0 + 收敛门禁 exit 0 | 已推 origin/master |
| 2026-08-18 | `c2afad6` | **阶段4 方案 A：Wave 实体强类型契约扩列与履约座舱 UI 全量闭环**：① base/order/wave.ts Wave+CreateWaveInput 增可选 customRequirements 契约（宪法 #2 增补不改义，createWave 透传缺省 undefined 零破坏）；② 发单端贯通（ChatPage 语音意图 ref 缓存注入 / PublishSheet note 驯化注单）；③ 座舱消费（FulfillmentCenter evaluateRuntimeSafety + forceArmed/safetyBadge 注入 ｜ FulfillmentCockpit 强化安全守护条 + describeCustomRequirementTags 中性标签栏 ｜ HousekeepingSlot/DynamicAmmoSlot 参数快照区结构化标签卡）；④ FulfillmentCockpit.test.tsx +7 例；**1458/1458 全绿**（vitest 583→591 +8，node:test 867 不动）+ tsc 0 + build exit 0 + 收敛门禁 exit 0（CONVERGENCE-LOG 条文 #2 已登记） | 已推 origin/master |
| 2026-08-18 | c2afad6（+工作区未提交）| **E2E 全量 12/12 闭环 + 离线导航/履约流稳健性修复**：① sw.ts 离线导航根因（离线时导航响应链 fetch 挂死不 reject、AbortSignal 无法中止）→ navigator.onLine 判定 + 离线绝不发 fetch、在线 fetch 失败落缓存兜底，e2e-offline 8s PASS；② supabase.ts createSupabaseTransport 8s fetch 超时（云端不可达快速 degrade）；③ MyWaves/MyClaims 履约流渲染条件改为 claims 事实源（wave.status 镜像会被远端旧快照覆盖回退 → active + accepted claim 并存时验收/申报卡缺失，E2E flaky 根因）；④ 测试脚本加固（等待加长 10→20/25s、认证后 reload、dock 就绪等待、store dump 诊断）；⑤ 生产 E2E 回归 12/12 全绿（acceptance/app/fulfil/governance/match/offline/openmatch/push/review/trust/trust-open/wave）；门禁：tsc 0 错 + build exit 0 + 收敛门禁 exit 0 | |
| 2026-08-18 | 2df9bdf（+工作区未提交）| **P0-2 隔离墙闭合：核心 API 路由收编 base 确定性引擎**：① orders/[id] 争议分账 4 处 amount*0.5 硬编码清零 → src/base/money/escrow.ts calculateMultiPartySplit 原语（resolve/settle 双口径同源）+ 主写回带 fund_status CAS 乐观锁（并发/重复请求 409 OPTIMISTIC_LOCK_CONFLICT）；② payment/release 补 generateComplianceSplitInstruction（BANK_ESCROW 合规分账指令 + wallet_logs split_instruction 留痕 + 响应携带）；③ finance/overview 可用余额估算 0.95 → calculateProviderSettlement 净得口径；④ finance/withdraw 余额校验 → verifyFundSafetyGuard（NaN/负数防御）；⑤ sos/trigger 接入 crisis-tracker triggerCrisisEscalation + recordBreadcrumbPoint（危机审计轨迹）；⑥ 新增路由收编测试 7 例（orders 5 + overview 2）；门禁：tsc 0 错 + **1465/1465 全绿**（vitest 591→598 +7）+ ARCHITECTURE_TAXONOMY D-4/P0-2 标记 🟢 已完全闭合 + E2E flaky 修复 2 处（trust-open 行程过期态改条件轮询、governance 封禁按钮 .first() 与 nth(0) 对齐），终轮 12/12 PASS | |
| 2026-08-18 | `1ed8b62` | **/api/tts 生产路由 Webpack 空桩崩溃与流挂起根治**：① 打包层：next.config.ts 追加 `serverExternalPackages: ['ws','bufferutil','utf-8-validate']`（根治空桩：webpack 将 ws 可选原生加速器 bufferutil 打成 `()=>{}` 空模块 → require 不抛错 → ws 内置 try/catch 不触发 → 掩码 wrapper 安装 → 发帧 `TypeError: b.mask is not a function` → msedge-tts 流永不产出/永不结束 → 路由无限挂起，全天 unhandledRejection 刷屏；外置后运行时 require 抛错 → 纯 JS 回退，与 dev 行为一致）；② 环境层：restart-prod.mjs 注入 WS_NO_BUFFER_UTIL=1/WS_NO_UTF_8_VALIDATE=1（PS 5.1 经 $env: 继承）双保险；③ 请求层：route.ts 8s 总预算看门狗（GLM fetch AbortSignal + edge 消费 Promise.race，超时 tts.close() + 504 交客户端 speechSynthesis 降级链，消费侧错误双分支收敛杜绝二次 unhandledRejection）；④ route.test.ts +5 例（停滞流确定性超时/快流拼接/空流/报错流/竞速不误伤）；⑤ 实测闭环：build exit 0 + 产物复核（route.js 无 b.mask 包装器）+ 真实 HTTP POST /api/tts → **200 audio/mpeg 14544B 1.64s**（修复前同请求永久挂起）+ 错误日志 0 unhandledRejection + 正式实例复测 200/26KB/1.99s；门禁：tsc 0 错 + **1473/1473 全绿**（vitest 598→603 +5）+ 收敛门禁 exit 0（CONVERGENCE-LOG 条文 #10 已登记） | |
